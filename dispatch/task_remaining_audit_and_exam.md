# 任務：Stage4 測試專區與平台剩餘稽核

## 背景
使用者要求全平台逐筆稽核，因第一章至第八章來源資料夾曾被刪除兩次，必須避免再次清理、移動、覆蓋或誤提交使用者來源資料。

已完成狀態：
- Stage1 來源檔案盤點已完成，缺漏清單：`來源檔案缺漏清單_FR10-01比對.md`。
- Stage2 各部門頁 vs WI10-01 法定受訓 SOP 缺漏已修復，commit `509c86d`。
- Stage3 年度行事曆完成狀態已依民國年度隔離，commit `3bbb7ff`。

本任務處理剩餘 Stage4 與必要的交接整理。

## 絕對安全限制
1. 公司 SOP/WI/FR 原文、題庫答案來源段落、個資、成績、內部機密不得送 NVIDIA 或其他雲端免費模型。
2. 逐題答案回 SOP 核對屬於敏感內容，必須由 Claude/Codex 本機處理，或使用本地模型；不可派給 NVIDIA 雲端模型。
3. 不得刪除、移動、改名、覆蓋第一章至第八章、官方常見缺失來源或任何未符合 `AI名稱_` 前綴 / `_暫存` 後綴的檔案資料夾。
4. 暫存檔檔名必須以 `_暫存` 結尾；下載檔必須以 AI 名稱前綴，例如 `codex_檔名.pdf`。
5. 不得使用 `git clean`、廣泛刪除或依未追蹤狀態清理檔案。

## 可派給 OpenCode/NVIDIA 的範圍
僅限不含 SOP 原文與個資的程式結構任務，例如：
- 檢查抽題函式是否純隨機 slice。
- 設計抽題分層演算法草案。
- 檢查題庫資料結構欄位是否一致。
- 產出不含 SOP 原文的差異摘要。

若派工，任務書只能包含必要規格與檔名，不可包含 SOP 原文段落。

## 不可派雲端、必須本機完成的範圍
- 逐題比對 `examTemplates.*.draftQuestions` 與第一章至第八章 SOP/WI/FR 原文。
- 判斷答案是否符合 SOP 原文。
- 從 SOP 原文新增題目、答案或解析。
- 任何涉及公司內部文件內容的內容修正。

## 具體工作項目

### A. HR 補政府公開缺失題
1. 檢查 `examTemplates.hr` 是否缺政府公開常見缺失題。
2. 題目來源只能使用既有 `官方常見缺失來源/`、TFDA/PIC/S 公開來源或使用者確認來源。
3. 若來源不足，只列「需使用者補來源」，不可自行推測。
4. 若來源足夠，新增題目仍需標記為草稿，不得宣稱正式啟用。

### B. 抽題改分層
1. 檢查目前 `buildQuestions` 是否純隨機 slice，是否會導致某些 SOP 或共同基礎完全沒抽到。
2. 設計並實作分層抽題：
   - 共同基礎 DM10-01 / WI10-01 至少覆蓋。
   - 每個 roleDocs 專業 SOP 原則上至少 1 題；若題數不足或 SOP 數大於題數，需輪替且記錄本次抽到來源。
   - 政府公開缺失題若該職責已有題目，年度測驗需至少覆蓋 1 題；新人測驗可依題數決定是否納入。
   - 題目仍需洗題與洗選項。
3. 不得改變 `options[0]` 為正解的資料層慣例；顯示時由既有洗牌邏輯處理。

### C. 275 題逐題答案回 SOP 核對
1. 只能本機核對，不派雲端。
2. 逐一盤點 7 個模板：
   - doc
   - hr
   - purchase
   - sales
   - pharmacist
   - warehouse
   - qa
3. 每題確認：
   - 題目是否有來源文件支持。
   - `options[0]` 是否為正解。
   - 錯誤選項是否不會造成錯誤制度教學。
   - 是否有冷鏈、冷藏倉、SOP 路徑、內部維護語外露。
4. 若無法在來源中確認，列為「需使用者/講師確認」，不得自行修成正式答案。

## 驗收要求
- `JS_PARSE_OK 1`
- `node verify_facts_ghpages.mjs`：1296/1296
- 禁用字掃描：冷鏈/冷藏倉/冷藏庫/SOP 路徑/內部題庫審查欄位不得前台外露。
- 若修改 HTML/CSS/JS：commit + push 後用 GitHub Pages 永久網址驗收。
- 更新 `HANDOFF.md` 與第二大腦專案 note / 踩坑紀錄。

## 輸出要求
- 稽核結果寫入 `dispatch/result_remaining_audit_and_exam.md`。
- 若只完成部分，明確分成：
  - 已完成
  - 需使用者補來源
  - 不可派雲端、待本機核對
  - 已驗收項目
