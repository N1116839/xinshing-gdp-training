# 新勝醫藥 GDP 智慧查詢 — 正式專案規範文件

> 版本：2.4　　建立：2026-05-28　　依據：15 次開工踩坑點整合＋部門頁精準性＋三階段藍圖＋帳號審核＋知識安全邊界＋資料庫維護機制＋常見缺失來源規則
> 適用範圍：`HTML資料庫/新勝GDP資料庫.html` **全頁面**（智慧查詢、年度行事曆、各部門頁、互動元件、Firebase 功能）
> **本文件為規範，不修改任何 HTML / Firebase / 程式碼**

---

## 零、教材整體目的（開工前必讀）

### 0.1 這份教材是什麼

本 HTML 資料庫是**新勝醫藥 GDP 內部教育訓練平台**，不是對外簡報、不是靜態文件、不是資料查詢系統。

**目標讀者：** 新進員工、各部門需應對 GDP 稽查的人員

### 0.2 教材四大核心目標（每頁設計都必須服務這四點）

| # | 核心目標 | 具體說明 |
|---|---------|---------|
| 1 | **GDP 是什麼** | 讓員工理解 GDP 的法規背景、為何重要、與自己工作的關係 |
| 2 | **常見缺失為何** | 讓員工知道稽查員最常抓到什麼、哪些文件最容易出問題 |
| 3 | **稽查員詢問時如何回答** | 讓員工能用自己的話、正確且自信地回答稽查員的問題 |
| 4 | **什麼時候要做文件** | 讓員工知道哪些時間點要填哪些表單，不等到稽查才手忙腳亂 |

**第三點與第四點最直接影響員工日常**：

- 第三點（稽查應答）→ 有稽查員來的時候知道怎麼說
- 第四點（文件時間點）→ 平常工作知道什麼時候要填什麼表

**正確回答範例（第三點）：**
> 稽查員問：「你們公司倉庫溫度是多少度啊？」
>
> 員工應回答：「15 度到 25 度，因為我們是常溫藥品。警戒值設在 23.5 度，超過的話系統會做提醒。」

這種回答的特點：
- 直接說出數字（15-25°C）
- 說明為何這樣設定（常溫藥品）
- 說出應對機制（警戒值、系統提醒）
- **不念 SOP 條文**、不說「依 DP33-01 規定」

**文件時間點範例（第四點）：**
> 倉管人員知道：「每兩個月要做庫存盤點，填 FR55-03；到貨時當天要填 FR54-01。」
> 不需要等組長提醒，平常就知道觸發點。

### 0.3 教材六個輔助問題（讓上述四大目標更完整）

| 問題 | 說明 | 對應目標 |
|-----|------|---------|
| GDP 是什麼 | 法規背景、為何重要 | 目標 1 |
| 為什麼要做 | 法規要求、稽查風險 | 目標 1、2 |
| 稽查看什麼 | 常見缺失、稽查員問法 | 目標 2、3 |
| 要做哪些資料 | 表單編號、文件清單 | 目標 4 |
| 怎麼做 | 作業程序、執行方式 | 目標 3 |
| 什麼時候做 | 頻率、時間點、觸發條件 | 目標 4 |

### 0.4 教材品質底線

- **完整**：每個部門的 GDP 責任都有涵蓋
- **可查**：每個事實都能追溯到 SOP/WI/FR 原文
- **可快速記憶**：不放冗長純文字，用互動、圖表、短句
- **多互動**：每頁至少有一個真正的互動元件（非靜態排版）
- **資料正確**：數字、頻率、表單號、人員職稱符合文件原文
- **員工能開口說**：任何一個 fact，員工看完後要能用自己的話回答稽查員
- **員工能主動做文件**：員工看完第四目標後，不需要別人提醒就知道哪天、哪個觸發條件要填哪張表

---

## 一、GDP 智慧查詢 fact 拆解原則

### 1.1 核心定義

每一筆 `fact` 只能回答**一個可獨立問答的問題**，且只對應一個 SOP/WI/FR 的一個具體事實。

| 正確示範 | 錯誤示範 |
|---------|---------|
| `fact-inventory-cycle`：盤點頻率（每兩個月） | 把「盤點頻率＋溫度條件＋不符合品處理」塞入同一筆 |
| `fact-temp-mapping-cycle`：溫度測繪週期（每三年）| 把「溫度測繪週期＋夏冬月份＋測繪天數」混放一欄 |

### 1.2 拆解判斷準則

- **能獨立成一個稽查員問句** → 應是獨立 fact
- **同一 SOP 若涵蓋多個主題** → 每個主題各建一筆 fact
- **clarify（流程型）條目** → 保留廣泛描述，但所有具體 who/when/how many 必須拆至 fact

### 1.3 fact 標準格式

```javascript
docs.push({
  id: "fact-[主題]-[面向]",          // 例：fact-temp-mapping-cycle
  sourceType: "fact",
  topic: "簡短題目（5-15字）",
  international: "PIC/S GDP 相關條文一句話",
  taiwan: "食藥署規定一句話（或「食藥署無明文規定，由公司內部自行訂定」）",
  xinshing: "新勝做法一句話（含數字/表單號碼/人員職稱，可直接回答稽查的短句）",
  sop_ref: ["來源SOP文件編號"],
  keywords: ["精準關鍵字（覆蓋各種問法，不跨主題）"]
});
```

### 1.4 拆解後驗收要求

拆完 fact 後，**必須先測試**：問 A 只出現 A，不出現 B 或 C。

---

## 二、Keywords 設計規則

### 2.1 覆蓋原則：動詞變體 × 受詞變體

每個 fact 建立時，keywords 必須涵蓋：
- **動詞變體**：何時 / 什麼時候 / 幾點 / 幾天 / 多久 / 幾個月 / 如何 / 由誰
- **受詞變體**：表單號碼（FR54-01）/ 表單中文名 / 主題詞（盤點/測繪）

**交叉範例（`fact-receiving-when`）：**
```
"何時填FR54-01", "FR54-01何時填", "何時填", "何時驗收",
"什麼時候填FR54-01", "什麼時候驗收", "到貨驗收時間"
```

### 2.2 最短有意義單詞規則

凡有長 keyword，**同時必須補最短形式**：

| 長 keyword | 必須也有 |
|-----------|---------|
| 病媒防治 | 病媒 |
| 倉庫盤點 | 盤點 |
| 溫度測繪 | 溫測（缺字問法）|
| 衛生管理 | 衛生 |

### 2.3 工具/系統名稱禁止單獨作為 keyword

**規則：正航、GDPHUB、嘉里等工具/系統名稱，不可單獨作為 fact 的 keyword。**

只有「系統名稱＋主題詞」的組合才有意義：
- ❌ `"正航"` 單獨放入 `fact-purchase-order-who`
- ✅ `"正航採購單"` 放入對應採購 fact
- ✅ `"正航是什麼"`, `"正航系統"`, `"正航是不是"` 放入 `clarify-computer-system`

### 2.4 clarify 與 fact 的 keywords 分工

導入 fact 架構後：
- **clarify 條目**：只保留廣泛流程型 keywords（如「進出貨流程」「到貨驗收流程」）
- **具體 who/when/數字/表單號問法**：全部移到對應的 fact 條目，不得留在 clarify

### 2.5 快速問題按鈕

按鈕 `data-q` 的完整字串也必須是某個 keyword 的子字串（`.includes()` 匹配），否則快速按鈕點擊等同無效。

---

## 三、Intent Regex 禁止規則

### 3.1 禁止通用詞作為 intent regex 觸發條件

**踩坑根因：** 正航意圖比對 regex 含「溫度|電腦化系統」→ 命中任何含溫度的文件，包括進出貨、委外等完全不相關的條目。

**禁止規則：**

| 禁止寫法 | 原因 |
|---------|------|
| intent regex: `溫度` | 觸發所有含「溫度」的文件 |
| intent regex: `電腦化系統` | 觸發電腦化系統以外的條目 |
| intent regex: `幾個月` | 夏季幾個月 / 回收幾個月 全部誤觸 |
| intent regex: `倉庫` | 所有倉庫相關條目全部觸發 |

### 3.2 正確的 intent regex 精度要求

**規則：intent regex 要精確到文件編號層級，不可用通用詞。**

| 正確寫法 | 說明 |
|---------|------|
| `正航\|fr5701\|dp3501` | 只有含「正航」或對應文件編號的 fact 才加分 |
| `盤點\|fr5503\|dp5501` | 只有盤點相關文件才通過 |

### 3.3 硬排除規則（已實作，不可移除）

- 查詢含「盤點」→ 文件必須含 `盤點|fr5503|dp5501`，否則 score=0
- 查詢含「正航」→ 文件必須含 `正航|fr5701|dp3501`，否則 score=0

---

## 四、同分競爭處理規則

### 4.1 clarify 蓋過 fact 的防治

**問題根因：** clarify 條目 keywords 裡保留了專屬 who/when 字串，與 fact 直接競爭，且 clarify 因關鍵字豐富往往得更高分。

**處理規則：**
1. 發現 clarify 搶先 fact 時，首先檢查 clarify 的 keywords 是否有「不屬於 clarify 層級」的具體問法
2. 移除 clarify 的專屬問法 keyword，只保留廣泛流程型描述
3. 確認對應 fact 的 keywords 補齊後，重新驗收

### 4.2 評分機制（記錄現況）

- `sourceType:"fact"` 且 strongHit → +55 分（高於查詢校正 +45）
- `project-source-file` 評分上限：12 分（避免原始同步文字蓋過正式 KB）
- maxScore ≥ 50 時閾值：0.45（原 0.35，已收緊）

### 4.3 同分時的決策順序

1. sourceType 為 `fact` 的條目優先
2. 次之為 `clarify` 條目
3. `project-source-file` 最低優先（評分上限 12）

---

## 五、多筆結果顯示規則

### 5.1 現行邏輯（已實作）

命中 fact 時，最多顯示 4 筆完整三欄卡；超過 4 筆提示縮小查詢。

### 5.2 不相關結果跟著出現的處理

**問題：**「公司有沒有冷藏倉」第一筆正確，後面不相關的溫度警戒值、測繪週期、盤點頻率也跟著出現。

**處理規則（二選一）：**
- 方案 A：最高分與第二名分差 > 30 時，只顯示最高分一筆
- 方案 B：收窄各 fact 的 keywords，確保不被上位詞（「倉庫」「溫度」）誤觸

**現階段建議優先採用方案 B**（keyword 層面解決，不需改程式邏輯）。

### 5.3 初始訊息清除

**踩坑記錄：** 查詢執行後用 `insertAdjacentHTML("beforeend")` 附加氣泡，但從未清除初始的「尚未查詢」訊息，導致兩個訊息並存。

**規則：** 在開始處理查詢時，先清空 `kbMessages` 內容（`box.innerHTML = ""`），再附加 loading → 結果/錯誤。

---

## 六、三欄回答標準：PIC/S GDP、台灣 GDP、新勝做法

### 6.1 三欄各自的填寫原則

| 欄位 | 填寫標準 | 禁止出現 |
|-----|---------|---------|
| `international` | PIC/S GDP 相關條文一句話，原文摘要 | 「請查閱 PE 011」/ SOP 索引佔位文字 |
| `taiwan` | 食藥署「西藥優良運銷準則」相關規定一句話 | AI 備忘（「目前資料索引寫到…」）|
| `xinshing` | 新勝做法一句話，含數字/表單號/人員職稱 | AI 指令語、否定推論（「不可寫成…」）|

### 6.2 食藥署欄（`taiwan`）絕對禁止放公司文件

**踩坑根因（2026-05-25）：** 三層對照的「台灣食藥署」欄一開始混入 DP/WI/FR 文件編號，稽查員看到是誤導，且法規欄就不該出現公司內部文件。

| 食藥署欄正確放 ✅ | 食藥署欄禁止放 ❌ |
|---------------|---------------|
| 「西藥優良運銷準則第X章」| DP12-01、WI22-01 等公司文件 |
| 「藥事法第 XX 條」| FR54-01 等公司表單 |
| 「食藥署 GDP 指引」| 公司作業程序說明 |

**公司文件獨立放**：若需要列出對應的 SOP/WI/FR，應放在 `xinshing` 欄或額外的「參考文件」欄，不放進 `taiwan` 欄。

### 6.4 無明文時的標準寫法

- 法規**有明文規定**由誰負責 → 依法規原文填
- 法規**無明文規定** → 填「食藥署 GDP 規範無明文規定，由公司內部自行訂定」

### 6.5 「新勝做法」欄的定位與命名原則

**「新勝做法」欄的本質：** 這是「來自新勝醫藥公司文件的執行內容」，讓員工一看就知道：**這是我們公司的規定，稽查員問我時我就這樣回答。**

#### 顯示標籤建議

前台顯示時，「新勝做法」的標籤應讓人一眼看出是「公司文件的部分」，可考慮：
- **「我們公司怎麼做」**
- **「新勝醫藥執行方式」**
- **「公司做法（依內部文件）」**

避免讓人誤解為「AI 整理的建議」或「一般業界做法」。

#### 欄位內容：寫成員工的回答，不是 SOP 條文

**目標：** 員工看完這欄，面對稽查員的問題，能直接開口說出來，不需要再翻文件。

| 寫法類型 | 範例 | 是否正確 |
|---------|------|---------|
| **✅ 員工回答語氣** | 「我們是常溫藥品，倉庫溫度維持 15 到 25 度，警戒值設在 23.5 度，超過會提醒。」 | ✅ |
| **✅ 直接短答** | 「每兩個月全面盤點一次。」 | ✅ |
| ❌ SOP 條文複製 | 「依 DP33-01 第三節規定，倉庫溫濕度應符合儲存條件要求。」 | ❌ |
| ❌ 整段流程 | 「收到貨物後，進貨人員填寫 FR54-01，採購部列印紀錄表，品保核對…」 | ❌ |

#### 稽查問答格式（標準答案範例）

> **稽查員問：** 「你們公司倉庫溫度是多少度啊？」
>
> **員工應答（對應 `xinshing` 欄內容）：**
> 「15 度到 25 度，因為我們是常溫藥品。警戒值設在 23.5 度，超過的話系統會做提醒。」

這種答案的結構：**數字 → 原因 → 例外處理機制**

每個 fact 的 `xinshing` 欄都應朝這個格式靠攏。

### 6.6 新勝做法欄位的品質標準

- **正面事實描述**：員工可直接說出口的短句
- **員工可直接回答稽查員**，不放冗長 SOP 敘述
- **只寫 SOP/WI/FR 原文有的內容**，不推論、不補充
- **禁止的寫法範例：**
  - ❌「不可寫成冷藏倉」（AI 指令）
  - ❌「本頁資料來源為…」（資料來源說明）
  - ❌「不是溫度監控系統」（AI 否定推論，文件沒有這樣記載）
  - ❌「目前資料索引尚未涵蓋…」（AI 備忘）
  - ❌「依 DP33-01 第X節規定…」（SOP 條文語氣，不是員工的話）

### 6.6 AI 建資料時的事實核對表（勿搞錯新勝的正確值）

> **此表目的：** 防止 AI 在建立 KB 條目時把新勝的正確事實寫錯。
> **此表不是教材的「常見缺失」清單**——常見缺失另見 6.7 節。

| 主題 | 新勝正確事實 | AI 最容易寫錯成 | 說明 |
|-----|------------|--------------|------|
| 倉庫類型 | 常溫倉，**無冷藏倉** | 冷藏倉、冷鏈 | 公司只有一間常溫倉 |
| 溫度記錄頻率 | 每 5 分鐘記錄一次 | 每 15 分鐘 | 自動記錄器頻率 |
| 內部稽核頻率 | 每年 12 月（DP82-01）| 每 6 個月 | 每年一次 |
| 溫度測繪週期 | 每三年或重大變更後；夏季 7-9 月、冬季隔年 1-3 月，各 7 天 | 每年 | 三年週期 |
| 溫度警戒值 | 23.5°C | 其他任意數值 | 此數值來自 DP33-01 |
| 庫存盤點頻率 | 每兩個月全面盤點一次（DP55-01）| 每月 / 每季 | 兩個月一次 |
| 模擬回收演練 | 每年執行 1 次 | 每半年 / 每季 | 每年一次，有紀錄 |
| 紀錄保存年限 | 至少 5 年 | 3 年 / 7 年 | 最短 5 年 |
| 正航系統 | 確效過的進銷存電腦化系統（FR57-01）| 溫度監控系統 | ERP，不是溫控設備 |
| 物流門管制時間 | 15:00-15:30 | 14:00 | 收貨期間進貨人員不得執行進貨作業 |

---

### 6.7 教材「常見缺失」的來源規則

> **硬性規則：教材中「常見缺失」的內容，必須來自實際公開的稽查資料，不可由 AI 自行推測。**

#### 合法來源（只能從這三類取材）

| 來源類型 | 說明 | 取材方式 |
|---------|------|---------|
| **PIC/S GDP 稽查指引** | PIC/S 官方發布的 GDP 查核重點與常見缺失 | 讀 PE 011 系列文件 |
| **台灣食藥署公開資料** | 食藥署公布的藥品運銷稽查結果、GDP 常見缺失報告 | 食藥署官網查詢 |
| **台灣 GDP 稽查公告** | 食藥署對藥品批發商的查核通報 | 官方公告 |

#### 禁止的來源

| 禁止 | 原因 |
|-----|------|
| AI 自行推測「可能有人會犯這個錯」| 未必是真實稽查缺失，誤導員工 |
| 與新勝正確做法相反的假設 | 例：新勝溫度 23.5°C → 寫「其他數值是常見錯誤」這不是稽查缺失 |
| 業界傳說或非官方資料 | 不具稽查公信力 |

#### 正確範例：兩種缺失類型的差異

| 缺失類型 | 範例 | 說明 |
|---------|------|------|
| **根本未執行** | 「公司未設定溫度警戒值」 | 稽查員最常見 → 壓根沒做 |
| **根本未執行** | 「未執行模擬回收演練，無演練紀錄」 | 有規定但從未做 |
| **記錄不完整** | 「訓練紀錄缺考核結果」 | 有做但文件不完整 |
| **執行不一致** | 「實際盤點間隔超過 SOP 規定」 | 做了但未照 SOP 頻率 |
| **職責不清** | 「無法確認溫度記錄的審核責任人」| 文件沒有明確誰負責 |

#### 「常見缺失」建立前的必做確認

```
□ 這個缺失是否出現在 PIC/S PE 011 或食藥署公告中？
□ 如果是，能引述具體來源嗎？（「PIC/S PE 011-3 附件X」或「食藥署OO年查核報告」）
□ 缺失的描述是「沒有做 X」或「X 做得不完整」，不是「X 用了錯誤數值」？
□ 這個缺失跟新勝的實際做法有直接對應嗎？（讓員工看了知道自己公司有沒有這個問題）
```

---

## 七、前台內容同步規則

### 7.1 必須同步的六個區塊

**硬性規則：** 改動任何一個 fact 時，以下六個區塊必須全部同步。

1. GDP 智慧查詢 fact（HTML kbStore + Firestore `gdpKnowledgeBase`）
2. 各部門頁面的「作法」與重點摘要
3. 「規範到現場」對照區
4. 「部門文件閱讀地圖」
5. 「稽查重點互動核對」
6. FAQ / 快速問題按鈕與手動搜尋入口

### 7.2 同步執行順序

1. 決定要修改的 fact 前，先列出「此 fact 影響哪些前台區塊」
2. 修改 fact 資料
3. 逐一驗收上述六個區塊是否顯示一致
4. 確認無落差後，才算此 fact 修改完成

### 7.3 標題一致性規則

**規則：標題必須符合實際內容，節點數/按鈕數/欄位數必須與顯示內容一致。**

- 若「規範到現場」只有三欄（PIC/S GDP / 台灣食藥署 / 新勝做法），**不得標成「四層對照」**
- 曾有四層，後來移除第四欄，必須同步更新標題

### 7.4 文件連結政策

依老闆文件保護指示（2026-05-27 確立）：
- **不做**可點擊文件按鈕，不顯示 SOP 路徑或內部文件細節
- 文件名稱以**純文字**列示即可（員工知道要申請哪份文件）
- 外部法規連結（PIC/S、食藥署官網）屬公開資訊，不受限制
- 員工如需閱讀原文件，向文管部填單申請調閱

---

## 八、GitHub Pages 驗收規則

### 8.1 驗收的唯一正式來源

```
https://n1116839.github.io/xinshing-gdp-training/
```

**本機驗收只算預檢，不算完成。** 必須 push 並從上述 URL 確認才算完成。

### 8.2 驗收標準程序

```
1. git commit + push 到 codex/gdp-html-training-pages
2. 等待 GitHub Pages 部署（約 1-3 分鐘）
3. 開啟永久網址
4. 對應修改區域逐項確認（點擊/輸入/查詢）
5. 在 HANDOFF 寫「已用 GitHub Pages URL 驗收：○○正常、○○正常」
6. 若發現錯誤 → 修正 → 再次 commit + push → 重新驗收
```

### 8.3 驗收記錄要求

**沒有驗收記錄 = 這次修改未完成。**

HANDOFF 中必須明確標示：
- ✅ 本機 N/N 通過（預檢）
- ✅ 已 push GitHub Pages，永久連結驗收通過

### 8.4 驗收腳本使用方式

使用 `node verify_facts_ghpages.mjs` 驗收時：
- 本機模式：對 HTML 本機文件抽取 kbStore 資料並模擬搜尋
- GitHub Pages 模式：第 134-135 行改為 `fetchUrl GH_PAGES_URL`

---

## 九、收工格式

### 9.1 收工必做五項（缺一不可）

```
□ 1. 更新 HANDOFF.md（本次完成 + 下次待辦 + 踩坑補記）
□ 2. 更新 踩坑紀錄.md（G:\我的雲端硬碟\我的第二大腦\踩坑紀錄.md）
□ 3. 更新 專案 note（G:\我的雲端硬碟\我的第二大腦\專案\2026codex AI測試.md）
□ 4. git commit（含本次所有改動）
□ 5. git push origin codex/gdp-html-training-pages
```

只口頭說收工、不執行上述五項，均視為**未完成收工**。

### 9.2 HANDOFF 更新格式

每輪收工在 HANDOFF 新增一節，包含：

```markdown
## 本輪完成（YYYY-MM-DD 第N次開工）commit [hash]

### [主要工作標題] ✅

[完成內容條列]

**驗收：** 本機 N/N 通過，已 push GitHub Pages

### ⚠️ 尚未完成（下次開工優先）

| 章節 | 待補項目 |
|------|---------|
| ... | ... |

**下次開工必做：**
1. ...
2. ...
```

### 9.3 踩坑記錄格式

每個踩坑必須包含：
1. **問題現象**（使用者看到了什麼）
2. **根本原因**（程式/資料/流程哪裡出了問題）
3. **修正方向**（具體可執行的修正步驟）
4. **規則**（未來要遵守的原則，防止重犯）

---

## 十、每章測試標準

### 10.1 測試數量要求（永久執行）

| 測試層級 | 要求 |
|---------|------|
| 每個問題 | 至少 **10 種不同問法**（同主題不同說法各算 1 種） |
| 每章 | 至少 **10 個不同問題**（不同主題） |
| 每部門 | 至少 **20 個不同問題** |
| 重點覆蓋 | 國際法規（PIC/S GDP）與台灣食藥署**常見缺失項目**必須包含 |

「不同問法」範例（倉庫盤點，以下算 10 種）：
1. 倉庫多久盤點一次
2. 盤點頻率是什麼
3. 多久盤點一次
4. 庫存多久盤點
5. 盤點週期
6. 幾個月盤點一次
7. 庫存盤點怎麼做
8. DP55-01 盤點
9. 庫存管理盤點頻率
10. 倉庫庫存幾個月檢查

### 10.2 測試的正確順序：搜尋結果只是假設，文件才是答案

> ⚠️ **這是最常被忽略的規則：查到答案 ≠ 答案正確。**

**正確的測試流程：**

```
Step 1：用 GDP 智慧查詢輸入問題
          ↓
Step 2：取得搜尋結果（fact 三欄內容）
          ↓
Step 3：回到資料夾，開啟對應的 SOP/WI/FR 原文
          ↓
Step 4：比對搜尋結果與文件原文是否一致
          ↓
Step 5a：一致 → 此題驗收通過 ✅
Step 5b：不一致 → 記錄差異，修正 KB，重新測試
```

**禁止的做法：**
- ❌ 查到結果就直接標記「通過」
- ❌ 結果「看起來合理」就當作正確
- ❌ 沒有找到對應的 SOP 原文就宣告驗收通過

**為什麼必須回查文件：**
KB 資料是人工建立或 AI 生成的，可能含有：
- 數字錯誤（溫度、頻率、期限）
- 事實錯誤（冷藏倉、正航用途）
- AI 自行補充的推論（文件沒有的描述）
- 亂碼（偉→偵、揃→揀）

搜尋引擎只負責「找到最相關的那筆」，不負責「那筆是否正確」。**正確性驗證是人（或 AI 讀文件）的責任，不是搜尋引擎的責任。**

### 10.3 測試驗收三件事（文件核對完才能做這三件事）

1. **第一名是否正確**：命中的 fact 就是該問題的答案（已與原文核對）
2. **有沒有多餘不相關結果**：第二名以後不應出現牛頭不對馬嘴的條目
3. **新勝做法是否能直接作為稽查應答**：員工看到此答案，稽查員問時可直接回答

### 10.4 修正時的批次歸因規則

發現問題時，先歸因，再修正，不可亂加 keyword：

| 症狀 | 正確處置 |
|-----|---------|
| 泛命中（答非所問） | 收窄 keyword、降低通用詞權重、加硬排除 |
| 查不到（沒有結果） | 補自然問法、缺字問法、同義詞、文件編號變體 |
| 回太長（整段 SOP） | 再拆 fact，不把整段塞進一筆 |
| 答案像 AI 備忘 | 改成正式三欄教材語氣；備忘寫進第二大腦，不進 KB |
| clarify 蓋過 fact | 移除 clarify 的專屬 who/when keywords |

### 10.5 宣告「完成」的條件

**不得宣稱一次到 100%。** 正確流程：

```
1. 選一章做完整樣板
2. 從 SOP/WI/FR 拆 fact
3. 每個 fact 補 10 種問法
4. 建測試表（10 個問題 × 10 種問法）
5. 本機預檢
6. push + GitHub Pages 永久網址驗收
7. 驗收通過後，複製同一流程到其他章
```

**宣告一章完成的最低條件：**
- 測試表 10 題全部通過
- 三欄內容符合 SOP 原文（可追溯）
- GitHub Pages 驗收記錄已寫入 HANDOFF

---

## 十一、禁止 AI 備忘進前台

### 11.1 三欄禁止出現的語句類型

KB 的三個欄位（`international` / `taiwan` / `xinshing`）是學員看到的**教材內容**，任何對 AI 的指令語、備忘、校正提示一律不得出現。

| 違規語句類型 | 具體範例 | 違規原因 |
|------------|---------|---------|
| AI 校正提示 | 「不可寫成冷藏倉」 | 對 AI 的指令，不是法規說明 |
| AI 否定推論 | 「不是溫度監控系統」 | 文件沒有這樣記載，不可補充否定句 |
| 資料來源說明 | 「此筆來自本頁已整理的 SOP 索引」 | 索引佔位，不是實質答案 |
| 開發者備忘 | 「目前資料索引寫到…但未明定夏冬月份」 | AI 的工作筆記，不是教材 |
| 追溯提示 | 「請依文件編號回到正式核准文件確認完整條文」 | 員工需要直接答案，不是索引路徑 |
| 未完成提示 | 「（待補）」「（暫缺）」 | 教材不應有未完成的欄位 |

### 11.2 正確的處置方式

- 有任何需要提醒 AI 的注意事項 → 寫進踩坑紀錄.md 或 HANDOFF.md，**不進 KB 欄位**
- 三欄有無法填寫的情況（法規無明文）→ 用標準寫法：「PIC/S GDP 無明文規定」/「食藥署 GDP 規範無明文規定，由公司內部自行訂定」
- xinshing 欄位找不到 SOP 依據 → 欄位留白或填「依公司內部規定執行」，**不補推論句**

### 11.3 驗收時的掃描規則

每次宣告 KB 完成前，對所有 fact/clarify 條目執行字串掃描，確認三欄不含：

```
不可寫成 / 不得寫成 / 此筆來自 / 文件未載明 / 目前資料 /
待補 / 暫缺 / 請依文件編號 / 本教材 / 資料來源
```

---

## 十二、資料來源優先順序

### 12.1 優先順序（由高到低）

1. **SOP/WI/FR 原文**（資料夾內文件，逐份讀取）
2. **使用者在對話中明確確認的最新版事實**（例：「溫度測繪夏季是 7-9 月」）
3. **Firestore `gdpKnowledgeBase`**（已核對且部署的資料）
4. **HTML kbStore fallback**（最後兜底，應與 Firestore 一致）

**若同一事實有新舊版本：以資料夾內較新的實際文件內容為準。**

### 12.2 來源衝突時的處置

| 衝突情形 | 處置方式 |
|---------|---------|
| Firestore 與 HTML fallback 不一致 | 兩邊同步修正，不可只改其中一邊 |
| SOP 原文與現有 KB 資料不符 | 以 SOP 原文為準，更新 KB 並記錄修正 |
| 目錄/行事曆與 SOP 原文有出入 | 列出差異，請使用者確認後才更新 |
| 不同版本 SOP 內容有差異 | 以資料夾內日期較新的版本為準 |

### 12.3 不可做的事

- **不可自行依判斷增加文件上沒有的事情**（不補推論、不補背景知識）
- **不可只修一個地方**：同一事實在多處出現，必須統一一次性修正
- Firestore 資料若是 AI 生成，有高機率出現亂碼（偉→偵、揃→揀、雔→隔），每次建置後需逐欄讀回核對

### 12.4 測試時的來源驗證規則

**搜尋結果只是「KB 資料庫的回答」，不等於「正確答案」。**

測試時若搜尋到一筆 fact，必須：
1. 找到 `sop_ref` 欄位列出的來源文件
2. 打開資料夾內對應的 SOP/WI/FR 原文
3. 確認搜尋結果的數字、頻率、表單號、人員職稱與原文完全一致

若 KB 資料與文件原文不一致：
- **以文件原文為準**
- 修正 KB（HTML kbStore + Firestore 兩邊同步）
- 記錄修正內容到 HANDOFF 與踩坑紀錄

→ 詳細流程見第十章 10.2 節

---

## 十三、驗收失敗分類與對應處置

### 13.1 六種失敗類型

| 失敗類型 | 症狀描述 | 對應處置 |
|---------|---------|---------|
| **查不到** | 輸入問題，結果為空或「找不到命中資料」 | 補自然問法 keyword、缺字問法、同義詞、文件編號變體 |
| **泛命中** | 第一名正確，但後面出現大量不相關結果 | 收窄 keyword、加硬排除、降低通用詞權重 |
| **答太長** | 命中了，但顯示整段 SOP 流程而非直接答案 | 再拆 fact，每筆只回答一個問題 |
| **答非所問** | 命中了，但答案與問題完全無關 | 確認 keyword 是否有跨主題污染；檢查 intent regex |
| **同分錯誤** | clarify 蓋過 fact，或次要 fact 蓋過主要 fact | 移除 clarify 的專屬 who/when keywords；或補強 fact 的 strongHit keywords |
| **前台不同步** | 智慧查詢答 A，部門頁顯示 B | 依第七章同步規則，找出所有出現處並統一更新 |

### 13.2 失敗根因追蹤流程

```
發現失敗
  ↓
1. 先確認是哪種失敗類型（上表六種之一）
  ↓
2. 找出根本原因（keyword？資料粒度？clarify 污染？）
  ↓
3. 批次修正同類問題（不可只修一題就宣告完成）
  ↓
4. 修正後重跑整章測試表，確認無副作用
  ↓
5. 寫入踩坑紀錄（問題現象 + 根因 + 修正方向 + 規則）
```

### 13.3 禁止的修正方式

- **禁止只修一題**：發現泛命中，只收窄那一個 keyword，不檢查同章其他 fact 是否有同樣問題
- **禁止亂加 keyword**：查不到就把所有想得到的詞全加上去，導致泛命中
- **禁止收工前不重跑測試表**：每次修正後，相關章節的全部測試題都要重跑一遍

---

## 十四、開工必讀文件清單

### 14.1 每次對話開工，按順序讀

| 順序 | 文件 | 路徑 | 讀取目的 |
|-----|-----|------|---------|
| 1 | HANDOFF.md | `G:\我的雲端硬碟\2026codex\AI測試\HANDOFF.md` | 上次做到哪、下次必做什麼 |
| 2 | 踩坑紀錄.md | `G:\我的雲端硬碟\我的第二大腦\踩坑紀錄.md` | 長期規則、已知錯誤模式 |
| 3 | 專案 note | `G:\我的雲端硬碟\我的第二大腦\專案\2026codex AI測試.md` | 專案整體方向與決策記錄 |

### 14.2 補充讀取（需要時）

| 文件 | 路徑 | 何時讀 |
|-----|------|-------|
| AGENTS.md | 專案根目錄 | 跨 AI agent 共用規則確認 |
| CLAUDE.md | 專案根目錄 | Claude Code 特定開工指示 |
| GDP_智慧查詢規範.md | 專案根目錄（本文件）| 規則有疑義時查閱 |

### 14.3 從摘要接續時的特別提醒

**從摘要接續（context 壓縮）時，同樣必須先讀第二大腦與踩坑紀錄，再開始任何修改。**

摘要只記錄上次做到哪，不包含第二大腦的長期規則。兩者都讀才算完整開工。

### 14.4 開工後的第一步

讀完上述文件後，**詢問使用者從哪個待辦項目開始，不可自行決定**。

---

## 十五、禁止事項彙整

### 15.1 作業流程禁止事項

| 禁止行為 | 後果 | 正確做法 |
|---------|------|---------|
| 一次同時處理多個大項（KB ＋ 互動化 ＋ 字體 ＋ 面板）| 任一未通過驗收，全部被回退 | 一次只做一項，驗收通過後再做下一項 |
| 只用本機驗收就宣告完成 | 永久網址可能是舊版 | 必須 push 並從 GitHub Pages 永久網址驗收 |
| 只補單一 keyword 就收工 | 同類問題下次繼續踩 | 批次歸因，同類問題一次修完 |
| 宣告「KB 完成」前未跑測試表 | 只是修改了資料，不代表搜尋正確 | 每章 10 題 × 10 問法全部跑完才算完成 |
| 修 KB 未同步前台 | 智慧查詢答 A，部門頁顯示 B | 依七章同步規則，所有區塊統一更新 |
| 收工只口頭說收工 | 換電腦或換 AI 後進度斷鏈 | 必須執行九章五項收工程序 |

### 15.2 資料內容禁止事項

| 禁止行為 | 具體說明 |
|---------|---------|
| KB 欄位放 AI 指令語 | 詳見第十一章 |
| KB 欄位放否定推論 | xinshing 欄只放 SOP 有明確記載的正面事實 |
| 出現「冷藏倉」「冷鏈」 | 公司無冷藏倉，這是錯誤事實 |
| HTML 本身作為 KB 來源 | 搜尋結果會顯示 HTML 標籤與 JS 程式碼 |
| clarify 條目保留具體 who/when keywords | 導入 fact 架構後，這些 keywords 必須移至對應 fact |
| 工具名稱單獨作為 keyword | 正航/GDPHUB/嘉里單獨放 keyword，會跨主題誤命中 |

### 15.3 程式與部署禁止事項

| 禁止行為 | 說明 |
|---------|------|
| 修改 HTML 後不 push 就收工 | 永久網址仍是舊版 |
| Firestore 資料不讀回驗證 | 中文字元在 Firestore 容易亂碼（偉/揃/雔等） |
| push 後不等 GitHub Pages 部署就驗收 | 約需 1-3 分鐘，太快驗收看到舊版 |
| 繞過安全審核推送外部 remote | 必須取得使用者明確授權 |

---

## 十六、互動設計原則

### 16.1 「視覺化」的正確定義

本專案中「視覺化」= **真正的 HTML 互動元素**，不是靜態排版或卡片美化。

| 屬於互動式 ✅ | 不算互動式 ❌ |
|------------|------------|
| 滑桿（`<input type="range">`）| 改卡片排版、換圖示 |
| 拖拉（drag-and-drop、swipe）| 只加陰影、漸層 |
| 點擊叫出（Modal / Accordion / Tab）| Hover 顏色變化（無 JS 事件）|
| 輪播（Carousel，自動或手動）| 改字體大小 |
| 動態表單（即時驗證、條件顯示）| 加動畫說明文字 |
| JS 事件驅動的內容切換 | RWD 響應式佈局 |

### 16.2 互動類型對應使用時機

| 互動類型 | 適合的內容場景 |
|---------|-------------|
| Accordion / 展開收合 | 稽查重點清單、FAQ 列表 |
| Modal / Lightbox | 補充說明、文件申請通知、圖片放大 |
| Tab panel | 不同部門/法規層/頻率分類切換 |
| Checkbox / 勾選 | 稽查核對清單、學習進度 |
| Range slider | 風險等級、頻率視覺化 |
| Scroll trigger | 元素進入畫面時滑入顯現（fade-in）|
| Ripple / 下陷效果 | 按鈕點擊回饋 |

### 16.3 互動優先順序（H4 原則）

> **大更動不影響其他區域的先做；牽一髮動全身的擺最後；有副作用的測試項目最後處理。**

```
1. KB 精準搜尋（獨立，不影響其他 UI）
2. H1 員工登入（後端改動，不影響現有 UI 顯示）
3. H2 職稱別權限（依賴 H1）
4. H3 晉升審核（依賴 H1+H2）
5. 全站互動化升級（牽動全站 UI，擺最後）
```

---

## 十七、頁面結構規則（禁止重複區塊）

### 17.1 文件清單：只能出現一次

每個部門頁目前有三個文件相關區域，**只保留一個**：

| 區域名稱 | 保留建議 |
|---------|---------|
| 頂部 pills（`pagePills`）| 在有 `departmentDocumentMaps` 的頁面已隱藏，不得重複顯示 |
| 部門文件閱讀地圖（`documentMapSection`）| **建議保留**（分類最清楚） |
| 本頁相關 SOP（`docButtons`）| 與文件地圖重複，擇一 |

**規則：** `pagePills` 在有 `departmentDocumentMaps` 的頁面必須隱藏，不可同時顯示兩個文件清單。

### 17.2 時程資訊：只能出現一次

| 區域 | 處置 |
|-----|------|
| `schedule` 區塊（基本時程）| 與 `deptScheduleSection` 重複 |
| `deptScheduleSection`（頻率分 tab 互動）| **建議保留**（互動功能較完整）|

**規則：** 同一資訊出現兩次無附加價值，選功能較完整的保留，另一個移除。

### 17.3 kbSection 佈局規則

- `kbSection` 用 `panel()` 直接放入 `.grid`
- **不可**用 `kb-wrap` 包裝（會縮成 1 欄）

### 17.4 標題一致性

標題必須與實際內容完全吻合：

| 情境 | 規則 |
|-----|------|
| 「規範到現場」只剩三欄 | **不得**繼續標為「四層對照」 |
| 節點數、按鈕數 | 必須與實際顯示欄位數量一致 |
| 修改內容後 | 標題必須同步更新 |

---

## 十八、稽查重點區塊設計規範

### 18.1 現行問題

稽查重點區塊目前多為靜態查核句列表，不符合使用者期待。

### 18.2 正確格式

稽查重點區必須改為**學習型格式**，包含四個要素：

```
1. 常見缺失（稽查員最常抓到什麼）
2. 稽查員可能怎麼問（問句範本）
3. 公司如何回答（標準應答短句）
4. 要準備什麼證據（表單/記錄編號）
```

**範例（教育訓練）：**
> - 常見缺失：訓練紀錄不完整，或找不到考核結果
> - 稽查員問：「你們教育訓練怎麼做？合格標準是什麼？」
> - 公司回答：「分為內部與外部教育訓練，考核分數 ≥70 分合格，保存訓練紀錄。」
> - 準備證據：FR24-01（年度訓練計畫）、FR24-02（考核表）

### 18.3 互動型態要求

稽查重點區若要做互動化，必須使用以下之一：
- Accordion（展開每條缺失的詳細問答）
- Checkbox（員工自評已準備哪些證據）
- Tab（依缺失類型分類切換）

---

## 十九、手機版設計規則

### 19.1 下拉選單

**禁止**：手機主導覽依賴原生 `<select>`

**原因**：原生 `<select>` 容易受手機瀏覽器、外層捲動容器或點擊事件影響，操作體驗差。

**正確做法**：
- 改用自製大按鈕下拉面板
- 每個選項都是可點擊 `<button>`
- 同步顯示目前頁面狀態（目前選項文字更新）

### 19.2 桌機 / 手機分流原則

- 桌機左側導覽：分組按鈕，維持原本設計，不受手機修正影響
- 手機導覽修正後：必須確認 JS 語法正確、選單可開合、點選後切換頁面並更新選項文字

### 19.3 部門篩選按鈕

保留的部門（依 SOP 內容）：
```
GDP 權責人 / 管理藥師 / 品保 / 倉管 / 採購 / 業務 / 文管 / 人事
```

---

## 二十、前台禁止顯示的內容

### 20.1 UI 禁止出現的文字類型

| 禁止類型 | 具體範例 | 原因 |
|---------|---------|------|
| Meta 說明文字 | 「資料由公司人員定期更新」 | 非教材內容 |
| 系統欄位 | 「更新者：system」 | 開發者資訊不應暴露 |
| AI 備忘 | 「待補」「此筆來自…」 | 詳見第十一章 |
| SOP 檔案路徑 | Google Drive 路徑 | 文件保護政策 |
| SOP 詳細閱讀重點 | 「第三節 3.1.2 規定…」| 公司內部細節不可外流 |
| 使用者回饋語言 | 「老師說這裡要改」 | 只屬於開發紀錄 |

### 20.2 文件連結政策（老闆 2026-05-27 決定）

**目的：** 防止競業取得平台 URL 後，看到新勝 SOP 架構與執行細節並照抄。

| 項目 | 政策 |
|-----|------|
| `doc-pill` 可點擊文件按鈕 | 移除，改為純文字文件名稱 |
| `openDoc()` modal 內容 | 改為「此文件為公司內部管控文件，請向文管部填單申請調閱」 |
| 部門文件閱讀地圖 | 加申請說明橫幅；只以純文字列示文件名稱與編號 |
| 外部法規連結（PIC/S、食藥署）| 保留，屬公開資訊 |

### 20.3 部門文件閱讀地圖的正確用途

**不是**：可點擊的文件連結清單

**應該是**：
- 學習重點摘要
- 資料準備重點（稽查前要備什麼）
- 若仍列文件，只能純文字，不可加連結

---

## 二十一、多個待辦同時處理的規則

### 21.1 核心原則

**一次只做一個待辦，驗收通過後再做下一個。**

**踩坑根因：** 曾一次同時處理 KB 問題＋互動化＋字體＋面板移除，最後因 KB 未通過驗收，互動化全部被回退，等於白做。

### 21.2 待辦優先順序判斷

```
使用者第一眼看到的功能 → 先修
（KB 查詢是第一眼功能，優先於視覺互動化）

獨立、不影響其他區域 → 先做
（KB 精準搜尋獨立修改，不影響 UI）

牽一髮動全身 → 擺最後
（全站互動化升級影響全站 UI，最後做）
```

### 21.3 在每輪開工時的確認流程

1. 讀 HANDOFF.md 的待辦清單
2. **詢問使用者要從哪一項開始**，不可自行決定
3. 確認後只做那一項
4. 驗收通過才問「下一項做什麼？」

---

## 二十二、「作法」區塊設計規範

### 22.1 作法顯示標準

「作法」區應顯示：
- 員工可**直接理解**的重點短句
- 稽查員問時**可直接回答**的簡要說明
- 不放冗長 SOP 敘述（不把整段程序書貼上去）

### 22.2 各部門作法範例方向

| 部門 | 作法顯示重點範例 |
|-----|--------------|
| 人事/教育訓練 | 人員教育訓練規劃、訓練通知、訓練紀錄保存、內部/外部教育訓練 |
| 倉管 | 庫存管理、每兩個月全面盤點、FEFO 原則、溫濕度記錄 |
| 採購 | 合格廠商評鑑、到貨驗收、FR54-01 填寫 |
| 品保 | 偏差管理、CAPA 開立、內部稽核年度計畫 |

### 22.3 外部教育訓練標準回答

> 「配合政府部門、學術單位及研發機構所舉辦有關 GDP 的人才培訓課程與研討會。」

---

## 二十三、開發版本管理與回退規則

### 23.1 修改前備份原則

每次修改前，應確認：
- 當前 commit hash 已記錄（HANDOFF 中的最後 commit）
- 本機 Git 狀態乾淨（無未 commit 的修改）

### 23.2 回退發生時的處置

| 情境 | 處置 |
|-----|------|
| 使用者要求回退 | 記錄哪個 commit 被回退，不再重做被回退的功能 |
| 被回退的功能 | 必須重新確認使用者意圖後，才能重新做 |
| eyebrow 字體 16px→18px 被回退 | 待使用者明確要求再做，不自行加回 |

### 23.3 一次只 commit 一個目的

- **不把 KB 修正與 UI 修正混在同一個 commit**
- commit message 要精確描述改了什麼（不用「修正多處問題」）
- 每次 push 後等 GitHub Pages 部署（1-3 分鐘）再驗收

---

## 二十四、環境與工具限制備忘

### 24.1 開發環境

| 項目 | 值 |
|-----|---|
| 預覽 server | `npx serve -p 3333`（launch.json 已設定） |
| Shell | PowerShell 5.1（`python`/`py` 不可用） |
| 中文路徑 | 用 UTF-8 儲存，避免亂碼 |
| Obsidian MCP | 中文路徑有時亂碼，改用 Read tool 直接讀寫 |

### 24.2 Firestore 注意事項

| 注意點 | 說明 |
|-------|------|
| 中文亂碼高機率 | 每次寫入後必須讀回逐欄核對 |
| 常見錯字 | 偉→偵、揃→揀（日文漢字）、雔→隔、山害→有害 |
| Unicode 坑 | U+63C3（揃，日文）≠ U+63C0（揀，繁中） |
| rules 部署方式 | 用 MCP tool `firebase_deploy(only:"firestore:rules")`，不需 CLI |

### 24.3 GitHub Pages 注意事項

| 注意點 | 說明 |
|-------|------|
| 部署延遲 | push 後約 1-3 分鐘才生效 |
| 只改 Firestore | 不需 push，永久網址即時反映 |
| 只改 HTML/CSS/JS | 必須 commit + push 才會更新 |
| packed-refs.lock 警告 | 無害，commit 仍成功，忽略即可 |

---

## 二十五、修改前備份規則

### 25.1 動手前必做的兩個確認

```
Step 1：git status
  → 若有未 commit 的修改，先 commit checkpoint（不要在 dirty state 開始改）

Step 2：記下當前 commit hash
  → 寫進 HANDOFF「本輪開始 commit：[hash]」
  → 若修改失敗需回退，確保有明確的回退點
```

### 25.2 Checkpoint Commit 格式

```
git commit -m "checkpoint: 修改前備份 [日期] [簡述當前狀態]"
```

**規則：checkpoint commit 不代表功能完成，只代表「回退點」。** HANDOFF 中需標明是 checkpoint 而非驗收通過的 commit。

### 25.3 大改動前的額外保護

| 改動規模 | 額外步驟 |
|---------|---------|
| 只改 KB 資料（Firestore）| git status 確認乾淨即可 |
| 修改 HTML 邏輯（JS/評分）| checkpoint commit + 記錄 hash |
| 全站重構 / 多章同時改 | 禁止。先拆成單章，逐章 commit |
| 修改 Firestore rules | 先讀 `Firebase設定/firestore.rules` 確認現況 |

### 25.4 回退時的規則

- 回退 **必須**說明哪個 commit 被回退、原因是什麼
- 被回退的功能不可自行加回，需等使用者重新指示
- 回退後立即更新 HANDOFF，說明目前有效的最後 commit

---

## 二十六、單一資料來源規則

### 26.1 問題根源

同一個事實分散在四個地方各寫各的，導致四處不一致：

| 位置 | 實例 |
|-----|-----|
| KB（kbStore fact）| 回答「溫度警戒值 23.5°C」 |
| 部門頁「作法」區 | 還寫著舊版「警戒值 24°C」 |
| FAQ 快速問題 | 按鈕查詢結果與部門頁不同 |
| 稽查重點 | 數字又不同 |

### 26.2 唯一真實來源定義

```
唯一真實來源 = SOP/WI/FR 原文（資料夾內文件）
                    ↓
              fact 資料（kbStore）
                    ↓
    ┌───────────────┬───────────────┐
    ↓               ↓               ↓
 部門頁作法       稽查重點         FAQ/快速問題
 規範到現場      文件地圖         手動搜尋
```

**規則：所有前台顯示的事實，都必須來自同一個 fact 資料，不可在各區塊各自維護。**

### 26.3 快速問題按鈕的一致性要求

- 快速問題按鈕的 `data-q` 值，必須是 KB 中某個 fact 能命中的查詢字串
- **不可**讓按鈕的查詢結果與部門頁顯示的內容不一致
- 每次新增快速問題按鈕，必須先確認 KB 有對應的 fact，且三欄內容正確

### 26.4 同步觸發點

任何一個地方改了事實，以下全部必須同步：

```
□ KB fact（kbStore）
□ Firestore gdpKnowledgeBase
□ HTML kbStore fallback
□ 部門頁「作法」區
□ 規範到現場對照區
□ 稽查重點區
□ 部門文件閱讀地圖
□ FAQ / 快速問題按鈕
```

---

## 二十七、前台文字校對規則

### 27.1 宣告完成前的掃描清單

每次宣告某頁面/功能完成前，必須執行以下掃描：

**第一類：系統/開發殘留**

```
□ 「更新者：system」
□ 「debug」「console.log」殘留在畫面
□ 「TODO」「FIXME」「待補」出現在前台
□ 「undefined」「null」出現在畫面
□ HTML 標籤出現在可見文字中（< > 沒有被 escape）
```

**第二類：AI 備忘殘留**

```
□ 「此筆來自…」
□ 「不可寫成…」
□ 「目前資料索引…」
□ 「（暫缺）」「（待補充）」
□ 「資料來源說明」類型文字
```

**第三類：內容品質**

```
□ 重複出現的句子（同段落內出現兩次以上相同語句）
□ 錯字（中文形近字：偉/偵、揃/揀、雔/隔）
□ 數字錯誤（溫度、頻率、期限）
□ 公司無冷藏倉：搜尋「冷藏」「冷鏈」是否仍出現
□ 表單號碼格式是否一致（FR54-01 而非 FR-54-01）
```

### 27.2 Firestore 資料的額外校對

Firestore 資料建立後**必須讀回驗證**，重點檢查：

| 易錯字元 | 正確 | 常見錯誤 |
|---------|------|---------|
| 偵測器 | 偵（U+5075）| 偉（U+5049）|
| 揀貨 | 揀（U+63C0）| 揃（U+63C3，日文）|
| 隔離 | 隔（U+96D4）| 雔（錯字）|
| 有害物 | 有害 | 山害 |
| 螞蟻 | 螞蟻 | 蟞蟻 |
| 蟑螂 | 蟑螂 | 螠船 |

---

## 二十八、內部文件保護規則

### 28.0 公司文件一律不得有超連結

> **硬性規則：凡屬公司內部文件（SOP、WI、FR、DM），在前台一律以純文字顯示，不得加超連結。**

| 禁止 | 正確 |
|-----|------|
| `<a href="...">DP55-01</a>` 可點擊連結 | `DP55-01` 純文字 |
| `doc-pill` 點擊後開啟文件內容 | 純文字文件名稱 + 申請說明 |
| 文件名稱帶超連結指向 Google Drive | 純文字，員工自行向文管部申請 |
| 文件按鈕觸發 Modal 顯示路徑或內容 | Modal 改為申請通知 |

**例外：** 外部法規連結（PIC/S 官網、食藥署官網）屬公開資訊，可保留超連結。

### 28.1 保護範圍（老闆 2026-05-27 決定）

公司擔心競業取得平台 URL 後，直接複製新勝 SOP 架構與執行細節。

**必須隱藏的資訊：**

| 資訊類型 | 範例 | 處置 |
|---------|------|------|
| Google Drive 路徑 | `G:\我的雲端硬碟\2026codex\…` | 完全不顯示 |
| SOP 詳細閱讀重點 | 「第三節 3.1.2 規定採購人員需…」| 完全不顯示 |
| 內部執行細節 | 具體操作步驟、人員名單 | 只保留抽象層級的事實 |
| 文件的可點擊路徑 | `doc-pill` 點擊後開 Modal 顯示路徑 | 改為申請通知 |

**可以保留的資訊：**

| 資訊類型 | 範例 | 說明 |
|---------|------|------|
| 文件名稱與編號 | DP55-01、FR54-01 | 純文字，員工知道申請哪份即可 |
| 抽象層事實 | 「每兩個月全面盤點一次」| 稽查應答需要 |
| 外部法規連結 | PIC/S、食藥署官網 | 公開資訊，可保留超連結 |

### 28.2 `openDoc()` Modal 標準文字

```
「此文件為公司內部管控文件，
  請向文管部填單申請調閱。」
```

文件名稱仍可見（純文字），員工知道要申請哪份文件，但不顯示路徑與詳細內容。

### 28.3 KB 智慧查詢的保護邊界

- 智慧查詢**繼續顯示**具體事實（頻率、表單號、人員職稱）
- 智慧查詢**不顯示** SOP 的完整程序步驟或路徑
- 資料夾文件作為 KB 建立的**來源依據**，不直接暴露於 UI

---

## 二十九、GitHub Pages 實機驗收規則

### 29.1 驗收層級定義

| 層級 | 描述 | 是否算完成 |
|-----|------|---------|
| 本機預檢 | `npx serve -p 3333` 測試 | **不算完成**，只算預檢 |
| Node.js 腳本驗收 | `node verify_facts_ghpages.mjs` 模擬搜尋 | 半算（搜尋邏輯），但非實機 |
| **GitHub Pages 實機驗收** | 永久網址開啟並實際操作 | **才算完成** |

### 29.2 實機驗收標準步驟

```
1. git commit + push
2. 等待 1-3 分鐘（GitHub Pages 部署時間）
3. 開啟 https://n1116839.github.io/xinshing-gdp-training/
4. 確認頁面 HTTP 200（非 404）
5. 對應本次修改的區域，逐項實際操作（不只是看畫面）
6. 在 HANDOFF 寫「已用 GitHub Pages URL 驗收：○○正常，○○正常」
7. 若發現錯誤 → 修正 → 再次 commit + push → 回到步驟 2
```

### 29.3 驗收必須實際操作的清單

根據本次修改的類型，對應驗收：

| 修改類型 | 驗收操作 |
|---------|---------|
| KB 新增/修改 fact | 輸入對應問題，確認命中正確 fact，三欄內容正確 |
| 快速問題按鈕 | 點擊每個按鈕，確認查詢結果正確 |
| 部門頁修改 | 點入該部門，確認顯示內容正確 |
| Modal 改動 | 觸發 Modal，確認顯示申請說明而非 SOP 內容 |
| 導覽修改 | 點選每個導覽項目，確認頁面切換正確 |
| 搜尋邏輯修改 | 至少輸入 5 個不同問題，確認結果正確 |

### 29.4 沒有驗收記錄 = 未完成

HANDOFF 若無驗收記錄，下一個 AI 必須視此修改為**未驗收狀態**，不得假設已完成。

---

## 三十、RWD 不互相破壞規則

### 30.1 核心原則

修改手機版時，**必須確認桌機版仍正常**；修改桌機版時，**必須確認手機版仍正常**。

### 30.2 需同時驗收的場景

| 修改項目 | 桌機驗收 | 手機驗收 |
|---------|---------|---------|
| 導覽修改 | 左側分組按鈕正常 | 下拉面板開合正常 |
| 版面佈局調整 | 多欄 grid 正常顯示 | 單欄堆疊正常顯示 |
| 字體大小修改 | 閱讀舒適 | 不超出容器 |
| 部門篩選修改 | 桌機篩選按鈕正常 | 手機版按鈕可點擊 |
| Modal 修改 | 正常置中顯示 | 手機上不超出螢幕 |

### 30.3 手機版驗收方式

- **Chrome DevTools**：F12 → Toggle device toolbar → 選 375px（iPhone SE）
- **或**：實機手機開啟 GitHub Pages 永久網址

### 30.4 手機與桌機各自的導覽規則

| 端 | 導覽元件 | 修改原則 |
|---|---------|---------|
| 桌機 | 左側分組按鈕 | 手機修正時不影響此元件 |
| 手機 | 自製大按鈕下拉面板 | 桌機修正時不影響此元件 |

---

## 三十一、互動元件驗收規則

### 31.1 「看起來正確」≠「互動正確」

**禁止只看畫面截圖或靜態 HTML 就宣告互動元件完成。** 每個互動元件都必須實際點擊操作。

### 31.2 各類元件的驗收動作

| 元件類型 | 必做驗收動作 |
|---------|------------|
| Accordion | 點擊展開 → 確認內容出現；再點擊收合 → 確認內容消失 |
| Modal | 點擊觸發按鈕 → 確認 Modal 開啟；點擊關閉或背景 → 確認 Modal 關閉 |
| Tab panel | 點擊每一個 Tab → 確認對應內容切換；確認預設 Tab 正確 |
| 下拉選單（自製）| 點擊開啟 → 確認選項出現；點選一項 → 確認頁面切換且選項文字更新 |
| 快速問題按鈕 | 點擊每個按鈕 → 確認查詢發出、結果正確、無「尚未查詢」殘留 |
| 搜尋輸入框 | 輸入問題 → 確認 loading → 確認結果或「找不到」訊息（不是空白）|
| Checkbox 核對清單 | 點擊勾選 → 確認視覺勾選狀態；取消勾選 → 確認還原 |
| Scroll fade-in | 向下捲動 → 確認元素在進入畫面時有動畫 |

### 31.3 驗收失敗的常見原因

| 症狀 | 常見根因 |
|-----|---------|
| 點擊無反應 | JS 事件未綁定 / 選取器錯誤 |
| Modal 開啟但關不掉 | 關閉按鈕事件未綁定 |
| Tab 切換顯示錯誤內容 | data 屬性或 id 對應錯誤 |
| 下拉選單開啟後點選沒反應 | 選項 button 事件未設定正確目標 |
| 搜尋後仍顯示「尚未查詢」| 初始訊息未在查詢前清除（`box.innerHTML = ""`）|
| Accordion 不收合 | toggle class 邏輯錯誤 |

---

## 三十二、漸進式修改規則

### 32.1 核心原則

> **先做一章完整樣板，驗收通過後，再以同一模板擴展到其他章。**
> 禁止一次對所有章節同時開工。

### 32.2 適用場景

| 場景 | 正確做法 |
|-----|---------|
| KB fact 全章建立 | 先建第二章（人事/教育訓練）樣板，通過後複製到其他章 |
| 稽查重點互動化 | 先做第一章的稽查重點，通過後複製格式 |
| 部門頁作法更新 | 先更新一個部門，確認格式正確後再展開 |
| 測試標準建立 | 先對一章跑完整 10 題 × 10 問法，建立標準再推廣 |

### 32.3 樣板建立的完整流程

```
1. 選定目標章節（建議第二章：人事/教育訓練）
2. 讀 SOP/WI/FR 原文，拆出所有可獨立問答的 fact
3. 每個 fact 設計 10 種自然問法
4. 建測試表（10 個問題 × 10 種問法）
5. 本機預檢（npx serve -p 3333）
6. git commit + push
7. 等 GitHub Pages 部署
8. 實機驗收（第二十九章規則）
9. 手機/桌機都驗收（第三十章規則）
10. 互動元件逐一點擊（第三十一章規則）
11. 通過 → 在 HANDOFF 標記「第二章樣板完成 ✅」
12. 將同一流程複製到第三章、第四章…
```

### 32.4 樣板複製時的注意事項

- 複製 fact 格式，**不複製 fact 內容**（每章 SOP 不同，內容必須重新讀原文）
- 複製測試表格式，**不複製測試題**（每章測試題不同）
- 複製驗收流程，**每章都要跑完整驗收**（不因為樣板通過就跳過）

---

## 三十三、年度行事曆設計規則

### 33.1 行事曆的定位

**年度行事曆是「收件人視角」，不是查詢工具。**

員工打開行事曆，看的是：「這個頻率我要做什麼、交給誰、填什麼表單」——不是「資料庫裡有哪些文件」。

### 33.2 行事曆資訊架構

```
年度行事曆（獨立導覽大項）
  ├── 依頻率分組：發生時啟動 / 每日 / 每月 / 每兩個月 / 每季 / 每半年 / 每年 / 每三年
  ├── 依部門篩選
  └── 關鍵設備行事曆（獨立區塊）
```

**行事曆必須與部門分類獨立**，不可混在部門頁內。

### 33.3 每筆收件項目的欄位

每筆資料應包含：
```
時間點 / 部門 / 資料名稱 / 啟動說明 / 表單/紀錄 / 負責人 / 來源 SOP
```

「已完成/未完成」checkbox 放在每筆項目旁，不做獨立「本月勾選」頁。

### 33.4 禁止重複的三個區域

| 容易重複的區域 | 正確處置 |
|-------------|---------|
| `schedule` 區塊（基本時程）vs `deptScheduleSection`（互動版）| 只保留互動版，移除基本版 |
| 「依頻率分組」明細 vs 每筆卡片的來源 | 「依頻率分組」改為摘要（數量+部門），不重複列明細 |
| 年度行事曆的「本頁相關 SOP」 vs 每筆卡片的來源欄 | 移除年度行事曆的 SOP 清單區塊，來源留在卡片 |

### 33.5 頻率術語統一規則

| 正確寫法 | 禁止寫法 |
|---------|---------|
| 每季（每三個月）| 每三個月（與每季重複） |
| 每兩個月 | 每兩月（不一致） |
| 每年 12 月 | 每年年底（不精確）|

**同義頻率只列一次，不可重複出現。**

### 33.6 行事曆完整性：必須逐份 SOP 讀取

**索引不能取代逐份 SOP 閱讀。**

**踩坑根因（2026-05-25）：** FR10-01 有時程 NEW 是方便查找的索引，但不能取代逐份 SOP 閱讀。DP14-01 管理階層檢討（每年 12 月）等具體時間點只寫在 SOP 正文，索引未必完整。DP56-01 廢棄物管理（11 月通知廠商、12 月底銷毀）也未出現在索引，靠索引一定漏。

**正確做法：**
1. 每次建立/核對行事曆，必須逐份讀取第一章到第八章所有 SOP 文件
2. 不可只掃 FR10-01 索引
3. 每章核對完必須記錄：「第X章 N 份文件，發現 M 個時程節點」
4. 若目錄與 SOP 原文有出入，以 SOP 原文為準，差異列給使用者確認

### 33.7 設備行事曆獨立規則

關鍵設備（分離式冷氣、UPS、溫度計、溫濕度記錄器、柴油發電機）獨立為「關鍵設備行事曆」section，不與資料收件行事曆混放。

格式：左側頻率 / 右側設備名稱、作業內容、負責人、表單、來源 SOP。

---

## 三十四、Firebase Q&A 功能規則

### 34.1 連線狀態顯示邏輯

**正確順序：**
```
頁面載入 → 顯示「連線中…」
  ↓
Firestore 連線成功 → 顯示「已連接 Firebase」
  ↓
顯示現有問題列表
```

**禁止：** 頁面載入後直接顯示「本機暫存 N 筆」或跳過連線提示。使用者會誤以為沒有連接 Firebase。

### 34.2 Q&A 區塊設計規則

| 規則 | 說明 |
|-----|------|
| 問題永久保留 | 不定期清除已回答的問題 |
| 移除提問人欄位 | 不收集個人資訊 |
| 回覆開放所有人 | 不限管理者才能回覆 |
| 雙欄佈局 | 左側 sticky 提問框，右側問題列表，加問題計數 badge |

### 34.3 Firestore Q&A 集合

使用集合：`gdpQ&A`（依 Firestore rules 授權）

問題狀態存在 Firestore，不可只存 localStorage（localStorage 不跨裝置）。

---

## 三十五、SOP 覆蓋完整性規則

### 35.1 核心原則

**資料夾有幾份文件，KB 就必須有幾筆。** 這個規則適用於所有章節，不只是現有的章節。

### 35.2 各章文件數（截至 2026-05-28）

| 章節 | 文件數 | 章節主題 |
|-----|------|---------|
| 第一章 | 11 | 品質管理系統 |
| 第二章 | 8 | 人員組織 |
| 第三章 | 14 | 場所設備 |
| 第四章 | 2 | 文件管制 |
| 第五章 | 7 | 作業（進出貨、儲存、揀貨） |
| 第六章 | 4 | 申訴、退回、偽禁藥、回收 |
| 第七章 | 1 | 委外 |
| 第八章 | 1 | 內部稽核 |
| **合計** | **48** | |

### 35.3 宣告某章 KB「完成」的條件

```
□ 已逐份讀取該章所有 SOP/WI/FR 原文
□ 每份文件至少有一筆對應的 fact 或 clarify 條目
□ 每筆 fact 的三欄內容已對應原文核對
□ 每章 10 個問題 × 10 種問法測試全部通過
□ GitHub Pages 實機驗收完成
```

### 35.4 索引核對規則

- **FR10-01 有時程 NEW** 等索引只作為「快速查找入口」，不作為完整性依據
- 任何作業型 SOP（不只管理型）都要逐份讀正文，不可只看索引標題
- 每次完成一章後，記錄「第X章：N 份文件核對完畢，fact N 筆，clarify N 筆」

---

## 附錄 A：開工必讀清單

每次對話開始，按順序讀：
1. `HANDOFF.md`
2. `G:\我的雲端硬碟\我的第二大腦\踩坑紀錄.md`
3. `G:\我的雲端硬碟\我的第二大腦\專案\2026codex AI測試.md`

讀完後詢問使用者從哪個待辦開始，**不可自行決定**。

---

## 附錄 B：KB 絕對禁止清單

| 禁止 | 原因 |
|-----|------|
| KB 欄位放 AI 指令語（「不可寫成…」「目前資料索引…」）| 學員看到的是教材，不是備忘 |
| KB 欄位放否定推論（「不是溫度監控系統」）| 文件沒有這樣記載，不可補充 |
| KB 欄位放佔位文字（「請依文件編號回到正式文件確認」）| 員工需要實質答案，不是索引 |
| HTML 本身作為 KB 來源 | 搜尋結果會顯示 HTML 標籤與 JS 程式碼 |
| 出現「冷藏倉」「冷鏈」| 公司無冷藏倉，這是錯誤事實 |
| 同時處理多個待辦（KB＋互動化＋字體＋面板）| 任一未通過驗收，全部被回退 |

---

## 三十六、部門頁面內容精準性規則

### 36.1 部門頁的設計場景（新員工視角）

**情境：** 倉庫組長要培訓新人，說「你去看教育平台的倉庫章節」。

新人打開倉庫頁後，應該能依序得到這四個答案：

| 順序 | 他應該得到的答案 | 對應目標 |
|-----|----------------|---------|
| 1 | GDP 是什麼、跟倉庫有什麼關係 | 目標 1：GDP 是什麼 |
| 2 | 稽查員來倉庫最常抓哪些缺失 | 目標 2：常見缺失 |
| 3 | 稽查員問「溫度幾度？盤點多久？」時怎麼回答 | 目標 3：稽查問答 |
| 4 | 每兩個月做盤點、到貨當天填 FR54-01、溫度每5分鐘記一次…哪個時間點做哪件事 | 目標 4：什麼時候要做文件 |

**以及：** 倉庫必讀 / 參考 / 共用的文件清單（現有功能，但必須精準）

---

### 36.2 文件分類三層：必讀 / 參考 / 共用

每個部門頁的文件清單，依重要性分三層：

| 分類 | 定義 | 範例（倉庫）|
|-----|------|-----------|
| **必讀** | 該部門員工每天工作直接用到的 SOP/WI/FR，稽查員一定會問 | DP33-01 溫濕度管理、DP55-01 庫存管理、FR54-01 到貨驗收紀錄表 |
| **參考** | 不直接操作，但需要了解流程的 SOP | DP12-01 品質政策、DP82-01 內部稽核 |
| **共用** | 跨部門使用、需要與其他部門協作的文件 | FR54-01（採購也用）、DP25-01（人事通知） |

---

### 36.3 文件分配的精準性原則（核心規則）

> **硬性規則：部門 A 的文件清單，只能放部門 A 相關的文件。**

**踩坑場景：** 倉庫頁的「本頁相關 SOP」放進了 DP62-01（申訴退回，屬品保/業務）或 DP72-01（委外，屬品保）。

| 禁止出現的情況 | 正確做法 |
|-------------|---------|
| 倉庫頁出現業務的 SOP（DP62-01）| 移到品保或業務頁 |
| 採購頁出現倉庫的 WI | 移到倉庫頁 |
| 人事頁出現稽核相關 SOP（不在人事職責內）| 移到品保頁 |
| 所有部門都放相同的共用清單 | 共用文件只在「真的會用到的部門」才顯示 |

**校對方法：** 建立或修改某部門的文件清單時，逐份確認「**這份文件的 SOP 裡，有哪個章節明確寫到這個部門的職責？**」，沒有就不放。

---

### 36.4 職位層級的內容差異

> **重要：** 以下分層是**展示規則**，不是系統功能（帳號權限功能在後期才做）。  
> 目前設計階段，應先按最高層級設計內容，帳號功能上線後再套用篩選邏輯。

**倉庫範例：**

| 職位層級 | 看到的內容 | 原因 |
|---------|-----------|------|
| **倉管一般員工** | 溫濕度管理、庫存盤點、到貨驗收、揀貨出貨 | 日常操作核心 |
| **倉庫組長** | 一般員工全部 + 文件修正申請流程 + 跨部門協作說明 | 需要管理職責 |
| **（跨部門協作範例）** | 採購叫貨後，倉庫需確認到貨驗收紀錄（FR54-01）；預計到貨日由採購通知倉庫 | 協作觸發點 |

**跨部門協作的標準呈現方式：**

```
【倉庫 ↔ 採購 協作】
觸發點：採購在正航建立採購單並確認到貨日
倉庫做什麼：依預計到貨日安排驗收人力，到貨當天填 FR54-01
必要文件：FR54-01 到貨驗收紀錄表
備註：驗收完成後，倉庫通知採購確認入庫
```

---

### 36.5 「什麼時候要做文件」在部門頁的呈現方式（目標 4）

每個部門頁應有一個「**我的文件時程**」區塊，列出該部門需要在什麼條件下填寫哪些表單。

**格式：**

| 觸發條件 | 要做什麼 | 填哪份表單 | 來源 SOP |
|---------|---------|-----------|---------|
| 有貨到 | 填到貨驗收紀錄 | FR54-01 | DP54-01 |
| 每兩個月 | 全面庫存盤點 | FR55-03 | DP55-01 |
| 溫度超過 23.5°C | 立即記錄並通報 | FR33-01 | DP33-01 |
| 發現不符合品 | 隔離並標示，填報告 | FR55-04 | DP55-01 |
| 有訪客 / 外部人員進入 | 登記門禁 | FR25-01 | WI25-01 |

**這個區塊取代「什麼時候做？」的純文字說明，改為可掃視的表格或互動清單。**

---

### 36.6 文件清單的建立順序（校對流程）

```
Step 1：列出此部門對應的所有 SOP/WI/FR（從資料夾逐份確認）
         ↓
Step 2：依「必讀 / 參考 / 共用」分類
         ↓
Step 3：確認每份文件確實有該部門的職責描述（不是因為「感覺相關」就放進去）
         ↓
Step 4：跨部門協作文件 → 在兩個部門頁都標示，並寫清楚各自的角色
         ↓
Step 5：與 KB 的 fact sop_ref 欄位比對 — 部門頁列的文件若有對應 fact，兩邊一致
         ↓
Step 6：完成後，在另一個部門頁確認「這份文件沒有出現在不該出現的地方」
```

---

### 36.7 驗收標準

宣告某部門頁文件清單完成前，必須驗收：

```
□ 必讀文件：每份都能在 SOP 原文找到「此部門」的職責說明
□ 沒有其他部門的 SOP 誤入
□ 共用文件：明確標示「此文件在哪幾個部門都會用到」
□ 「文件時程」表格有對應的 SOP 來源（非推論）
□ 跨部門協作說明：觸發點、各部門角色、表單都寫清楚
□ 職位層級內容（如有）：組長看到的比一般員工多的部分已確認來源
```

---

## 三十七、三階段開發藍圖

### 37.1 優先順序（此順序不可調換）

| 階段 | 目標 | 前提條件 | 狀態 |
|-----|------|---------|------|
| **Phase 1** | KB 全量完成 + 收件時程（角色開放）| 無（第一優先）| 進行中 |
| **Phase 2** | 所有文字區塊視覺化 / 互動化 | Phase 1 完成 | 未開始 |
| **Phase 3** | 帳號申請 + 管理員審核 + 角色晉升 | Phase 1 完成 | 設計完成，待實作 |

> **規則：Phase 1 完成前，不開始 Phase 2 或 Phase 3 的實作工作。**
> Phase 2 和 Phase 3 可以「設計」，但不可以「寫 code」。

### 37.2 Phase 1 完成條件

```
□ 第一至八章所有 KB fact 條目已建立
□ 每章至少 10 個問題 × 10 種問法，測試全部通過
□ GitHub Pages 實機驗收通過
□ 收件時程（年度行事曆）資料完整（依角色控制可見性）
□ HANDOFF 標記「Phase 1 完成 ✅」
```

### 37.3 Phase 2 完成條件

```
□ 每個部門頁都有至少一個真正的互動元件
□ 稽查重點區塊：學習型格式（問題/應答/證據）
□ 文件時程：可篩選的互動表格
□ 所有桌機 + 手機驗收通過
```

### 37.4 Phase 3 完成條件

```
□ 員工可自行申請帳號（填表 + 等待審核）
□ 管理員後台可審核、停用、改角色
□ 一次性啟用碼機制可用
□ 組長可代申請員工晉升
□ 角色控制：一般/主管/管理員三層內容正確隔離
□ 收件時程對「主管以上」才顯示
```

---

## 三十八、帳號申請與審核平台設計（Phase 3）

### 38.1 角色三層設計（已確認）

| 層級 | 識別名稱 | 可見內容 | 後台功能 |
|-----|---------|---------|---------|
| **Level 1** | 一般員工 | 自己部門的 GDP 內容（四大目標） | 無 |
| **Level 2** | 組長 / 主管 | 跨部門協作說明 + 收件時程 | 可代提交「成員晉升申請」 |
| **Level 3** | 管理員 | 全站所有部門內容 | 審核帳號、停用、改角色、產生啟用碼 |

> **附加角色：超級管理員（Super Admin）**
> 僅限 1-2 個指定 email，hardcode 在設定中。負責新增第一個管理員帳號、系統設定。不透過申請流程，直接由系統設定。

### 38.2 員工申請帳號流程

```
Step 1：員工開啟平台 → 點「申請帳號」
          ↓
Step 2：填入申請資料
          ├── 員工編號（文字輸入）
          ├── 姓名（文字輸入）
          ├── 部門（下拉選單，由管理員維護清單）
          └── 職稱（下拉選單，由管理員維護清單）
          ↓
Step 3：系統建立「待審核」申請
         → 通知管理員（email + 後台角標）
          ↓
Step 4：管理員登入後台
         → 查看申請清單 → 選擇「批准」並指定層級（Level 1/2）
          ↓
Step 5：系統產生一次性啟用碼（48 小時內有效）
         → 管理員透過 LINE 或當面告知員工
          ↓
Step 6：員工回到平台 → 輸入啟用碼 → 設定密碼 → 帳號啟用
          ↓
Step 7：員工正常登入，依角色顯示對應內容
```

### 38.3 啟用碼安全設計

| 規則 | 說明 |
|-----|------|
| 一次性 | 啟用碼使用一次即失效，無法重用 |
| 有效期 | 48 小時後自動過期 |
| 綁定申請 | 啟用碼只對應這一份申請，無法拿來啟用其他帳號 |
| 離職防護 | 即使離職員工仿冒資料通過申請，沒有啟用碼就進不來 |
| 停用即時生效 | 管理員點「停用」→ 帳號立即無法登入 |

### 38.4 晉升流程（組長代申請）

```
Step 1：組長登入後台 → 進入「成員管理」
          ↓
Step 2：點選要晉升的員工 → 填寫「晉升原因」→ 選目標角色
          ↓
Step 3：系統通知管理員（email + 後台通知）
          ↓
Step 4：管理員審核 → 批准 → 員工角色自動更新
          （不需要重新輸入啟用碼）
          ↓
Step 5：員工下次登入即看到新層級的內容
```

### 38.5 帳號停用流程（員工離職）

```
管理員 → 後台「帳號管理」→ 找到離職員工帳號 → 點「停用」
→ 帳號立即無法登入
→ 資料保留（查稽核紀錄用）
→ 不刪除（避免日後稽查需要查歷史記錄）
```

### 38.6 Firebase 技術對應

| 功能 | Firebase 元件 | Firestore Collection |
|-----|------------|-------------------|
| 帳號登入 | Firebase Authentication | — |
| 使用者資料 | Firestore | `gdpUsers` |
| 申請待審核 | Firestore | `gdpUserApplications` |
| 一次性啟用碼 | Firestore | `gdpActivationCodes` |
| 晉升申請 | Firestore | `gdpPromotionRequests` |
| 職稱/部門清單 | Firestore | `gdpDeptConfig` |
| 管理員通知 | Firebase Extensions / Cloud Functions | — |

> **注意：** Firebase Extensions（Send email）可用於通知管理員。若要 LINE 通知，需額外整合 LINE Notify API，屬額外開發工作。Phase 3 初版先用 email 通知，LINE 通知列為後期選項。

### 38.7 收件時程（年度行事曆）的角色控制

| 角色 | 收件時程可見性 |
|-----|-------------|
| 一般員工 | **不顯示**（只看自己部門的四大目標內容）|
| 組長/主管 | **顯示**（看到自己部門相關的收件項目）|
| 管理員 | **顯示全部**（所有部門所有收件時程）|

**收件時程的定義：** 員工需要「提交、填寫、完成、回報」某份文件的時間點。
- 例：倉管人員每兩個月需提交盤點表（FR55-03）→ 只有倉管組長以上可見收件時程提醒
- 例：品保每年 12 月需提交內部稽核報告 → 只有品保組長以上可見

### 38.8 職稱/部門清單的維護規則

> 使用者確認（2026-05-28）：職稱下拉選單由管理員維護，因為職位隨時會因人力需求增減。

**規則：**
- 部門清單和職稱清單存放在 Firestore `gdpDeptConfig`，不 hardcode 在 HTML
- 管理員後台有「清單管理」功能可新增/移除部門與職稱
- 員工申請表的下拉選單從 `gdpDeptConfig` 即時讀取
- **停用一個職稱 ≠ 刪除已有該職稱員工的帳號**（只影響新申請的下拉選項）

### 38.9 Phase 3 實作前的必要準備

在開始寫 Phase 3 的程式之前，必須先確認：

```
□ 管理員 email 清單（誰有管理員權限？具體幾個 email？）
□ 超級管理員 email（hardcode，1-2 個）
□ 初始部門清單（從 AGENTS.md 的部門列表開始）
□ 初始職稱清單（參考 SOP 中的人員職稱）
□ email 通知服務（Firebase Extensions 或其他）是否已啟用
□ Firestore rules 更新（新增 gdpUsers、gdpUserApplications 等集合的規則）
```

---

## 三十九、知識安全邊界設計（防止平台被複製成 GDP 範本）

### 39.1 核心安全原則

> **這份教育平台的知識，不可以讓外部人員一拿到就能複製出一份一模一樣的 GDP 文件系統。**

醫藥 GDP 文件系統是公司的核心合規資產，許多大公司視為機密。平台設計必須在「員工能學習」和「外部無法複製」之間找到明確邊界。

---

### 39.2 知識三層邊界

| 層級 | 名稱 | 定義 | 能放進平台？ |
|-----|------|------|-----------|
| **Layer 1** | 公開法規知識 | PIC/S GDP 原則、台灣食藥署規定 → 任何人查法規都查得到 | ✅ 可放，且無登入限制 |
| **Layer 2** | 合規事實陳述 | 員工回答稽查員的短答（頻率、溫度、表單號）→ 是「有做」的證明，不是「怎麼做」的說明書 | ✅ 登入後可見 |
| **Layer 3** | 作業細節程序 | SOP 的完整流程、表單欄位設計、決策樹、例外處理步驟 | ❌ 不進平台，留在 SOP 文件 |

---

### 39.3 Layer 2 vs Layer 3 判斷標準

**判斷問題：「這段內容，是幫員工回答稽查員，還是在教別人怎麼做系統？」**

| 內容類型 | 範例 | 層級 | 平台？ |
|---------|------|------|------|
| 頻率陳述 | 「每兩個月全面盤點一次」 | Layer 2 | ✅ |
| 表單號碼 | 「填 FR55-03」 | Layer 2 | ✅ |
| 溫度數值 | 「15-25°C，警戒值 23.5°C」 | Layer 2 | ✅ |
| 人員職稱 | 「由倉管人員執行」 | Layer 2 | ✅ |
| 表單欄位說明 | 「FR55-03 第一欄填品名，第二欄填批號…」 | Layer 3 | ❌ |
| 完整決策流程 | 「若差異 >3% 則啟動複查，若複查仍有差異則…」 | Layer 3 | ❌ |
| 例外處理步驟 | 「溫度超標時，先通報品保，品保確認後發不符合報告…」 | Layer 3 | ❌ |
| SOP 的整個程序章節 | 「3.1 收貨程序：3.1.1 採購人員須在24小時內…」 | Layer 3 | ❌ |

---

### 39.4 文件編號的安全處理

**文件編號（DP55-01、FR55-03）可以出現在平台上**，但只作為「這份文件你要申請來讀」的指引，不展示文件內容。

| 處理方式 | 說明 |
|---------|------|
| 出現文件編號 | ✅ 員工知道要申請哪份文件 |
| 展示文件清單 | ✅ 知道有哪些文件，不知道內容 |
| 展示文件內容 | ❌ 任何 SOP/WI/FR 的正文都不進平台 |
| 可點擊連結開啟文件 | ❌ 已在 Chapter 28 禁止 |

---

### 39.5 整體系統架構的保護

**禁止在平台上展示可推導出「新勝完整 GDP 文件架構」的資訊：**

| 禁止 | 原因 |
|-----|------|
| 所有 SOP/WI/FR 的完整編號清單（可瀏覽全貌）| 讓人知道公司有哪些文件、數量、結構 |
| 各文件之間的關聯圖（上下層關係）| 等同公開文件架構 |
| SOP 各章節標題清單 | 可推導出公司的管理系統設計邏輯 |

**允許的呈現方式：**
- 依部門呈現「此部門相關的文件名稱」（員工需要申請哪幾份）
- 只顯示與當前頁面主題相關的 1-3 份文件，不顯示全站文件索引
- 文件清單在登入後才顯示（Phase 3 上線後）

---

### 39.6 登入前與登入後的內容差異

| 內容類型 | 未登入 | 登入（一般員工）| 登入（主管以上）|
|---------|--------|--------------|-------------|
| GDP 法規介紹（Layer 1）| ✅ 可見 | ✅ 可見 | ✅ 可見 |
| 常見缺失說明（一般性）| ✅ 可見 | ✅ 可見 | ✅ 可見 |
| 新勝做法（Layer 2，稽查應答）| ❌ 不顯示 | ✅ 可見（自己部門）| ✅ 可見（全部）|
| 文件清單（表單編號）| ❌ 不顯示 | ✅ 可見（自己部門）| ✅ 可見（全部）|
| 收件時程 | ❌ 不顯示 | ❌ 不顯示 | ✅ 可見 |

> **Phase 3 前的暫行方案：** 沒有登入系統前，所有人可看到所有內容。Phase 3 上線後再套用上表的分層。

---

### 39.7 KB 重建時的安全審查

每次建立或更新 KB 條目時，在完成前必須做安全審查：

```
□ xinshing 欄是否只有「員工回答稽查員的短句」？（Layer 2）
□ 有沒有出現 SOP 的條文段落或完整程序步驟？（Layer 3 → 刪掉）
□ 有沒有展示表單的欄位設計或填寫邏輯？（Layer 3 → 刪掉）
□ 有沒有洩露決策樹或例外處理的完整流程？（Layer 3 → 刪掉）
□ 如果外部人員看到這筆資料，能複製出新勝的作業程序嗎？能 → 縮減
```

---

## 四十、資料庫維護機制（文件追蹤 + 新增/刪除管理）

### 40.1 核心規則：一份文件，至少一筆覆蓋

> **資料夾有幾份文件，KB 就要有對應的覆蓋。**
> 一份文件可以對應一筆 clarify（流程概述）+ 多筆 fact（各個具體事實）。
> 但每份文件至少必須有一筆條目，不可有文件但完全無 KB 覆蓋。

---

### 40.2 各章文件數與目前完成狀態

| 章節 | 文件數 | 章節主題 | 目前狀態 |
|-----|-------|---------|---------|
| 第一章 | 11 | 品質管理系統 | ✅ fact 架構完成（驗收通過）|
| 第二章 | 8 | 人員組織 | ✅ fact 架構完成（驗收通過）|
| 第三章 | 14 | 場所設備 | ✅ fact 架構完成（驗收通過）|
| 第四章 | 2 | 文件管制 | ⚠️ clarify 條目存在，fact 待補 |
| 第五章 | 7 | 作業（進出貨、儲存、揀貨）| ✅ fact 架構完成（驗收通過）|
| 第六章 | 4 | 申訴、退回、偽禁藥、回收 | ⚠️ clarify 條目存在，fact 待補 |
| 第七章 | 1 | 委外 | ⚠️ clarify 條目存在，fact 待補 |
| 第八章 | 1 | 內部稽核 | ⚠️ 已有部分 fact，需核對 |
| **合計** | **48** | | |

> **注意：** 「文件數 = 48」是指 SOP/WI/FR 文件份數。KB 條目數（71筆）≠ 文件數。
> 一份文件通常產生 1 個 clarify + 2-5 個 fact，合計每章 3-10 筆。

---

### 40.3 新增文件時的 KB 維護流程

```
觸發：發現資料夾新增了一份 SOP/WI/FR
      ↓
Step 1：確認文件編號與章節（放入哪一章）
      ↓
Step 2：讀取文件全文，拆解可獨立問答的 fact
      ↓
Step 3：建立對應的 clarify 條目（若該文件尚無）
      ↓
Step 4：建立各個 fact 條目，每個 fact 補 10 種問法
      ↓
Step 5：在 HANDOFF 更新「第X章：新增文件 [編號]，增加 N 筆 fact」
      ↓
Step 6：跑驗收腳本，確認新 fact 可正確命中
      ↓
Step 7：GitHub Pages 實機驗收通過後 commit + push
```

---

### 40.4 文件修訂時的 KB 維護流程

```
觸發：SOP/WI/FR 發行新版本（內容有變動）
      ↓
Step 1：比對新舊版本差異（只看有變動的段落）
      ↓
Step 2：找出對應的 KB 條目（透過 sop_ref 欄位追蹤）
      ↓
Step 3：更新有變動的 KB 條目內容（不影響無變動的條目）
      ↓
Step 4：同步前台顯示區塊（依第七章同步規則）
      ↓
Step 5：在 HANDOFF 記錄「[文件編號] 版本更新，KB 對應條目 [fact ID] 已更新」
      ↓
Step 6：驗收 + push
```

---

### 40.5 刪除文件時的 KB 維護流程

```
觸發：某份 SOP/WI/FR 被廢止或合併
      ↓
Step 1：找出所有 sop_ref 含該文件編號的 KB 條目
      ↓
Step 2：確認這些事實是否有其他文件支撐
      ├── 有其他文件支撐 → 更新 sop_ref，不刪除條目
      └── 無其他支撐 → 刪除該 KB 條目
      ↓
Step 3：刪除前先 git commit checkpoint（確保可回退）
      ↓
Step 4：在 HANDOFF 記錄「[文件編號] 廢止，相關 KB 條目 [fact ID] 已刪除/更新」
```

---

### 40.6 流程圖文件的 Phase 2 標記

**部分 SOP/WI 文件內含流程圖**，這些流程圖是 Phase 2 視覺互動的素材來源。

**標記規則：** 在對應的 KB clarify 條目加入標記（不影響搜尋邏輯）：

```javascript
docs.push({
  id: "clarify-receiving-shipping",
  sourceType: "clarify",
  hasFlowchart: true,           // ← 文件有流程圖，Phase 2 可做視覺化
  flowchartDesc: "進貨驗收流程圖（4步驟）",  // ← 描述流程圖內容
  // ... 其他欄位
});
```

**Phase 2 工作時，優先從 `hasFlowchart: true` 的條目製作互動流程圖元件。**

---

### 40.7 Phase 1 系統性重建計畫

> **原則：不是刪掉全部重建，而是「逐章審核+補齊」。**

**已完成章節（一、二、三、五章）：**
- 維持現有 fact 架構
- 依本規範（Ch.39 知識安全邊界）做一次安全審查
- 確認每份文件有對應的 KB 覆蓋

**待補章節（四、六、七、八章）：**
- 依第三十二章漸進式修改規則，逐章補建
- 每章完成後跑驗收腳本，通過後再進下一章

**重建順序建議（使用者確認後才開始）：**
```
第四章（2份文件，最少，適合快速完成）
  → 第八章（1份文件，最快）
  → 第七章（1份文件）
  → 第六章（4份文件，最後）
```

**每章重建包含：**
1. 逐份讀取文件原文
2. 拆解 fact，每個補 10 種問法
3. 安全審查（Layer 2 vs Layer 3 邊界）
4. 驗收腳本全章通過
5. GitHub Pages 實機驗收
6. HANDOFF 標記完成

---

## 附錄 C：文件結構說明

| 路徑 | 用途 |
|-----|------|
| `HTML資料庫/新勝GDP資料庫.html` | 主要工作檔 |
| `Firebase設定/firestore.rules` | Firestore 規則 |
| `Firebase設定/sync-gdp-knowledge.ps1` | Firestore 同步腳本 |
| `HANDOFF.md` | 開工接續紀錄 |
| `AGENTS.md` | 跨 AI agent 共用規則 |
| `CLAUDE.md` | Claude Code 開工指示 |
| `GDP_智慧查詢規範.md` | 本文件 |

---

*本文件 v2.4 修正 6.6（AI事實核對表：溫度警戒值常見錯誤→未設警戒值、模擬回收常見錯誤→未執行），新增 6.7「教材常見缺失的來源規則」（必須來自 PIC/S/食藥署公開稽查資料，禁止 AI 推測）。後續如有新踩坑點，應同步更新此規範，並更新版本號。*

*規範更新責任：每次收工若發現新踩坑，先寫入踩坑紀錄.md，再決定是否需補入本規範；若屬新規則（非已有章節覆蓋）則必須補入。*
