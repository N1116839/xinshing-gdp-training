# 新勝醫藥 GDP 智慧查詢 — 正式專案規範文件

> 版本：3.3　　建立：2026-05-28　　依據：15 次開工踩坑點整合＋部門頁精準性＋三階段藍圖＋帳號審核＋知識安全邊界＋資料庫維護機制＋常見缺失來源規則＋開工自動檢查規則＋部門權限邊界＋互動視覺化規則＋角色拒答規則＋互動等級驗收＋快速查詢全量違規掃描＋交接狀態判讀優先序＋外部明文與公司 SOP 自動核對規則＋ERP 式角色權限矩陣設計（§48）＋AI 管理助理維護模式（§49）
> 適用範圍：`HTML資料庫/新勝GDP資料庫.html` **全頁面**（智慧查詢、年度行事曆、各部門頁、互動元件、Firebase 功能）
> **本文件為規範，不修改任何 HTML / Firebase / 程式碼**

---

## 零、教材整體目的（開工前必讀）

### 0.1 這份教材是什麼

本 HTML 資料庫是**新勝醫藥 GDP 內部教育訓練平台**，不是對外簡報、不是靜態文件、不是資料查詢系統。

**目標讀者：** 新進員工、各部門需應對 GDP 稽查的人員

### 0.2 教材四大核心目標（每頁設計都必須服務這四點）

| # | 核心目標 | 具體說明 |
|---|---------|---------|
| 1 | **GDP 是什麼** | 讓員工理解 GDP 的法規背景、為何重要、與自己工作的關係 |
| 2 | **常見缺失為何** | 讓員工知道稽查員最常抓到什麼、哪些文件最容易出問題 |
| 3 | **稽查員詢問時如何回答** | 讓員工能用自己的話、正確且自信地回答稽查員的問題 |
| 4 | **什麼時候要做文件** | 讓員工知道哪些時間點要填哪些表單，不等到稽查才手忙腳亂 |

**第三點與第四點最直接影響員工日常**：

- 第三點（稽查應答）→ 有稽查員來的時候知道怎麼說
- 第四點（文件時間點）→ 平常工作知道什麼時候要填什麼表

**正確回答範例（第三點）：**
> 稽查員問：「你們公司倉庫溫度是多少度啊？」
>
> 員工應回答：「15 度到 25 度，因為我們是常溫藥品。警戒值設在 23.5 度，超過的話系統會做提醒。」

這種回答的特點：
- 直接說出數字（15-25°C）
- 說明為何這樣設定（常溫藥品）
- 說出應對機制（警戒值、系統提醒）
- **不念 SOP 條文**、不說「依 DP33-01 規定」

**文件時間點範例（第四點）：**
> 倉管人員知道：「每兩個月要做庫存盤點，填 FR55-03；到貨時當天要填 FR54-01。」
> 不需要等組長提醒，平常就知道觸發點。

### 0.3 教材六個輔助問題（讓上述四大目標更完整）

| 問題 | 說明 | 對應目標 |
|-----|------|---------|
| GDP 是什麼 | 法規背景、為何重要 | 目標 1 |
| 為什麼要做 | 法規要求、稽查風險 | 目標 1、2 |
| 稽查看什麼 | 常見缺失、稽查員問法 | 目標 2、3 |
| 要做哪些資料 | 表單編號、文件清單 | 目標 4 |
| 怎麼做 | 作業程序、執行方式 | 目標 3 |
| 什麼時候做 | 頻率、時間點、觸發條件 | 目標 4 |

### 0.4 教材品質底線

- **完整**：每個部門的 GDP 責任都有涵蓋
- **可查**：每個事實都能追溯到 SOP/WI/FR 原文
- **可快速記憶**：不放冗長純文字，用互動、圖表、短句
- **多互動**：每頁至少有一個真正的互動元件（非靜態排版）
- **資料正確**：數字、頻率、表單號、人員職稱符合文件原文
- **員工能開口說**：任何一個 fact，員工看完後要能用自己的話回答稽查員
- **員工能主動做文件**：員工看完第四目標後，不需要別人提醒就知道哪天、哪個觸發條件要填哪張表

---

## 一、GDP 智慧查詢 fact 拆解原則

### 1.1 核心定義

每一筆 `fact` 只能回答**一個可獨立問答的問題**，且只對應一個 SOP/WI/FR 的一個具體事實。

| 正確示範 | 錯誤示範 |
|---------|---------|
| `fact-inventory-cycle`：盤點頻率（每兩個月） | 把「盤點頻率＋溫度條件＋不符合品處理」塞入同一筆 |
| `fact-temp-mapping-cycle`：溫度測繪週期（每三年）| 把「溫度測繪週期＋夏冬月份＋測繪天數」混放一欄 |

### 1.2 拆解判斷準則

- **能獨立成一個稽查員問句** → 應是獨立 fact
- **同一 SOP 若涵蓋多個主題** → 每個主題各建一筆 fact
- **clarify（流程型）條目** → 保留廣泛描述，但所有具體 who/when/how many 必須拆至 fact

### 1.3 fact 標準格式

```javascript
docs.push({
  id: "fact-[主題]-[面向]",          // 例：fact-temp-mapping-cycle
  sourceType: "fact",
  topic: "簡短題目（5-15字）",
  international: "PIC/S GDP 相關條文一句話",
  taiwan: "食藥署規定一句話（或「食藥署無明文規定，由公司內部自行訂定」）",
  xinshing: "新勝做法一句話（含數字/表單號碼/人員職稱，可直接回答稽查的短句）",
  sop_ref: ["來源SOP文件編號"],
  keywords: ["精準關鍵字（覆蓋各種問法，不跨主題）"]
});
```

### 1.4 拆解後驗收要求

拆完 fact 後，**必須先測試**：問 A 只出現 A，不出現 B 或 C。

---

## 二、Keywords 設計規則

### 2.1 覆蓋原則：動詞變體 × 受詞變體

每個 fact 建立時，keywords 必須涵蓋：
- **動詞變體**：何時 / 什麼時候 / 幾點 / 幾天 / 多久 / 幾個月 / 如何 / 由誰
- **受詞變體**：表單號碼（FR54-01）/ 表單中文名 / 主題詞（盤點/測繪）

**交叉範例（`fact-receiving-when`）：**
```
"何時填FR54-01", "FR54-01何時填", "何時填", "何時驗收",
"什麼時候填FR54-01", "什麼時候驗收", "到貨驗收時間"
```

### 2.2 最短有意義單詞規則

凡有長 keyword，**同時必須補最短形式**：

| 長 keyword | 必須也有 |
|-----------|---------|
| 病媒防治 | 病媒 |
| 倉庫盤點 | 盤點 |
| 溫度測繪 | 溫測（缺字問法）|
| 衛生管理 | 衛生 |

### 2.3 工具/系統名稱禁止單獨作為 keyword

**規則：正航、GDPHUB、嘉里等工具/系統名稱，不可單獨作為 fact 的 keyword。**

只有「系統名稱＋主題詞」的組合才有意義：
- ❌ `"正航"` 單獨放入 `fact-purchase-order-who`
- ✅ `"正航採購單"` 放入對應採購 fact
- ✅ `"正航是什麼"`, `"正航系統"`, `"正航是不是"` 放入 `clarify-computer-system`

### 2.4 clarify 與 fact 的 keywords 分工

導入 fact 架構後：
- **clarify 條目**：只保留廣泛流程型 keywords（如「進出貨流程」「到貨驗收流程」）
- **具體 who/when/數字/表單號問法**：全部移到對應的 fact 條目，不得留在 clarify

### 2.5 快速問題按鈕

按鈕 `data-q` 的完整字串也必須是某個 keyword 的子字串（`.includes()` 匹配），否則快速按鈕點擊等同無效。

---

## 三、Intent Regex 禁止規則

### 3.1 禁止通用詞作為 intent regex 觸發條件

**踩坑根因：** 正航意圖比對 regex 含「溫度|電腦化系統」→ 命中任何含溫度的文件，包括進出貨、委外等完全不相關的條目。

**禁止規則：**

| 禁止寫法 | 原因 |
|---------|------|
| intent regex: `溫度` | 觸發所有含「溫度」的文件 |
| intent regex: `電腦化系統` | 觸發電腦化系統以外的條目 |
| intent regex: `幾個月` | 夏季幾個月 / 回收幾個月 全部誤觸 |
| intent regex: `倉庫` | 所有倉庫相關條目全部觸發 |

### 3.2 正確的 intent regex 精度要求

**規則：intent regex 要精確到文件編號層級，不可用通用詞。**

| 正確寫法 | 說明 |
|---------|------|
| `正航\|fr5701\|dp3501` | 只有含「正航」或對應文件編號的 fact 才加分 |
| `盤點\|fr5503\|dp5501` | 只有盤點相關文件才通過 |

### 3.3 硬排除規則（已實作，不可移除）

- 查詢含「盤點」→ 文件必須含 `盤點|fr5503|dp5501`，否則 score=0
- 查詢含「正航」→ 文件必須含 `正航|fr5701|dp3501`，否則 score=0

---

## 四、同分競爭處理規則

### 4.1 clarify 蓋過 fact 的防治

**問題根因：** clarify 條目 keywords 裡保留了專屬 who/when 字串，與 fact 直接競爭，且 clarify 因關鍵字豐富往往得更高分。

**處理規則：**
1. 發現 clarify 搶先 fact 時，首先檢查 clarify 的 keywords 是否有「不屬於 clarify 層級」的具體問法
2. 移除 clarify 的專屬問法 keyword，只保留廣泛流程型描述
3. 確認對應 fact 的 keywords 補齊後，重新驗收

### 4.2 評分機制（記錄現況）

- `sourceType:"fact"` 且 strongHit → +55 分（高於查詢校正 +45）
- `project-source-file` 評分上限：12 分（避免原始同步文字蓋過正式 KB）
- maxScore ≥ 50 時閾值：0.45（原 0.35，已收緊）

### 4.3 同分時的決策順序

1. sourceType 為 `fact` 的條目優先
2. 次之為 `clarify` 條目
3. `project-source-file` 最低優先（評分上限 12）

---

## 五、多筆結果顯示規則

### 5.1 現行邏輯（已實作）

命中 fact 時，最多顯示 4 筆完整三欄卡；超過 4 筆提示縮小查詢。

### 5.2 不相關結果跟著出現的處理

**問題：**「公司有沒有冷藏倉」第一筆正確，後面不相關的溫度警戒值、測繪週期、盤點頻率也跟著出現。

**處理規則（二選一）：**
- 方案 A：最高分與第二名分差 > 30 時，只顯示最高分一筆
- 方案 B：收窄各 fact 的 keywords，確保不被上位詞（「倉庫」「溫度」）誤觸

**現階段建議優先採用方案 B**（keyword 層面解決，不需改程式邏輯）。

### 5.3 初始訊息清除

**踩坑記錄：** 查詢執行後用 `insertAdjacentHTML("beforeend")` 附加氣泡，但從未清除初始的「尚未查詢」訊息，導致兩個訊息並存。

**規則：** 在開始處理查詢時，先清空 `kbMessages` 內容（`box.innerHTML = ""`），再附加 loading → 結果/錯誤。

---

## 六、三欄回答標準：PIC/S GDP、台灣 GDP、新勝做法

### 6.1 三欄各自的填寫原則

| 欄位 | 填寫標準 | 禁止出現 |
|-----|---------|---------|
| `international` | PIC/S GDP 相關條文一句話，原文摘要 | 「請查閱 PE 011」/ SOP 索引佔位文字 |
| `taiwan` | 食藥署「西藥優良運銷準則」相關規定一句話 | AI 備忘（「目前資料索引寫到…」）|
| `xinshing` | 新勝做法一句話，含數字/表單號/人員職稱 | AI 指令語、否定推論（「不可寫成…」）|

### 6.2 食藥署欄（`taiwan`）絕對禁止放公司文件

**踩坑根因（2026-05-25）：** 三層對照的「台灣食藥署」欄一開始混入 DP/WI/FR 文件編號，稽查員看到是誤導，且法規欄就不該出現公司內部文件。

| 食藥署欄正確放 ✅ | 食藥署欄禁止放 ❌ |
|---------------|---------------|
| 「西藥優良運銷準則第X章」| DP12-01、WI22-01 等公司文件 |
| 「藥事法第 XX 條」| FR54-01 等公司表單 |
| 「食藥署 GDP 指引」| 公司作業程序說明 |

**公司文件獨立放**：若需要列出對應的 SOP/WI/FR，應放在 `xinshing` 欄或額外的「參考文件」欄，不放進 `taiwan` 欄。

### 6.4 無明文時的標準寫法

- 法規**有明文規定**由誰負責 → 依法規原文填
- 法規**無明文規定** → 填「食藥署 GDP 規範無明文規定，由公司內部自行訂定」

### 6.5 「新勝做法」欄的定位與命名原則

**「新勝做法」欄的本質：** 這是「來自新勝醫藥公司文件的執行內容」，讓員工一看就知道：**這是我們公司的規定，稽查員問我時我就這樣回答。**

#### 顯示標籤建議

前台顯示時，「新勝做法」的標籤應讓人一眼看出是「公司文件的部分」，可考慮：
- **「我們公司怎麼做」**
- **「新勝醫藥執行方式」**
- **「公司做法（依內部文件）」**

避免讓人誤解為「AI 整理的建議」或「一般業界做法」。

#### 欄位內容：寫成員工的回答，不是 SOP 條文

**目標：** 員工看完這欄，面對稽查員的問題，能直接開口說出來，不需要再翻文件。

| 寫法類型 | 範例 | 是否正確 |
|---------|------|---------|
| **✅ 員工回答語氣** | 「我們是常溫藥品，倉庫溫度維持 15 到 25 度，警戒值設在 23.5 度，超過會提醒。」 | ✅ |
| **✅ 直接短答** | 「每兩個月全面盤點一次。」 | ✅ |
| ❌ SOP 條文複製 | 「依 DP33-01 第三節規定，倉庫溫濕度應符合儲存條件要求。」 | ❌ |
| ❌ 整段流程 | 「收到貨物後，進貨人員填寫 FR54-01，採購部列印紀錄表，品保核對…」 | ❌ |

#### 稽查問答格式（標準答案範例）

> **稽查員問：** 「你們公司倉庫溫度是多少度啊？」
>
> **員工應答（對應 `xinshing` 欄內容）：**
> 「15 度到 25 度，因為我們是常溫藥品。警戒值設在 23.5 度，超過的話系統會做提醒。」

這種答案的結構：**數字 → 原因 → 例外處理機制**

每個 fact 的 `xinshing` 欄都應朝這個格式靠攏。

### 6.6 新勝做法欄位的品質標準

- **正面事實描述**：員工可直接說出口的短句
- **員工可直接回答稽查員**，不放冗長 SOP 敘述
- **只寫 SOP/WI/FR 原文有的內容**，不推論、不補充
- **禁止的寫法範例：**
  - ❌「不可寫成冷藏倉」（AI 指令）
  - ❌「本頁資料來源為…」（資料來源說明）
  - ❌「不是溫度監控系統」（AI 否定推論，文件沒有這樣記載）
  - ❌「目前資料索引尚未涵蓋…」（AI 備忘）
  - ❌「依 DP33-01 第X節規定…」（SOP 條文語氣，不是員工的話）

### 6.6 AI 建資料時的事實核對表（勿搞錯新勝的正確值）

> **此表目的：** 防止 AI 在建立 KB 條目時把新勝的正確事實寫錯。
> **此表不是教材的「常見缺失」清單**——常見缺失另見 6.7 節。

| 主題 | 新勝正確事實 | AI 最容易寫錯成 | 說明 |
|-----|------------|--------------|------|
| 倉庫類型 | 常溫倉，**無冷藏倉** | 冷藏倉、冷鏈 | 公司只有一間常溫倉 |
| 溫度記錄頻率 | 每 5 分鐘記錄一次 | 每 15 分鐘 | 自動記錄器頻率 |
| 內部稽核頻率 | 每年 12 月（DP82-01）| 每 6 個月 | 每年一次 |
| 溫度測繪週期 | 每三年或重大變更後；夏季 7-9 月、冬季隔年 1-3 月，各 7 天 | 每年 | 三年週期 |
| 溫度警戒值 | 23.5°C | 其他任意數值 | 此數值來自 DP33-01 |
| 庫存盤點頻率 | 每兩個月全面盤點一次（DP55-01）| 每月 / 每季 | 兩個月一次 |
| 模擬回收演練 | 每年執行 1 次 | 每半年 / 每季 | 每年一次，有紀錄 |
| 紀錄保存年限 | 至少 5 年 | 3 年 / 7 年 | 最短 5 年 |
| 正航系統 | 確效過的進銷存電腦化系統（FR57-01）| 溫度監控系統 | ERP，不是溫控設備 |
| 物流門管制時間 | 15:00-15:30 | 14:00 | 收貨期間進貨人員不得執行進貨作業 |

---

### 6.7 教材「常見缺失」的來源規則

> **硬性規則：教材中「常見缺失」的內容，必須來自實際公開的稽查資料，不可由 AI 自行推測。**

#### 合法來源（只能從這三類取材）

| 來源類型 | 說明 | 取材方式 |
|---------|------|---------|
| **PIC/S GDP 稽查指引** | PIC/S 官方發布的 GDP 查核重點與常見缺失 | 讀 PE 011 系列文件 |
| **台灣食藥署公開資料** | 食藥署公布的藥品運銷稽查結果、GDP 常見缺失報告 | 食藥署官網查詢 |
| **台灣 GDP 稽查公告** | 食藥署對藥品批發商的查核通報 | 官方公告 |

#### 禁止的來源

| 禁止 | 原因 |
|-----|------|
| AI 自行推測「可能有人會犯這個錯」| 未必是真實稽查缺失，誤導員工 |
| 與新勝正確做法相反的假設 | 例：新勝溫度 23.5°C → 寫「其他數值是常見錯誤」這不是稽查缺失 |
| 業界傳說或非官方資料 | 不具稽查公信力 |

#### 正確範例：兩種缺失類型的差異

| 缺失類型 | 範例 | 說明 |
|---------|------|------|
| **根本未執行** | 「公司未設定溫度警戒值」 | 稽查員最常見 → 壓根沒做 |
| **根本未執行** | 「未執行模擬回收演練，無演練紀錄」 | 有規定但從未做 |
| **記錄不完整** | 「訓練紀錄缺考核結果」 | 有做但文件不完整 |
| **執行不一致** | 「實際盤點間隔超過 SOP 規定」 | 做了但未照 SOP 頻率 |
| **職責不清** | 「無法確認溫度記錄的審核責任人」| 文件沒有明確誰負責 |

#### 「常見缺失」建立前的必做確認

```
□ 這個缺失是否出現在 PIC/S PE 011 或食藥署公告中？
□ 如果是，能引述具體來源嗎？（「PIC/S PE 011-3 附件X」或「食藥署OO年查核報告」）
□ 缺失的描述是「沒有做 X」或「X 做得不完整」，不是「X 用了錯誤數值」？
□ 這個缺失跟新勝的實際做法有直接對應嗎？（讓員工看了知道自己公司有沒有這個問題）
```

#### 外部明文與公司 SOP 的自動核對規則

使用者 2026-06-10 裁示：`PIC/S 明文但公司 SOP 漏列項目盤點` 不是一個等使用者另行交辦的獨立待辦，而是 agent 在製作資料時必須同步執行的自動比對。

適用情境：

- 建立或修正政府公開常見缺失。
- 撰寫稽查員問法、公司回答方向、內部人員標準回答。
- 建立或逐題核對測驗題庫。
- 拆 fact、補 keywords、校對三欄回答。
- 從 Firestore 或 HTML fallback 讀回資料做逐筆檢查。

執行方式：

1. 先確認外部來源是否有明文要求或查核重點，例如 PIC/S、食藥署公開常見缺失、台灣 GDP 規範。
2. 再回公司 SOP/WI/FR 原文核對是否有對應做法、數值、頻率、表單或責任人。
3. 若公司 SOP 有明文，才可寫成新勝做法或內部人員回答。例如常見缺失涉及倉庫溫度時，可回答「15 度至 25 度」的前提，是已核對到公司 SOP 明文來源。
4. 若外部有明文，但公司 SOP/WI/FR 沒有明文，不得自行補成公司制度、不得放入新勝做法欄、不得直接做成正式測驗題。
5. 上述落差應記錄為「外部明文與公司 SOP 差異清單」，交由使用者判斷是修改 SOP、列為外部查核提醒，或不納入教材。

差異清單至少包含：

| 欄位 | 說明 |
|------|------|
| 外部來源 | PIC/S、食藥署或官方資料標題與年度 |
| 外部明文重點 | 外部要求或常見缺失的短句 |
| 對應公司文件 | 已核對的 SOP/WI/FR 文件名稱或代碼 |
| 公司文件狀態 | 有明文 / 未見明文 / 文字不一致 |
| 建議處理 | 可作教材、只作外部提醒、需使用者判斷是否修 SOP |
| 不可自動做的事 | 是否禁止進題庫、禁止寫入新勝做法、禁止當作公司缺失 |

---

## 七、前台內容同步規則

### 7.1 必須同步的六個區塊

**硬性規則：** 改動任何一個 fact 時，以下六個區塊必須全部同步。

1. GDP 智慧查詢 fact（HTML kbStore + Firestore `gdpKnowledgeBase`）
2. 各部門頁面的「作法」與重點摘要
3. 「規範到現場」對照區
4. 「部門文件閱讀地圖」
5. 「稽查重點互動核對」
6. FAQ / 快速問題按鈕與手動搜尋入口

### 7.2 同步執行順序

1. 決定要修改的 fact 前，先列出「此 fact 影響哪些前台區塊」
2. 修改 fact 資料
3. 逐一驗收上述六個區塊是否顯示一致
4. 確認無落差後，才算此 fact 修改完成

### 7.3 標題一致性規則

**規則：標題必須符合實際內容，節點數/按鈕數/欄位數必須與顯示內容一致。**

- 若「規範到現場」只有三欄（PIC/S GDP / 台灣食藥署 / 新勝做法），**不得標成「四層對照」**
- 曾有四層，後來移除第四欄，必須同步更新標題

### 7.4 文件連結政策

依老闆文件保護指示（2026-05-27 確立）：
- **不做**可點擊文件按鈕，不顯示 SOP 路徑或內部文件細節
- 文件名稱以**純文字**列示即可（員工知道要申請哪份文件）
- 外部法規連結（PIC/S、食藥署官網）屬公開資訊，不受限制
- 員工如需閱讀原文件，向文管部填單申請調閱

---

## 八、GitHub Pages 驗收規則

### 8.1 驗收的唯一正式來源

```
https://n1116839.github.io/xinshing-gdp-training/
```

**本機驗收只算預檢，不算完成。** 必須 push 並從上述 URL 確認才算完成。

### 8.2 驗收標準程序

```
1. git commit + push 到 codex/gdp-html-training-pages
2. 等待 GitHub Pages 部署（約 1-3 分鐘）
3. 開啟永久網址
4. 對應修改區域逐項確認（點擊/輸入/查詢）
5. 在 HANDOFF 寫「已用 GitHub Pages URL 驗收：○○正常、○○正常」
6. 若發現錯誤 → 修正 → 再次 commit + push → 重新驗收
```

### 8.3 驗收記錄要求

**沒有驗收記錄 = 這次修改未完成。**

HANDOFF 中必須明確標示：
- ✅ 本機 N/N 通過（預檢）
- ✅ 已 push GitHub Pages，永久連結驗收通過

### 8.4 驗收腳本使用方式

使用 `node verify_facts_ghpages.mjs` 驗收時：
- 本機模式：對 HTML 本機文件抽取 kbStore 資料並模擬搜尋
- GitHub Pages 模式：第 134-135 行改為 `fetchUrl GH_PAGES_URL`

---

## 九、收工格式

### 9.1 收工必做五項（缺一不可）

```
□ 1. 更新 HANDOFF.md（本次完成 + 下次待辦 + 踩坑補記）
□ 2. 更新 踩坑紀錄.md（G:\我的雲端硬碟\我的第二大腦\踩坑紀錄.md）
□ 3. 更新 專案 note（G:\我的雲端硬碟\我的第二大腦\專案\2026codex AI測試.md）
□ 4. git commit（含本次所有改動）
□ 5. git push origin codex/gdp-html-training-pages
```

只口頭說收工、不執行上述五項，均視為**未完成收工**。

### 9.2 HANDOFF 更新格式

每輪收工在 HANDOFF 新增一節，包含：

```markdown
## 本輪完成（YYYY-MM-DD 第N次開工）commit [hash]

### [主要工作標題] ✅

[完成內容條列]

**驗收：** 本機 N/N 通過，已 push GitHub Pages

### ⚠️ 尚未完成（下次開工優先）

| 章節 | 待補項目 |
|------|---------|
| ... | ... |

**下次開工必做：**
1. ...
2. ...
```

### 9.3 踩坑記錄格式

每個踩坑必須包含：
1. **問題現象**（使用者看到了什麼）
2. **根本原因**（程式/資料/流程哪裡出了問題）
3. **修正方向**（具體可執行的修正步驟）
4. **規則**（未來要遵守的原則，防止重犯）

---

## 十、每章測試標準

### 10.1 測試數量要求（永久執行）

| 測試層級 | 要求 |
|---------|------|
| 每個問題 | 至少 **10 種不同問法**（同主題不同說法各算 1 種） |
| 每章 | 至少 **10 個不同問題**（不同主題） |
| 每部門 | 至少 **20 個不同問題** |
| 重點覆蓋 | 國際法規（PIC/S GDP）與台灣食藥署**常見缺失項目**必須包含 |

「不同問法」範例（倉庫盤點，以下算 10 種）：
1. 倉庫多久盤點一次
2. 盤點頻率是什麼
3. 多久盤點一次
4. 庫存多久盤點
5. 盤點週期
6. 幾個月盤點一次
7. 庫存盤點怎麼做
8. DP55-01 盤點
9. 庫存管理盤點頻率
10. 倉庫庫存幾個月檢查

### 10.2 測試的正確順序：搜尋結果只是假設，文件才是答案

> ⚠️ **這是最常被忽略的規則：查到答案 ≠ 答案正確。**

**正確的測試流程：**

```
Step 1：用 GDP 智慧查詢輸入問題
          ↓
Step 2：取得搜尋結果（fact 三欄內容）
          ↓
Step 3：回到資料夾，開啟對應的 SOP/WI/FR 原文
          ↓
Step 4：比對搜尋結果與文件原文是否一致
          ↓
Step 5a：一致 → 此題驗收通過 ✅
Step 5b：不一致 → 記錄差異，修正 KB，重新測試
```

**禁止的做法：**
- ❌ 查到結果就直接標記「通過」
- ❌ 結果「看起來合理」就當作正確
- ❌ 沒有找到對應的 SOP 原文就宣告驗收通過

**為什麼必須回查文件：**
KB 資料是人工建立或 AI 生成的，可能含有：
- 數字錯誤（溫度、頻率、期限）
- 事實錯誤（冷藏倉、正航用途）
- AI 自行補充的推論（文件沒有的描述）
- 亂碼（偉→偵、揃→揀）

搜尋引擎只負責「找到最相關的那筆」，不負責「那筆是否正確」。**正確性驗證是人（或 AI 讀文件）的責任，不是搜尋引擎的責任。**

### 10.3 測試驗收三件事（文件核對完才能做這三件事）

1. **第一名是否正確**：命中的 fact 就是該問題的答案（已與原文核對）
2. **有沒有多餘不相關結果**：第二名以後不應出現牛頭不對馬嘴的條目
3. **新勝做法是否能直接作為稽查應答**：員工看到此答案，稽查員問時可直接回答

### 10.4 修正時的批次歸因規則

發現問題時，先歸因，再修正，不可亂加 keyword：

| 症狀 | 正確處置 |
|-----|---------|
| 泛命中（答非所問） | 收窄 keyword、降低通用詞權重、加硬排除 |
| 查不到（沒有結果） | 補自然問法、缺字問法、同義詞、文件編號變體 |
| 回太長（整段 SOP） | 再拆 fact，不把整段塞進一筆 |
| 答案像 AI 備忘 | 改成正式三欄教材語氣；備忘寫進第二大腦，不進 KB |
| clarify 蓋過 fact | 移除 clarify 的專屬 who/when keywords |

#### 拆 fact 實例：溫度測繪

**情境：** 一條 fact「倉庫溫度測繪週期」的 xinshing 同時包含測繪頻率（每三年）和季節月份（夏季7-9月、冬季1-3月）。

**問題：** 查「夏季溫測幾月份」會看到「每三年」為主的整段答案，而非直接答「7～9月」。

**解法：** 拆分為三筆 fact，維持原始完整 fact 作為泛用查詢結果：

| fact ID | topic | xinshing | 用途 |
|---------|-------|----------|------|
| `fact-temp-mapping-cycle` | 倉庫溫度測繪週期 | 每三年（或重大變更後）執行倉庫環境溫度測繪；夏季於 7～9 月、冬季於隔年 1～3 月，每次測量 7 天。 | 泛用查詢（「溫測」「測繪週期」），保留完整資訊 |
| `fact-temp-mapping-summer` | 夏季溫度測繪月份 | 夏季溫測於 7～9 月執行，每次連續測量 7 天。 | 精準查詢（「夏季溫測幾月份」），直接答月份 |
| `fact-temp-mapping-winter` | 冬季溫度測繪月份 | 冬季溫測於隔年 1～3 月執行，每次連續測量 7 天。 | 精準查詢（「冬季溫測幾月份」），直接答月份 |

**關鍵原則：**
- **拆 fact 後，原始完整 fact 不得縮減內容。** 泛用查詢仍須看到完整資訊，而精準查詢則由專屬 fact 給出直答。
- 各子 fact 的 keyword 不可互相污染：夏季 fact 的 keyword 如「夏季溫測」不應以 startsWith 匹配泛用查詢「溫測」（已在原始 fact 預留「溫測」開頭 keyword，確保 startsWith 匹配數 cycle > summer）。
- 短查詢（≤2 字）優先匹配多個 keyword 開頭相同的 fact，因此「溫測」→ cycle（5 個 keyword 開頭為「溫測」），而非 summer（1 個）。

### 10.5 宣告「完成」的條件

**不得宣稱一次到 100%。** 正確流程：

```
1. 選一章做完整樣板
2. 從 SOP/WI/FR 拆 fact
3. 每個 fact 補 10 種問法
4. 建測試表（10 個問題 × 10 種問法）
5. 本機預檢
6. push + GitHub Pages 永久網址驗收
7. 驗收通過後，複製同一流程到其他章
```

**宣告一章完成的最低條件：**
- 測試表 10 題全部通過
- 三欄內容符合 SOP 原文（可追溯）
- GitHub Pages 驗收記錄已寫入 HANDOFF

---

## 十一、禁止 AI 備忘進前台

### 11.1 三欄禁止出現的語句類型

KB 的三個欄位（`international` / `taiwan` / `xinshing`）是學員看到的**教材內容**，任何對 AI 的指令語、備忘、校正提示一律不得出現。

| 違規語句類型 | 具體範例 | 違規原因 |
|------------|---------|---------|
| AI 校正提示 | 「不可寫成冷藏倉」 | 對 AI 的指令，不是法規說明 |
| AI 否定推論 | 「不是溫度監控系統」 | 文件沒有這樣記載，不可補充否定句 |
| 資料來源說明 | 「此筆來自本頁已整理的 SOP 索引」 | 索引佔位，不是實質答案 |
| 開發者備忘 | 「目前資料索引寫到…但未明定夏冬月份」 | AI 的工作筆記，不是教材 |
| 追溯提示 | 「請依文件編號回到正式核准文件確認完整條文」 | 員工需要直接答案，不是索引路徑 |
| 未完成提示 | 「（待補）」「（暫缺）」 | 教材不應有未完成的欄位 |

### 11.2 正確的處置方式

- 有任何需要提醒 AI 的注意事項 → 寫進踩坑紀錄.md 或 HANDOFF.md，**不進 KB 欄位**
- 三欄有無法填寫的情況（法規無明文）→ 用標準寫法：「PIC/S GDP 無明文規定」/「食藥署 GDP 規範無明文規定，由公司內部自行訂定」
- xinshing 欄位找不到 SOP 依據 → 欄位留白或填「依公司內部規定執行」，**不補推論句**

### 11.3 驗收時的掃描規則

每次宣告 KB 完成前，對所有 fact/clarify 條目執行字串掃描，確認三欄不含：

```
不可寫成 / 不得寫成 / 此筆來自 / 文件未載明 / 目前資料 /
待補 / 暫缺 / 請依文件編號 / 本教材 / 資料來源
```

---

## 十二、資料來源優先順序

### 12.1 優先順序（由高到低）

1. **SOP/WI/FR 原文**（資料夾內文件，逐份讀取）
2. **使用者在對話中明確確認的最新版事實**（例：「溫度測繪夏季是 7-9 月」）
3. **Firestore `gdpKnowledgeBase`**（已核對且部署的資料）
4. **HTML kbStore fallback**（最後兜底，應與 Firestore 一致）

**若同一事實有新舊版本：以資料夾內較新的實際文件內容為準。**

### 12.2 來源衝突時的處置

| 衝突情形 | 處置方式 |
|---------|---------|
| Firestore 與 HTML fallback 不一致 | 兩邊同步修正，不可只改其中一邊 |
| SOP 原文與現有 KB 資料不符 | 以 SOP 原文為準，更新 KB 並記錄修正 |
| 目錄/行事曆與 SOP 原文有出入 | 列出差異，請使用者確認後才更新 |
| 不同版本 SOP 內容有差異 | 以資料夾內日期較新的版本為準 |

### 12.3 不可做的事

- **不可自行依判斷增加文件上沒有的事情**（不補推論、不補背景知識）
- **不可只修一個地方**：同一事實在多處出現，必須統一一次性修正
- Firestore 資料若是 AI 生成，有高機率出現亂碼（偉→偵、揃→揀、雔→隔），每次建置後需逐欄讀回核對

### 12.4 測試時的來源驗證規則

**搜尋結果只是「KB 資料庫的回答」，不等於「正確答案」。**

測試時若搜尋到一筆 fact，必須：
1. 找到 `sop_ref` 欄位列出的來源文件
2. 打開資料夾內對應的 SOP/WI/FR 原文
3. 確認搜尋結果的數字、頻率、表單號、人員職稱與原文完全一致

若 KB 資料與文件原文不一致：
- **以文件原文為準**
- 修正 KB（HTML kbStore + Firestore 兩邊同步）
- 記錄修正內容到 HANDOFF 與踩坑紀錄

→ 詳細流程見第十章 10.2 節

---

## 十三、驗收失敗分類與對應處置

### 13.1 六種失敗類型

| 失敗類型 | 症狀描述 | 對應處置 |
|---------|---------|---------|
| **查不到** | 輸入問題，結果為空或「找不到命中資料」 | 補自然問法 keyword、缺字問法、同義詞、文件編號變體 |
| **泛命中** | 第一名正確，但後面出現大量不相關結果 | 收窄 keyword、加硬排除、降低通用詞權重 |
| **答太長** | 命中了，但顯示整段 SOP 流程而非直接答案 | 再拆 fact，每筆只回答一個問題 |
| **答非所問** | 命中了，但答案與問題完全無關 | 確認 keyword 是否有跨主題污染；檢查 intent regex |
| **同分錯誤** | clarify 蓋過 fact，或次要 fact 蓋過主要 fact | 移除 clarify 的專屬 who/when keywords；或補強 fact 的 strongHit keywords |
| **前台不同步** | 智慧查詢答 A，部門頁顯示 B | 依第七章同步規則，找出所有出現處並統一更新 |

### 13.2 失敗根因追蹤流程

```
發現失敗
  ↓
1. 先確認是哪種失敗類型（上表六種之一）
  ↓
2. 找出根本原因（keyword？資料粒度？clarify 污染？）
  ↓
3. 批次修正同類問題（不可只修一題就宣告完成）
  ↓
4. 修正後重跑整章測試表，確認無副作用
  ↓
5. 寫入踩坑紀錄（問題現象 + 根因 + 修正方向 + 規則）
```

### 13.3 禁止的修正方式

- **禁止只修一題**：發現泛命中，只收窄那一個 keyword，不檢查同章其他 fact 是否有同樣問題
- **禁止亂加 keyword**：查不到就把所有想得到的詞全加上去，導致泛命中
- **禁止收工前不重跑測試表**：每次修正後，相關章節的全部測試題都要重跑一遍

---

## 十四、開工必讀文件清單

### 14.1 每次對話開工，按順序讀

| 順序 | 文件 | 路徑 | 讀取目的 |
|-----|-----|------|---------|
| 1 | HANDOFF.md | `G:\我的雲端硬碟\2026codex\AI測試\HANDOFF.md` | 上次做到哪、下次必做什麼 |
| 2 | 踩坑紀錄.md | `G:\我的雲端硬碟\我的第二大腦\踩坑紀錄.md` | 長期規則、已知錯誤模式 |
| 3 | 專案 note | `G:\我的雲端硬碟\我的第二大腦\專案\2026codex AI測試.md` | 專案整體方向與決策記錄 |

### 14.2 補充讀取（需要時）

| 文件 | 路徑 | 何時讀 |
|-----|------|-------|
| AGENTS.md | 專案根目錄 | 跨 AI agent 共用規則確認 |
| CLAUDE.md | 專案根目錄 | Claude Code 特定開工指示 |
| GDP_智慧查詢規範.md | 專案根目錄（本文件）| 規則有疑義時查閱 |

### 14.3 從摘要接續時的特別提醒

**從摘要接續（context 壓縮）時，同樣必須先讀第二大腦與踩坑紀錄，再開始任何修改。**

摘要只記錄上次做到哪，不包含第二大腦的長期規則。兩者都讀才算完整開工。

### 14.4 開工後的第一步

讀完上述文件後，**詢問使用者從哪個待辦項目開始，不可自行決定**。

---

## 十五、禁止事項彙整

### 15.1 作業流程禁止事項

| 禁止行為 | 後果 | 正確做法 |
|---------|------|---------|
| 一次同時處理多個大項（KB ＋ 互動化 ＋ 字體 ＋ 面板）| 任一未通過驗收，全部被回退 | 一次只做一項，驗收通過後再做下一項 |
| 只用本機驗收就宣告完成 | 永久網址可能是舊版 | 必須 push 並從 GitHub Pages 永久網址驗收 |
| 只補單一 keyword 就收工 | 同類問題下次繼續踩 | 批次歸因，同類問題一次修完 |
| 宣告「KB 完成」前未跑測試表 | 只是修改了資料，不代表搜尋正確 | 每章 10 題 × 10 問法全部跑完才算完成 |
| 修 KB 未同步前台 | 智慧查詢答 A，部門頁顯示 B | 依七章同步規則，所有區塊統一更新 |
| 收工只口頭說收工 | 換電腦或換 AI 後進度斷鏈 | 必須執行九章五項收工程序 |

### 15.2 資料內容禁止事項

| 禁止行為 | 具體說明 |
|---------|---------|
| KB 欄位放 AI 指令語 | 詳見第十一章 |
| KB 欄位放否定推論 | xinshing 欄只放 SOP 有明確記載的正面事實 |
| 出現「冷藏倉」「冷鏈」 | 公司無冷藏倉，這是錯誤事實 |
| HTML 本身作為 KB 來源 | 搜尋結果會顯示 HTML 標籤與 JS 程式碼 |
| clarify 條目保留具體 who/when keywords | 導入 fact 架構後，這些 keywords 必須移至對應 fact |
| 工具名稱單獨作為 keyword | 正航/GDPHUB/嘉里單獨放 keyword，會跨主題誤命中 |

### 15.3 程式與部署禁止事項

| 禁止行為 | 說明 |
|---------|------|
| 修改 HTML 後不 push 就收工 | 永久網址仍是舊版 |
| Firestore 資料不讀回驗證 | 中文字元在 Firestore 容易亂碼（偉/揃/雔等） |
| push 後不等 GitHub Pages 部署就驗收 | 約需 1-3 分鐘，太快驗收看到舊版 |
| 繞過安全審核推送外部 remote | 必須取得使用者明確授權 |

---

## 十六、互動設計原則

### 16.1 「視覺化」的正確定義

本專案中「視覺化」= **真正的 HTML 互動元素**，不是靜態排版或卡片美化。

| 屬於互動式 ✅ | 不算互動式 ❌ |
|------------|------------|
| 滑桿（`<input type="range">`）| 改卡片排版、換圖示 |
| 拖拉（drag-and-drop、swipe）| 只加陰影、漸層 |
| 點擊叫出（Modal / Accordion / Tab）| Hover 顏色變化（無 JS 事件）|
| 輪播（Carousel，自動或手動）| 改字體大小 |
| 動態表單（即時驗證、條件顯示）| 加動畫說明文字 |
| JS 事件驅動的內容切換 | RWD 響應式佈局 |

### 16.2 互動類型對應使用時機

| 互動類型 | 適合的內容場景 |
|---------|-------------|
| Accordion / 展開收合 | 稽查重點清單、FAQ 列表 |
| Modal / Lightbox | 補充說明、文件申請通知、圖片放大 |
| Tab panel | 不同部門/法規層/頻率分類切換 |
| Checkbox / 勾選 | 稽查核對清單、學習進度 |
| Range slider | 風險等級、頻率視覺化 |
| Scroll trigger | 元素進入畫面時滑入顯現（fade-in）|
| Ripple / 下陷效果 | 按鈕點擊回饋 |

### 16.3 互動優先順序（H4 原則）

> **大更動不影響其他區域的先做；牽一髮動全身的擺最後；有副作用的測試項目最後處理。**

```
1. KB 精準搜尋（獨立，不影響其他 UI）
2. H1 員工登入（後端改動，不影響現有 UI 顯示）
3. H2 職稱別權限（依賴 H1）
4. H3 晉升審核（依賴 H1+H2）
5. 全站互動化升級（牽動全站 UI，擺最後）
```

---

## 十七、頁面結構規則（禁止重複區塊）

### 17.1 文件清單：只能出現一次

每個部門頁目前有三個文件相關區域，**只保留一個**：

| 區域名稱 | 保留建議 |
|---------|---------|
| 頂部 pills（`pagePills`）| 在有 `departmentDocumentMaps` 的頁面已隱藏，不得重複顯示 |
| 部門文件閱讀地圖（`documentMapSection`）| **建議保留**（分類最清楚） |
| 本頁相關 SOP（`docButtons`）| 與文件地圖重複，擇一 |

**規則：** `pagePills` 在有 `departmentDocumentMaps` 的頁面必須隱藏，不可同時顯示兩個文件清單。

### 17.2 時程資訊：只能出現一次

| 區域 | 處置 |
|-----|------|
| `schedule` 區塊（基本時程）| 與 `deptScheduleSection` 重複 |
| `deptScheduleSection`（頻率分 tab 互動）| **建議保留**（互動功能較完整）|

**規則：** 同一資訊出現兩次無附加價值，選功能較完整的保留，另一個移除。

### 17.3 kbSection 佈局規則

- `kbSection` 用 `panel()` 直接放入 `.grid`
- **不可**用 `kb-wrap` 包裝（會縮成 1 欄）

### 17.4 標題一致性

標題必須與實際內容完全吻合：

| 情境 | 規則 |
|-----|------|
| 「規範到現場」只剩三欄 | **不得**繼續標為「四層對照」 |
| 節點數、按鈕數 | 必須與實際顯示欄位數量一致 |
| 修改內容後 | 標題必須同步更新 |

---

## 十八、稽查重點區塊設計規範

### 18.1 現行問題

稽查重點區塊目前多為靜態查核句列表，不符合使用者期待。

### 18.2 正確格式

稽查重點區必須改為**學習型格式**，包含四個要素：

```
1. 常見缺失（稽查員最常抓到什麼）
2. 稽查員可能怎麼問（問句範本）
3. 公司如何回答（標準應答短句）
4. 要準備什麼證據（表單/記錄編號）
```

**範例（教育訓練）：**
> - 常見缺失：訓練紀錄不完整，或找不到考核結果
> - 稽查員問：「你們教育訓練怎麼做？合格標準是什麼？」
> - 公司回答：「分為內部與外部教育訓練，考核分數 ≥70 分合格，保存訓練紀錄。」
> - 準備證據：FR24-01（年度訓練計畫）、FR24-02（考核表）

### 18.3 互動型態要求

稽查重點區若要做互動化，必須使用以下之一：
- Accordion（展開每條缺失的詳細問答）
- Checkbox（員工自評已準備哪些證據）
- Tab（依缺失類型分類切換）

---

## 十九、手機版設計規則

### 19.1 下拉選單

**禁止**：手機主導覽依賴原生 `<select>`

**原因**：原生 `<select>` 容易受手機瀏覽器、外層捲動容器或點擊事件影響，操作體驗差。

**正確做法**：
- 改用自製大按鈕下拉面板
- 每個選項都是可點擊 `<button>`
- 同步顯示目前頁面狀態（目前選項文字更新）

### 19.2 桌機 / 手機分流原則

- 桌機左側導覽：分組按鈕，維持原本設計，不受手機修正影響
- 手機導覽修正後：必須確認 JS 語法正確、選單可開合、點選後切換頁面並更新選項文字

### 19.3 部門篩選按鈕

保留的部門（依 SOP 內容）：
```
GDP 權責人 / 管理藥師 / 品保 / 倉管 / 採購 / 業務 / 文管 / 人事
```

---

## 二十、前台禁止顯示的內容

### 20.1 UI 禁止出現的文字類型

| 禁止類型 | 具體範例 | 原因 |
|---------|---------|------|
| Meta 說明文字 | 「資料由公司人員定期更新」 | 非教材內容 |
| 系統欄位 | 「更新者：system」 | 開發者資訊不應暴露 |
| AI 備忘 | 「待補」「此筆來自…」 | 詳見第十一章 |
| SOP 檔案路徑 | Google Drive 路徑 | 文件保護政策 |
| SOP 詳細閱讀重點 | 「第三節 3.1.2 規定…」| 公司內部細節不可外流 |
| 使用者回饋語言 | 「老師說這裡要改」 | 只屬於開發紀錄 |

### 20.2 文件連結政策（老闆 2026-05-27 決定）

**目的：** 防止競業取得平台 URL 後，看到新勝 SOP 架構與執行細節並照抄。

| 項目 | 政策 |
|-----|------|
| `doc-pill` 可點擊文件按鈕 | 移除，改為純文字文件名稱 |
| `openDoc()` modal 內容 | 改為「此文件為公司內部管控文件，請向文管部填單申請調閱」 |
| 部門文件閱讀地圖 | 加申請說明橫幅；只以純文字列示文件名稱與編號 |
| 外部法規連結（PIC/S、食藥署）| 保留，屬公開資訊 |

### 20.3 部門文件閱讀地圖的正確用途

**不是**：可點擊的文件連結清單

**應該是**：
- 學習重點摘要
- 資料準備重點（稽查前要備什麼）
- 若仍列文件，只能純文字，不可加連結

---

## 二十一、多個待辦同時處理的規則

### 21.1 核心原則

**一次只做一個待辦，驗收通過後再做下一個。**

**踩坑根因：** 曾一次同時處理 KB 問題＋互動化＋字體＋面板移除，最後因 KB 未通過驗收，互動化全部被回退，等於白做。

### 21.2 待辦優先順序判斷

```
使用者第一眼看到的功能 → 先修
（KB 查詢是第一眼功能，優先於視覺互動化）

獨立、不影響其他區域 → 先做
（KB 精準搜尋獨立修改，不影響 UI）

牽一髮動全身 → 擺最後
（全站互動化升級影響全站 UI，最後做）
```

### 21.3 在每輪開工時的確認流程

1. 讀 HANDOFF.md 的待辦清單
2. **詢問使用者要從哪一項開始**，不可自行決定
3. 確認後只做那一項
4. 驗收通過才問「下一項做什麼？」

---

## 二十二、「作法」區塊設計規範

### 22.1 作法顯示標準

「作法」區應顯示：
- 員工可**直接理解**的重點短句
- 稽查員問時**可直接回答**的簡要說明
- 不放冗長 SOP 敘述（不把整段程序書貼上去）

### 22.2 各部門作法範例方向

| 部門 | 作法顯示重點範例 |
|-----|--------------|
| 人事/教育訓練 | 人員教育訓練規劃、訓練通知、訓練紀錄保存、內部/外部教育訓練 |
| 倉管 | 庫存管理、每兩個月全面盤點、FEFO 原則、溫濕度記錄 |
| 採購 | 合格廠商評鑑、到貨驗收、FR54-01 填寫 |
| 品保 | 偏差管理、CAPA 開立、內部稽核年度計畫 |

### 22.3 外部教育訓練標準回答

> 「配合政府部門、學術單位及研發機構所舉辦有關 GDP 的人才培訓課程與研討會。」

---

## 二十三、開發版本管理與回退規則

### 23.1 修改前備份原則

每次修改前，應確認：
- 當前 commit hash 已記錄（HANDOFF 中的最後 commit）
- 本機 Git 狀態乾淨（無未 commit 的修改）

### 23.2 回退發生時的處置

| 情境 | 處置 |
|-----|------|
| 使用者要求回退 | 記錄哪個 commit 被回退，不再重做被回退的功能 |
| 被回退的功能 | 必須重新確認使用者意圖後，才能重新做 |
| eyebrow 字體 16px→18px 被回退 | 待使用者明確要求再做，不自行加回 |

### 23.3 一次只 commit 一個目的

- **不把 KB 修正與 UI 修正混在同一個 commit**
- commit message 要精確描述改了什麼（不用「修正多處問題」）
- 每次 push 後等 GitHub Pages 部署（1-3 分鐘）再驗收

---

## 二十四、環境與工具限制備忘

### 24.1 開發環境

| 項目 | 值 |
|-----|---|
| 預覽 server | `npx serve -p 3333`（launch.json 已設定） |
| Shell | PowerShell 5.1（`python`/`py` 不可用） |
| 中文路徑 | 用 UTF-8 儲存，避免亂碼 |
| Obsidian MCP | 中文路徑有時亂碼，改用 Read tool 直接讀寫 |

### 24.2 Firestore 注意事項

| 注意點 | 說明 |
|-------|------|
| 中文亂碼高機率 | 每次寫入後必須讀回逐欄核對 |
| 常見錯字 | 偉→偵、揃→揀（日文漢字）、雔→隔、山害→有害 |
| Unicode 坑 | U+63C3（揃，日文）≠ U+63C0（揀，繁中） |
| rules 部署方式 | 用 MCP tool `firebase_deploy(only:"firestore:rules")`，不需 CLI |

### 24.3 GitHub Pages 注意事項

| 注意點 | 說明 |
|-------|------|
| 部署延遲 | push 後約 1-3 分鐘才生效 |
| 只改 Firestore | 不需 push，永久網址即時反映 |
| 只改 HTML/CSS/JS | 必須 commit + push 才會更新 |
| packed-refs.lock 警告 | 無害，commit 仍成功，忽略即可 |

---

## 二十五、修改前備份規則

### 25.1 動手前必做的兩個確認

```
Step 1：git status
  → 若有未 commit 的修改，先 commit checkpoint（不要在 dirty state 開始改）

Step 2：記下當前 commit hash
  → 寫進 HANDOFF「本輪開始 commit：[hash]」
  → 若修改失敗需回退，確保有明確的回退點
```

### 25.2 Checkpoint Commit 格式

```
git commit -m "checkpoint: 修改前備份 [日期] [簡述當前狀態]"
```

**規則：checkpoint commit 不代表功能完成，只代表「回退點」。** HANDOFF 中需標明是 checkpoint 而非驗收通過的 commit。

### 25.3 大改動前的額外保護

| 改動規模 | 額外步驟 |
|---------|---------|
| 只改 KB 資料（Firestore）| git status 確認乾淨即可 |
| 修改 HTML 邏輯（JS/評分）| checkpoint commit + 記錄 hash |
| 全站重構 / 多章同時改 | 禁止。先拆成單章，逐章 commit |
| 修改 Firestore rules | 先讀 `Firebase設定/firestore.rules` 確認現況 |

### 25.4 回退時的規則

- 回退 **必須**說明哪個 commit 被回退、原因是什麼
- 被回退的功能不可自行加回，需等使用者重新指示
- 回退後立即更新 HANDOFF，說明目前有效的最後 commit

---

## 二十六、單一資料來源規則

### 26.1 問題根源

同一個事實分散在四個地方各寫各的，導致四處不一致：

| 位置 | 實例 |
|-----|-----|
| KB（kbStore fact）| 回答「溫度警戒值 23.5°C」 |
| 部門頁「作法」區 | 還寫著舊版「警戒值 24°C」 |
| FAQ 快速問題 | 按鈕查詢結果與部門頁不同 |
| 稽查重點 | 數字又不同 |

### 26.2 唯一真實來源定義

```
唯一真實來源 = SOP/WI/FR 原文（資料夾內文件）
                    ↓
              fact 資料（kbStore）
                    ↓
    ┌───────────────┬───────────────┐
    ↓               ↓               ↓
 部門頁作法       稽查重點         FAQ/快速問題
 規範到現場      文件地圖         手動搜尋
```

**規則：所有前台顯示的事實，都必須來自同一個 fact 資料，不可在各區塊各自維護。**

### 26.3 快速問題按鈕的一致性要求

- 快速問題按鈕的 `data-q` 值，必須是 KB 中某個 fact 能命中的查詢字串
- **不可**讓按鈕的查詢結果與部門頁顯示的內容不一致
- 每次新增快速問題按鈕，必須先確認 KB 有對應的 fact，且三欄內容正確

### 26.3.1 快速查詢全量驗收規則

快速查詢不是入口文字而已，必須視為固定測試題。每次修改智慧查詢、keywords、quickQuestions、搜尋評分或 fact/clarify 條目時，必須全量檢查所有快速查詢按鈕。

硬性規則：

- 每一個快速查詢問題都必須第一名命中 `sourceType:"fact"`。
- 快速查詢不得第一名命中 `clarify`，除非該題本身明確是流程概述題，且使用者已確認不需要精準短答。
- 快速查詢題目若問「怎麼處理、多久一次、幾月、幾筆、由誰、填什麼表、如何評鑑」等具體答案，必須有對應 fact，不得依賴 clarify 的長段流程回答。
- 快速查詢的 `data-q`、fact keywords、`verify_facts_ghpages.mjs` 測試題必須同步。
- 發現一題錯誤時，不可只修該題；必須掃描所有快速查詢是否有同型違規。

本規則來自 2026-06-01 修正案例：快速查詢曾出現「溫度超標怎麼處理」混入不符合品處理、「運輸溫度如何管控」命中出貨揀貨流程、「委外作業多久評估一次」回覆整段委外流程。正確修法是新增或補強對應 fact，並移除 clarify 內會搶答的具體 keyword。

### 26.4 同步觸發點

任何一個地方改了事實，以下全部必須同步：

```
□ KB fact（kbStore）
□ Firestore gdpKnowledgeBase
□ HTML kbStore fallback
□ 部門頁「作法」區
□ 規範到現場對照區
□ 稽查重點區
□ 部門文件閱讀地圖
□ FAQ / 快速問題按鈕
```

---

## 二十七、前台文字校對規則

### 27.1 宣告完成前的掃描清單

每次宣告某頁面/功能完成前，必須執行以下掃描：

**第一類：系統/開發殘留**

```
□ 「更新者：system」
□ 「debug」「console.log」殘留在畫面
□ 「TODO」「FIXME」「待補」出現在前台
□ 「undefined」「null」出現在畫面
□ HTML 標籤出現在可見文字中（< > 沒有被 escape）
```

**第二類：AI 備忘殘留**

```
□ 「此筆來自…」
□ 「不可寫成…」
□ 「目前資料索引…」
□ 「（暫缺）」「（待補充）」
□ 「資料來源說明」類型文字
```

**第三類：內容品質**

```
□ 重複出現的句子（同段落內出現兩次以上相同語句）
□ 錯字（中文形近字：偉/偵、揃/揀、雔/隔）
□ 數字錯誤（溫度、頻率、期限）
□ 公司無冷藏倉：搜尋「冷藏」「冷鏈」「冷藏倉」「冷藏庫」「冷藏設備」是否仍出現
□ 表單號碼格式是否一致（FR54-01 而非 FR-54-01）
```

公司無冷藏倉為硬性事實邊界。除非使用者提供新的 SOP/WI/FR 並明確確認公司新增相關設施，否則 HTML、KB、quickQuestions、query intent、搜尋加權、稽核提示、Firestore 同步資料中不得出現「冷藏」「冷鏈」「冷藏倉」「冷藏庫」「冷藏設備」等字樣。若只是要描述公司現況，使用「單一常溫倉庫」或「室溫藥品儲存溫度維持 15～25°C」。

### 27.2 Firestore 資料的額外校對

Firestore 資料建立後**必須讀回驗證**，重點檢查：

| 易錯字元 | 正確 | 常見錯誤 |
|---------|------|---------|
| 偵測器 | 偵（U+5075）| 偉（U+5049）|
| 揀貨 | 揀（U+63C0）| 揃（U+63C3，日文）|
| 隔離 | 隔（U+96D4）| 雔（錯字）|
| 有害物 | 有害 | 山害 |
| 螞蟻 | 螞蟻 | 蟞蟻 |
| 蟑螂 | 蟑螂 | 螠船 |

---

## 二十八、內部文件保護規則

### 28.0 公司文件一律不得有超連結

> **硬性規則：凡屬公司內部文件（SOP、WI、FR、DM），在前台一律以純文字顯示，不得加超連結。**

| 禁止 | 正確 |
|-----|------|
| `<a href="...">DP55-01</a>` 可點擊連結 | `DP55-01` 純文字 |
| `doc-pill` 點擊後開啟文件內容 | 純文字文件名稱 + 申請說明 |
| 文件名稱帶超連結指向 Google Drive | 純文字，員工自行向文管部申請 |
| 文件按鈕觸發 Modal 顯示路徑或內容 | Modal 改為申請通知 |

**例外：** 外部法規連結（PIC/S 官網、食藥署官網）屬公開資訊，可保留超連結。

### 28.1 保護範圍（老闆 2026-05-27 決定）

公司擔心競業取得平台 URL 後，直接複製新勝 SOP 架構與執行細節。

**必須隱藏的資訊：**

| 資訊類型 | 範例 | 處置 |
|---------|------|------|
| Google Drive 路徑 | `G:\我的雲端硬碟\2026codex\…` | 完全不顯示 |
| SOP 詳細閱讀重點 | 「第三節 3.1.2 規定採購人員需…」| 完全不顯示 |
| 內部執行細節 | 具體操作步驟、人員名單 | 只保留抽象層級的事實 |
| 文件的可點擊路徑 | `doc-pill` 點擊後開 Modal 顯示路徑 | 改為申請通知 |

**可以保留的資訊：**

| 資訊類型 | 範例 | 說明 |
|---------|------|------|
| 文件名稱與編號 | DP55-01、FR54-01 | 純文字，員工知道申請哪份即可 |
| 抽象層事實 | 「每兩個月全面盤點一次」| 稽查應答需要 |
| 外部法規連結 | PIC/S、食藥署官網 | 公開資訊，可保留超連結 |

### 28.2 `openDoc()` Modal 標準文字

```
「此文件為公司內部管控文件，
  請向文管部填單申請調閱。」
```

文件名稱仍可見（純文字），員工知道要申請哪份文件，但不顯示路徑與詳細內容。

### 28.3 KB 智慧查詢的保護邊界

- 智慧查詢**繼續顯示**具體事實（頻率、表單號、人員職稱）
- 智慧查詢**不顯示** SOP 的完整程序步驟或路徑
- 資料夾文件作為 KB 建立的**來源依據**，不直接暴露於 UI

---

## 二十九、GitHub Pages 實機驗收規則

### 29.1 驗收層級定義

| 層級 | 描述 | 是否算完成 |
|-----|------|---------|
| 本機預檢 | `npx serve -p 3333` 測試 | **不算完成**，只算預檢 |
| Node.js 腳本驗收 | `node verify_facts_ghpages.mjs` 模擬搜尋 | 半算（搜尋邏輯），但非實機 |
| **GitHub Pages 實機驗收** | 永久網址開啟並實際操作 | **才算完成** |

### 29.2 實機驗收標準步驟

```
1. git commit + push
2. 等待 1-3 分鐘（GitHub Pages 部署時間）
3. 開啟 https://n1116839.github.io/xinshing-gdp-training/
4. 確認頁面 HTTP 200（非 404）
5. 對應本次修改的區域，逐項實際操作（不只是看畫面）
6. 在 HANDOFF 寫「已用 GitHub Pages URL 驗收：○○正常，○○正常」
7. 若發現錯誤 → 修正 → 再次 commit + push → 回到步驟 2
```

### 29.3 驗收必須實際操作的清單

根據本次修改的類型，對應驗收：

| 修改類型 | 驗收操作 |
|---------|---------|
| KB 新增/修改 fact | 輸入對應問題，確認命中正確 fact，三欄內容正確 |
| 快速問題按鈕 | 點擊每個按鈕，確認查詢結果正確 |
| 部門頁修改 | 點入該部門，確認顯示內容正確 |
| Modal 改動 | 觸發 Modal，確認顯示申請說明而非 SOP 內容 |
| 導覽修改 | 點選每個導覽項目，確認頁面切換正確 |
| 搜尋邏輯修改 | 至少輸入 5 個不同問題，確認結果正確 |

### 29.4 沒有驗收記錄 = 未完成

HANDOFF 若無驗收記錄，下一個 AI 必須視此修改為**未驗收狀態**，不得假設已完成。

---

## 三十、RWD 不互相破壞規則

### 30.1 核心原則

修改手機版時，**必須確認桌機版仍正常**；修改桌機版時，**必須確認手機版仍正常**。

### 30.2 需同時驗收的場景

| 修改項目 | 桌機驗收 | 手機驗收 |
|---------|---------|---------|
| 導覽修改 | 左側分組按鈕正常 | 下拉面板開合正常 |
| 版面佈局調整 | 多欄 grid 正常顯示 | 單欄堆疊正常顯示 |
| 字體大小修改 | 閱讀舒適 | 不超出容器 |
| 部門篩選修改 | 桌機篩選按鈕正常 | 手機版按鈕可點擊 |
| Modal 修改 | 正常置中顯示 | 手機上不超出螢幕 |

### 30.3 手機版驗收方式

- **Chrome DevTools**：F12 → Toggle device toolbar → 選 375px（iPhone SE）
- **或**：實機手機開啟 GitHub Pages 永久網址

### 30.4 手機與桌機各自的導覽規則

| 端 | 導覽元件 | 修改原則 |
|---|---------|---------|
| 桌機 | 左側分組按鈕 | 手機修正時不影響此元件 |
| 手機 | 自製大按鈕下拉面板 | 桌機修正時不影響此元件 |

---

## 三十一、互動元件驗收規則

### 31.1 「看起來正確」≠「互動正確」

**禁止只看畫面截圖或靜態 HTML 就宣告互動元件完成。** 每個互動元件都必須實際點擊操作。

### 31.2 各類元件的驗收動作

| 元件類型 | 必做驗收動作 |
|---------|------------|
| Accordion | 點擊展開 → 確認內容出現；再點擊收合 → 確認內容消失 |
| Modal | 點擊觸發按鈕 → 確認 Modal 開啟；點擊關閉或背景 → 確認 Modal 關閉 |
| Tab panel | 點擊每一個 Tab → 確認對應內容切換；確認預設 Tab 正確 |
| 下拉選單（自製）| 點擊開啟 → 確認選項出現；點選一項 → 確認頁面切換且選項文字更新 |
| 快速問題按鈕 | 點擊每個按鈕 → 確認查詢發出、結果正確、無「尚未查詢」殘留 |
| 搜尋輸入框 | 輸入問題 → 確認 loading → 確認結果或「找不到」訊息（不是空白）|
| Checkbox 核對清單 | 點擊勾選 → 確認視覺勾選狀態；取消勾選 → 確認還原 |
| Scroll fade-in | 向下捲動 → 確認元素在進入畫面時有動畫 |

### 31.3 驗收失敗的常見原因

| 症狀 | 常見根因 |
|-----|---------|
| 點擊無反應 | JS 事件未綁定 / 選取器錯誤 |
| Modal 開啟但關不掉 | 關閉按鈕事件未綁定 |
| Tab 切換顯示錯誤內容 | data 屬性或 id 對應錯誤 |
| 下拉選單開啟後點選沒反應 | 選項 button 事件未設定正確目標 |
| 搜尋後仍顯示「尚未查詢」| 初始訊息未在查詢前清除（`box.innerHTML = ""`）|
| Accordion 不收合 | toggle class 邏輯錯誤 |

---

## 三十二、漸進式修改規則

### 32.1 核心原則

> **先做一章完整樣板，驗收通過後，再以同一模板擴展到其他章。**
> 禁止一次對所有章節同時開工。

### 32.2 適用場景

| 場景 | 正確做法 |
|-----|---------|
| KB fact 全章建立 | 先建第二章（人事/教育訓練）樣板，通過後複製到其他章 |
| 稽查重點互動化 | 先做第一章的稽查重點，通過後複製格式 |
| 部門頁作法更新 | 先更新一個部門，確認格式正確後再展開 |
| 測試標準建立 | 先對一章跑完整 10 題 × 10 問法，建立標準再推廣 |

### 32.3 樣板建立的完整流程

```
1. 選定目標章節（建議第二章：人事/教育訓練）
2. 讀 SOP/WI/FR 原文，拆出所有可獨立問答的 fact
3. 每個 fact 設計 10 種自然問法
4. 建測試表（10 個問題 × 10 種問法）
5. 本機預檢（npx serve -p 3333）
6. git commit + push
7. 等 GitHub Pages 部署
8. 實機驗收（第二十九章規則）
9. 手機/桌機都驗收（第三十章規則）
10. 互動元件逐一點擊（第三十一章規則）
11. 通過 → 在 HANDOFF 標記「第二章樣板完成 ✅」
12. 將同一流程複製到第三章、第四章…
```

### 32.4 樣板複製時的注意事項

- 複製 fact 格式，**不複製 fact 內容**（每章 SOP 不同，內容必須重新讀原文）
- 複製測試表格式，**不複製測試題**（每章測試題不同）
- 複製驗收流程，**每章都要跑完整驗收**（不因為樣板通過就跳過）

---

## 三十三、年度行事曆設計規則

### 33.1 行事曆的定位

**年度行事曆是「收件人視角」，不是查詢工具。**

員工打開行事曆，看的是：「這個頻率我要做什麼、交給誰、填什麼表單」——不是「資料庫裡有哪些文件」。

### 33.2 行事曆資訊架構

```
年度行事曆（獨立導覽大項）
  ├── 依頻率分組：發生時啟動 / 每日 / 每月 / 每兩個月 / 每季 / 每半年 / 每年 / 每三年
  ├── 依部門篩選
  └── 關鍵設備行事曆（獨立區塊）
```

**行事曆必須與部門分類獨立**，不可混在部門頁內。

### 33.3 每筆收件項目的欄位

每筆資料應包含：
```
時間點 / 部門 / 資料名稱 / 啟動說明 / 表單/紀錄 / 負責人 / 來源 SOP
```

「已完成/未完成」checkbox 放在每筆項目旁，不做獨立「本月勾選」頁。

### 33.4 禁止重複的三個區域

| 容易重複的區域 | 正確處置 |
|-------------|---------|
| `schedule` 區塊（基本時程）vs `deptScheduleSection`（互動版）| 只保留互動版，移除基本版 |
| 「依頻率分組」明細 vs 每筆卡片的來源 | 「依頻率分組」改為摘要（數量+部門），不重複列明細 |
| 年度行事曆的「本頁相關 SOP」 vs 每筆卡片的來源欄 | 移除年度行事曆的 SOP 清單區塊，來源留在卡片 |

### 33.5 頻率術語統一規則

| 正確寫法 | 禁止寫法 |
|---------|---------|
| 每季（每三個月）| 每三個月（與每季重複） |
| 每兩個月 | 每兩月（不一致） |
| 每年 12 月 | 每年年底（不精確）|

**同義頻率只列一次，不可重複出現。**

### 33.6 行事曆完整性：必須逐份 SOP 讀取

**索引不能取代逐份 SOP 閱讀。**

**踩坑根因（2026-05-25）：** FR10-01 有時程 NEW 是方便查找的索引，但不能取代逐份 SOP 閱讀。DP14-01 管理階層檢討（每年 12 月）等具體時間點只寫在 SOP 正文，索引未必完整。DP56-01 廢棄物管理（11 月通知廠商、12 月底銷毀）也未出現在索引，靠索引一定漏。

**正確做法：**
1. 每次建立/核對行事曆，必須逐份讀取第一章到第八章所有 SOP 文件
2. 不可只掃 FR10-01 索引
3. 每章核對完必須記錄：「第X章 N 份文件，發現 M 個時程節點」
4. 若目錄與 SOP 原文有出入，以 SOP 原文為準，差異列給使用者確認

### 33.7 設備行事曆獨立規則

關鍵設備（分離式冷氣、UPS、溫度計、溫濕度記錄器、柴油發電機）獨立為「關鍵設備行事曆」section，不與資料收件行事曆混放。

格式：左側頻率 / 右側設備名稱、作業內容、負責人、表單、來源 SOP。

---

## 三十四、Firebase Q&A 功能規則

### 34.1 連線狀態顯示邏輯

**正確順序：**
```
頁面載入 → 顯示「連線中…」
  ↓
Firestore 連線成功 → 顯示「已連接 Firebase」
  ↓
顯示現有問題列表
```

**禁止：** 頁面載入後直接顯示「本機暫存 N 筆」或跳過連線提示。使用者會誤以為沒有連接 Firebase。

### 34.2 Q&A 區塊設計規則

| 規則 | 說明 |
|-----|------|
| 問題永久保留 | 不定期清除已回答的問題 |
| 移除提問人欄位 | 不收集個人資訊 |
| 回覆開放所有人 | 不限管理者才能回覆 |
| 雙欄佈局 | 左側 sticky 提問框，右側問題列表，加問題計數 badge |

### 34.3 Firestore Q&A 集合

使用集合：`gdpQ&A`（依 Firestore rules 授權）

問題狀態存在 Firestore，不可只存 localStorage（localStorage 不跨裝置）。

---

## 三十五、SOP 覆蓋完整性規則

### 35.1 核心原則

**資料夾有幾份文件，KB 就必須有幾筆。** 這個規則適用於所有章節，不只是現有的章節。

### 35.2 各章文件數（截至 2026-05-28）

| 章節 | 文件數 | 章節主題 |
|-----|------|---------|
| 第一章 | 11 | 品質管理系統 |
| 第二章 | 8 | 人員組織 |
| 第三章 | 14 | 場所設備 |
| 第四章 | 2 | 文件管制 |
| 第五章 | 7 | 作業（進出貨、儲存、揀貨） |
| 第六章 | 4 | 申訴、退回、偽禁藥、回收 |
| 第七章 | 1 | 委外 |
| 第八章 | 1 | 內部稽核 |
| **合計** | **48** | |

### 35.3 宣告某章 KB「完成」的條件

```
□ 已逐份讀取該章所有 SOP/WI/FR 原文
□ 每份文件至少有一筆對應的 fact 或 clarify 條目
□ 每筆 fact 的三欄內容已對應原文核對
□ 每章 10 個問題 × 10 種問法測試全部通過
□ GitHub Pages 實機驗收完成
```

### 35.4 索引核對規則

- **FR10-01 有時程 NEW** 等索引只作為「快速查找入口」，不作為完整性依據
- 任何作業型 SOP（不只管理型）都要逐份讀正文，不可只看索引標題
- 每次完成一章後，記錄「第X章：N 份文件核對完畢，fact N 筆，clarify N 筆」

---

## 附錄 A：開工必讀清單

每次對話開始，按順序讀：
1. `HANDOFF.md`
2. `G:\我的雲端硬碟\我的第二大腦\踩坑紀錄.md`
3. `G:\我的雲端硬碟\我的第二大腦\專案\2026codex AI測試.md`

讀完後詢問使用者從哪個待辦開始，**不可自行決定**。

---

## 附錄 B：KB 絕對禁止清單

| 禁止 | 原因 |
|-----|------|
| KB 欄位放 AI 指令語（「不可寫成…」「目前資料索引…」）| 學員看到的是教材，不是備忘 |
| KB 欄位放否定推論（「不是溫度監控系統」）| 文件沒有這樣記載，不可補充 |
| KB 欄位放佔位文字（「請依文件編號回到正式文件確認」）| 員工需要實質答案，不是索引 |
| HTML 本身作為 KB 來源 | 搜尋結果會顯示 HTML 標籤與 JS 程式碼 |
| 出現「冷藏倉」「冷鏈」| 公司無冷藏倉，這是錯誤事實 |
| 同時處理多個待辦（KB＋互動化＋字體＋面板）| 任一未通過驗收，全部被回退 |

---

## 三十六、部門頁面內容精準性規則

### 36.1 部門頁的設計場景（新員工視角）

**情境：** 倉庫組長要培訓新人，說「你去看教育平台的倉庫章節」。

新人打開倉庫頁後，應該能依序得到這四個答案：

| 順序 | 他應該得到的答案 | 對應目標 |
|-----|----------------|---------|
| 1 | GDP 是什麼、跟倉庫有什麼關係 | 目標 1：GDP 是什麼 |
| 2 | 稽查員來倉庫最常抓哪些缺失 | 目標 2：常見缺失 |
| 3 | 稽查員問「溫度幾度？盤點多久？」時怎麼回答 | 目標 3：稽查問答 |
| 4 | 每兩個月做盤點、到貨當天填 FR54-01、溫度每5分鐘記一次…哪個時間點做哪件事 | 目標 4：什麼時候要做文件 |

**以及：** 倉庫必讀 / 參考 / 共用的文件清單（現有功能，但必須精準）

---

### 36.2 文件分類三層：必讀 / 參考 / 共用

每個部門頁的文件清單，依重要性分三層：

| 分類 | 定義 | 範例（倉庫）|
|-----|------|-----------|
| **必讀** | 該部門員工每天工作直接用到的 SOP/WI/FR，稽查員一定會問 | DP33-01 溫濕度管理、DP55-01 庫存管理、FR54-01 到貨驗收紀錄表 |
| **參考** | 不直接操作，但需要了解流程的 SOP | DP12-01 品質政策、DP82-01 內部稽核 |
| **共用** | 跨部門使用、需要與其他部門協作的文件 | FR54-01（採購也用）、DP25-01（人事通知） |

---

### 36.3 文件分配的精準性原則（核心規則）

> **硬性規則：部門 A 的文件清單，只能放部門 A 相關的文件。**

**踩坑場景：** 倉庫頁的「本頁相關 SOP」放進了 DP62-01（申訴退回，屬品保/業務）或 DP72-01（委外，屬品保）。

| 禁止出現的情況 | 正確做法 |
|-------------|---------|
| 倉庫頁出現業務的 SOP（DP62-01）| 移到品保或業務頁 |
| 採購頁出現倉庫的 WI | 移到倉庫頁 |
| 人事頁出現稽核相關 SOP（不在人事職責內）| 移到品保頁 |
| 所有部門都放相同的共用清單 | 共用文件只在「真的會用到的部門」才顯示 |

**校對方法：** 建立或修改某部門的文件清單時，逐份確認「**這份文件的 SOP 裡，有哪個章節明確寫到這個部門的職責？**」，沒有就不放。

---

### 36.4 職位層級的內容差異

> **重要：** 以下分層是**展示規則**，不是系統功能（帳號權限功能在後期才做）。  
> 目前設計階段，應先按最高層級設計內容，帳號功能上線後再套用篩選邏輯。

**倉庫範例：**

| 職位層級 | 看到的內容 | 原因 |
|---------|-----------|------|
| **倉管一般員工** | 溫濕度管理、庫存盤點、到貨驗收、揀貨出貨 | 日常操作核心 |
| **倉庫組長** | 一般員工全部 + 文件修正申請流程 + 跨部門協作說明 | 需要管理職責 |
| **（跨部門協作範例）** | 採購叫貨後，倉庫需確認到貨驗收紀錄（FR54-01）；預計到貨日由採購通知倉庫 | 協作觸發點 |

**跨部門協作的標準呈現方式：**

```
【倉庫 ↔ 採購 協作】
觸發點：採購在正航建立採購單並確認到貨日
倉庫做什麼：依預計到貨日安排驗收人力，到貨當天填 FR54-01
必要文件：FR54-01 到貨驗收紀錄表
備註：驗收完成後，倉庫通知採購確認入庫
```

---

### 36.5 「什麼時候要做文件」在部門頁的呈現方式（目標 4）

每個部門頁應有一個「**我的文件時程**」區塊，列出該部門需要在什麼條件下填寫哪些表單。

**格式：**

| 觸發條件 | 要做什麼 | 填哪份表單 | 來源 SOP |
|---------|---------|-----------|---------|
| 有貨到 | 填到貨驗收紀錄 | FR54-01 | DP54-01 |
| 每兩個月 | 全面庫存盤點 | FR55-03 | DP55-01 |
| 溫度超過 23.5°C | 立即記錄並通報 | FR33-01 | DP33-01 |
| 發現不符合品 | 隔離並標示，填報告 | FR55-04 | DP55-01 |
| 有訪客 / 外部人員進入 | 登記門禁 | FR25-01 | WI25-01 |

**這個區塊取代「什麼時候做？」的純文字說明，改為可掃視的表格或互動清單。**

---

### 36.6 文件清單的建立順序（校對流程）

```
Step 1：列出此部門對應的所有 SOP/WI/FR（從資料夾逐份確認）
         ↓
Step 2：依「必讀 / 參考 / 共用」分類
         ↓
Step 3：確認每份文件確實有該部門的職責描述（不是因為「感覺相關」就放進去）
         ↓
Step 4：跨部門協作文件 → 在兩個部門頁都標示，並寫清楚各自的角色
         ↓
Step 5：與 KB 的 fact sop_ref 欄位比對 — 部門頁列的文件若有對應 fact，兩邊一致
         ↓
Step 6：完成後，在另一個部門頁確認「這份文件沒有出現在不該出現的地方」
```

---

### 36.7 驗收標準

宣告某部門頁文件清單完成前，必須驗收：

```
□ 必讀文件：每份都能在 SOP 原文找到「此部門」的職責說明
□ 沒有其他部門的 SOP 誤入
□ 共用文件：明確標示「此文件在哪幾個部門都會用到」
□ 「文件時程」表格有對應的 SOP 來源（非推論）
□ 跨部門協作說明：觸發點、各部門角色、表單都寫清楚
□ 職位層級內容（如有）：組長看到的比一般員工多的部分已確認來源
```

---

## 三十七、三階段開發藍圖

### 37.1 優先順序（此順序不可調換）

| 階段 | 目標 | 前提條件 | 狀態 |
|-----|------|---------|------|
| **Phase 1** | KB 全量完成 + 收件時程（角色開放）| 無（第一優先）| ✅ 完成（2026-06-03 第六十五次，§37.2 條件全達成；ch7/ch8 單 SOP 章覆蓋說明見 §42.7）|
| **Phase 2** | 所有文字區塊視覺化 / 互動化 | Phase 1 完成 | 進行中（45–64 次已完成翻卡/Stepper/證據核對台等互動）|
| **Phase 3** | 帳號申請 + 管理員審核 + 角色晉升 | Phase 1 完成 | 設計完成，待實作（登入端使用者確認暫停）|

> **規則：Phase 1 完成前，不開始 Phase 2 或 Phase 3 的實作工作。**
> Phase 2 和 Phase 3 可以「設計」，但不可以「寫 code」。

### 37.2 Phase 1 完成條件

```
□ 第一至八章所有 KB fact 條目已建立
□ 每章至少 10 個問題 × 10 種問法，測試全部通過
□ GitHub Pages 實機驗收通過
□ 收件時程（年度行事曆）資料完整（依角色控制可見性）
□ HANDOFF 標記「Phase 1 完成 ✅」
```

### 37.3 Phase 2 完成條件

```
□ 每個部門頁都有至少一個真正的互動元件
□ 稽查重點區塊：學習型格式（問題/應答/證據）
□ 文件時程：可篩選的互動表格
□ 所有桌機 + 手機驗收通過
```

### 37.4 Phase 3 完成條件

```
□ 員工可自行申請帳號（填表 + 等待審核）
□ 管理員後台可審核、停用、改角色
□ 一次性啟用碼機制可用
□ 組長可代申請員工晉升
□ 角色控制：一般/主管/管理員三層內容正確隔離
□ 收件時程對「主管以上」才顯示
```

---

## 三十八、帳號申請與審核平台設計（Phase 3）

### 38.1 角色三層設計（已確認）

| 層級 | 識別名稱 | 可見內容 | 後台功能 |
|-----|---------|---------|---------|
| **Level 1** | 一般員工 | 自己部門的 GDP 內容（四大目標） | 無 |
| **Level 2** | 組長 / 主管 | 跨部門協作說明 + 收件時程 | 可代提交「成員晉升申請」 |
| **Level 3** | 管理員 | 全站所有部門內容 | 審核帳號、停用、改角色、產生啟用碼 |

> **附加角色：超級管理員（Super Admin）**
> 僅限 1-2 個指定 email，hardcode 在設定中。負責新增第一個管理員帳號、系統設定。不透過申請流程，直接由系統設定。

### 38.2 員工申請帳號流程

```
Step 1：員工開啟平台 → 點「申請帳號」
          ↓
Step 2：填入申請資料
          ├── 員工編號（文字輸入）
          ├── 姓名（文字輸入）
          ├── 部門（下拉選單，由管理員維護清單）
          └── 職稱（下拉選單，由管理員維護清單）
          ↓
Step 3：系統建立「待審核」申請
         → 通知管理員（email + 後台角標）
          ↓
Step 4：管理員登入後台
         → 查看申請清單 → 選擇「批准」並指定層級（Level 1/2）
          ↓
Step 5：系統產生一次性啟用碼（48 小時內有效）
         → 管理員透過 LINE 或當面告知員工
          ↓
Step 6：員工回到平台 → 輸入啟用碼 → 設定密碼 → 帳號啟用
          ↓
Step 7：員工正常登入，依角色顯示對應內容
```

### 38.3 啟用碼安全設計

| 規則 | 說明 |
|-----|------|
| 一次性 | 啟用碼使用一次即失效，無法重用 |
| 有效期 | 48 小時後自動過期 |
| 綁定申請 | 啟用碼只對應這一份申請，無法拿來啟用其他帳號 |
| 離職防護 | 即使離職員工仿冒資料通過申請，沒有啟用碼就進不來 |
| 停用即時生效 | 管理員點「停用」→ 帳號立即無法登入 |

### 38.4 晉升流程（組長代申請）

```
Step 1：組長登入後台 → 進入「成員管理」
          ↓
Step 2：點選要晉升的員工 → 填寫「晉升原因」→ 選目標角色
          ↓
Step 3：系統通知管理員（email + 後台通知）
          ↓
Step 4：管理員審核 → 批准 → 員工角色自動更新
          （不需要重新輸入啟用碼）
          ↓
Step 5：員工下次登入即看到新層級的內容
```

### 38.5 帳號停用流程（員工離職）

```
管理員 → 後台「帳號管理」→ 找到離職員工帳號 → 點「停用」
→ 帳號立即無法登入
→ 資料保留（查稽核紀錄用）
→ 不刪除（避免日後稽查需要查歷史記錄）
```

### 38.6 Firebase 技術對應

| 功能 | Firebase 元件 | Firestore Collection |
|-----|------------|-------------------|
| 帳號登入 | Firebase Authentication | — |
| 使用者資料 | Firestore | `gdpUsers` |
| 申請待審核 | Firestore | `gdpUserApplications` |
| 一次性啟用碼 | Firestore | `gdpActivationCodes` |
| 晉升申請 | Firestore | `gdpPromotionRequests` |
| 職稱/部門清單 | Firestore | `gdpDeptConfig` |
| 管理員通知 | Firebase Extensions / Cloud Functions | — |

> **注意：** Firebase Extensions（Send email）可用於通知管理員。若要 LINE 通知，需額外整合 LINE Notify API，屬額外開發工作。Phase 3 初版先用 email 通知，LINE 通知列為後期選項。

### 38.7 收件時程（年度行事曆）的角色控制

| 角色 | 收件時程可見性 |
|-----|-------------|
| 一般員工 | **不顯示**（只看自己部門的四大目標內容）|
| 組長/主管 | **顯示**（看到自己部門相關的收件項目）|
| 管理員 | **顯示全部**（所有部門所有收件時程）|

**收件時程的定義：** 員工需要「提交、填寫、完成、回報」某份文件的時間點。
- 例：倉管人員每兩個月需提交盤點表（FR55-03）→ 只有倉管組長以上可見收件時程提醒
- 例：品保每年 12 月需提交內部稽核報告 → 只有品保組長以上可見

### 38.8 職稱/部門清單的維護規則

> 使用者確認（2026-05-28）：職稱下拉選單由管理員維護，因為職位隨時會因人力需求增減。

**規則：**
- 部門清單和職稱清單存放在 Firestore `gdpDeptConfig`，不 hardcode 在 HTML
- 管理員後台有「清單管理」功能可新增/移除部門與職稱
- 員工申請表的下拉選單從 `gdpDeptConfig` 即時讀取
- **停用一個職稱 ≠ 刪除已有該職稱員工的帳號**（只影響新申請的下拉選項）

### 38.9 Phase 3 實作前的必要準備

在開始寫 Phase 3 的程式之前，必須先確認：

```
□ 管理員 email 清單（誰有管理員權限？具體幾個 email？）
□ 超級管理員 email（hardcode，1-2 個）
□ 初始部門清單（從 AGENTS.md 的部門列表開始）
□ 初始職稱清單（參考 SOP 中的人員職稱）
□ email 通知服務（Firebase Extensions 或其他）是否已啟用
□ Firestore rules 更新（新增 gdpUsers、gdpUserApplications 等集合的規則）
```

---

## 三十九、知識安全邊界設計（防止平台被複製成 GDP 範本）

### 39.1 核心安全原則

> **這份教育平台的知識，不可以讓外部人員一拿到就能複製出一份一模一樣的 GDP 文件系統。**

醫藥 GDP 文件系統是公司的核心合規資產，許多大公司視為機密。平台設計必須在「員工能學習」和「外部無法複製」之間找到明確邊界。

---

### 39.2 知識三層邊界

| 層級 | 名稱 | 定義 | 能放進平台？ |
|-----|------|------|-----------|
| **Layer 1** | 公開法規知識 | PIC/S GDP 原則、台灣食藥署規定 → 任何人查法規都查得到 | ✅ 可放，且無登入限制 |
| **Layer 2** | 合規事實陳述 | 員工回答稽查員的短答（頻率、溫度、表單號）→ 是「有做」的證明，不是「怎麼做」的說明書 | ✅ 登入後可見 |
| **Layer 3** | 作業細節程序 | SOP 的完整流程、表單欄位設計、決策樹、例外處理步驟 | ❌ 不進平台，留在 SOP 文件 |

---

### 39.3 Layer 2 vs Layer 3 判斷標準

**判斷問題：「這段內容，是幫員工回答稽查員，還是在教別人怎麼做系統？」**

| 內容類型 | 範例 | 層級 | 平台？ |
|---------|------|------|------|
| 頻率陳述 | 「每兩個月全面盤點一次」 | Layer 2 | ✅ |
| 表單號碼 | 「填 FR55-03」 | Layer 2 | ✅ |
| 溫度數值 | 「15-25°C，警戒值 23.5°C」 | Layer 2 | ✅ |
| 人員職稱 | 「由倉管人員執行」 | Layer 2 | ✅ |
| 表單欄位說明 | 「FR55-03 第一欄填品名，第二欄填批號…」 | Layer 3 | ❌ |
| 完整決策流程 | 「若差異 >3% 則啟動複查，若複查仍有差異則…」 | Layer 3 | ❌ |
| 例外處理步驟 | 「溫度超標時，先通報品保，品保確認後發不符合報告…」 | Layer 3 | ❌ |
| SOP 的整個程序章節 | 「3.1 收貨程序：3.1.1 採購人員須在24小時內…」 | Layer 3 | ❌ |

---

### 39.4 文件編號的安全處理

**文件編號（DP55-01、FR55-03）可以出現在平台上**，但只作為「這份文件你要申請來讀」的指引，不展示文件內容。

| 處理方式 | 說明 |
|---------|------|
| 出現文件編號 | ✅ 員工知道要申請哪份文件 |
| 展示文件清單 | ✅ 知道有哪些文件，不知道內容 |
| 展示文件內容 | ❌ 任何 SOP/WI/FR 的正文都不進平台 |
| 可點擊連結開啟文件 | ❌ 已在 Chapter 28 禁止 |

---

### 39.5 整體系統架構的保護

**禁止在平台上展示可推導出「新勝完整 GDP 文件架構」的資訊：**

| 禁止 | 原因 |
|-----|------|
| 所有 SOP/WI/FR 的完整編號清單（可瀏覽全貌）| 讓人知道公司有哪些文件、數量、結構 |
| 各文件之間的關聯圖（上下層關係）| 等同公開文件架構 |
| SOP 各章節標題清單 | 可推導出公司的管理系統設計邏輯 |

**允許的呈現方式：**
- 依部門呈現「此部門相關的文件名稱」（員工需要申請哪幾份）
- 只顯示與當前頁面主題相關的 1-3 份文件，不顯示全站文件索引
- 文件清單在登入後才顯示（Phase 3 上線後）

---

### 39.6 登入前與登入後的內容差異

| 內容類型 | 未登入 | 登入（一般員工）| 登入（主管以上）|
|---------|--------|--------------|-------------|
| GDP 法規介紹（Layer 1）| ✅ 可見 | ✅ 可見 | ✅ 可見 |
| 常見缺失說明（一般性）| ✅ 可見 | ✅ 可見 | ✅ 可見 |
| 新勝做法（Layer 2，稽查應答）| ❌ 不顯示 | ✅ 可見（自己部門）| ✅ 可見（全部）|
| 文件清單（表單編號）| ❌ 不顯示 | ✅ 可見（自己部門）| ✅ 可見（全部）|
| 收件時程 | ❌ 不顯示 | ❌ 不顯示 | ✅ 可見 |

> **Phase 3 前的暫行方案：** 沒有登入系統前，所有人可看到所有內容。Phase 3 上線後再套用上表的分層。

---

### 39.7 KB 重建時的安全審查

每次建立或更新 KB 條目時，在完成前必須做安全審查：

```
□ xinshing 欄是否只有「員工回答稽查員的短句」？（Layer 2）
□ 有沒有出現 SOP 的條文段落或完整程序步驟？（Layer 3 → 刪掉）
□ 有沒有展示表單的欄位設計或填寫邏輯？（Layer 3 → 刪掉）
□ 有沒有洩露決策樹或例外處理的完整流程？（Layer 3 → 刪掉）
□ 如果外部人員看到這筆資料，能複製出新勝的作業程序嗎？能 → 縮減
```

---

### 39.8 xinshing 欄文件代號禁止規則

**§39.5 延伸執行細則：xinshing 欄不得出現 standalone 文件類型代號。**

| 違規寫法 | 正確寫法 |
|---------|---------|
| `依DM品質手冊規定` | `依品質手冊規定` |
| `四階文件（DM、DP、WI、FR）` | `四階文件（品質手冊、程序書、作業指導書、表單）` |
| `記錄於FR42-06文件銷毀紀錄表中`（代號可保留） | ✅ 文件編號＋中文名稱可同時出現 |
| `記錄於FR42-06`（無中文名稱，單獨代號） | ❌ 禁止 |

**允許的文件編號使用方式：**
- 文件編號＋中文全名可同時出現（如 `FR42-06 文件銷毀紀錄表`）
- 不得在上下文無中文全名時單獨出現代號

**批次移除代號後的語意安全檢查：**
```
批次執行前                     批次執行後（殘渣）          正確修復
依DM10-01規定                  依，                       依規定
記錄於FR42-06文件銷毀紀錄表中   記錄於；                   記錄於文件銷毀紀錄表中
填於FR42-06中                  填於。                     填於文件銷毀紀錄表中
登錄於FR42-02並列入FR42-06    登錄於並列入                登錄後列入
（依DM10-01）                  （依）                     （依規定）
保存於DM10-01品質手冊          保存於                     保存於品質手冊
表單：FR42-02/FR42-06         表單：。                    表單：品質紀錄總覽表、文件銷毀紀錄表
```

**檢查規則：**
1. 代號批次移除後，逐筆檢查 `依` `記錄於` `填於` `保存` `表單：` 等動詞/介詞後的上下文，確認文法完整
2. `依` 若後方代號移除後無內容 → 改 `依規定`
3. `記錄於` `填於` `保存` 等動詞後需接明確表單中文名稱
4. `表單：` 後若只剩代號 → 補表單中文全名
5. 批次腳本無法處理所有邊界案例，需搭配人工逐筆確認

---

### 39.9 三種文件編號的差別對待

| 類型 | 舉例 | xinshing 能否出現 | 理由 |
|-----|------|-----------------|------|
| 文件類型代號（standalone） | `DM` `DP` `WI` `FR` | ❌ 禁止 | §39.5 禁止展示文件架構 |
| 文件編號（有上下文） | `DP42-01 文件管制程序書` | ✅ 允許 | §39.4 允許員工看到文件編號以利申請 |
| 表單編號（有中文名） | `FR42-06 文件銷毀紀錄表` | ✅ 允許 | 同上 |

---

## 四十、Keywords 設計規範（自然語言查詢精準度）

### 40.1 三種關鍵字型態

每個 fact 的 keywords 陣列必須涵蓋以下三種型態，缺一不可：

| 型態 | 舉例 | 用途 |
|-----|------|------|
| 短代碼型 | `FR72-02` `委外作業廠商評鑑表` | 員工知道表單號時快速命中 |
| 自然問法型 | `委外物流怎麼評鑑` `委外如何評估` | 一般員工用完整句子查詢 |
| 中斷補償型 | `物流要怎麼評鑑`（跨停用詞 `要`）`委外物流評鑑`（跨 `要怎麼`） | 長句中停用詞打斷匹配時兜底 |

### 40.2 關鍵字連續性規則

kbStore 的關鍵字比對邏輯：**keyword 必須是查詢字串的連續 substring**（`query.includes(keyword)`，keyword ≥ 4 字）。

**常見中斷模式：**
```
查詢：委外物流要怎麼評鑑合格分數為多少
      物流 要 怎麼 評鑑    合格 分數  多少
            ↑停用詞          ↑停用詞 ↑停用詞

中斷點：要、怎麼、的、為、才、算、還、有、在、可以
```

**因應策略：**
- 先列出真實使用者可能寫出的 10 種問法
- 找出每種問法中的停用詞中斷點
- 補中斷點兩側的片段組合（去掉停用詞）
- 補完整長句變體（保留停用詞）

**實際案例（第 25 次開工修正）：**

| 查不到的原因 | 補的關鍵字 |
|-------------|-----------|
| `委外物流要怎麼評鑑` 中 `要` 中斷 `委外物流` 與 `怎麼評鑑` | `物流要怎麼評鑑` `委外物流評鑑` `委外要怎麼評鑑` |
| `委外廠商要幾分才算合格` 中 `要` `才` `算` 中斷 | `委外合格分數` `幾分才算合格` `委外廠商要幾分` |

### 40.3 測試驗收每 fact 的最低問法數量

- 每 fact 至少設計 **10 種不同問法**，且必須全部通過驗收
- 驗收方式：對每個 fact 執行 5 組不同問法，確認所有問法都命中
- 若某自然問法有 `≥4 字連續 keyword` 仍無法命中 → 檢查該 keyword 是否被停用詞中斷

---

## 四十一、批次修改安全流程

### 41.1 批次修改後必須做的檢查

當對 kbStore 的 xinshing 欄/keywords 陣列進行批次（auto-fix 腳本）修改後：

1. **語意殘渣檢查：** 逐筆確認代號移除後 `依` `記錄於` `填於` `保存` `表單：` 等動詞/介詞後的文法完整
2. **verify 腳本同步：** verify_facts_ghpages.mjs 的 expectHint 必須同步更新（比對實際 xinshing 文字）
3. **全域 grep 殘留：** `"DM"|"DP"|"WI"|"FR"` 確認沒有任何 standalone 代號殘留
4. **驗收測試：** 跑 `node verify_facts_ghpages.mjs` 確認 100% 通過
5. **智慧查詢回歸測試：** 至少測試 5 組常用自然語言查詢，確認修改後無副作用

### 41.2 腳本與人工搭配原則

- 批次腳本適合：規則明確的替代（代號→中文名、`依，`→`依規定，`）
- 人工檢查適合：邊界案例（`登錄於並列入`→`登錄後列入`）、語意殘渣（`記錄於；`→後需補表單名）
- **規則：** 批次腳本處理後，必須人工逐筆抽查至少 30% 的修改處

---

## 四十二、資料庫維護機制（文件追蹤 + 新增/刪除管理）

### 42.1 核心規則：一份文件，至少一筆覆蓋

> **資料夾有幾份文件，KB 就要有對應的覆蓋。**
> 一份文件可以對應一筆 clarify（流程概述）+ 多筆 fact（各個具體事實）。
> 但每份文件至少必須有一筆條目，不可有文件但完全無 KB 覆蓋。

---

### 42.2 各章文件數與目前完成狀態

| 章節 | 文件數 | 章節主題 | 目前狀態 |
|-----|-------|---------|---------|
| 第一章 | 11 | 品質管理系統 | ✅ fact 架構完成（驗收通過）|
| 第二章 | 8 | 人員組織 | ✅ fact 架構完成（驗收通過）|
| 第三章 | 14 | 場所設備 | ✅ fact 架構完成（驗收通過）|
| 第四章 | 2 | 文件管制 | ⚠️ clarify 條目存在，fact 待補 |
| 第五章 | 7 | 作業（進出貨、儲存、揀貨）| ✅ fact 架構完成（驗收通過）|
| 第六章 | 4 | 申訴、退回、偽禁藥、回收 | ⚠️ clarify 條目存在，fact 待補 |
| 第七章 | 1 | 委外 | ⚠️ clarify 條目存在，fact 待補 |
| 第八章 | 1 | 內部稽核 | ⚠️ 已有部分 fact，需核對 |
| **合計** | **48** | | |

> **注意：** 「文件數 = 48」是指 SOP/WI/FR 文件份數。KB 條目數（71筆）≠ 文件數。
> 一份文件通常產生 1 個 clarify + 2-5 個 fact，合計每章 3-10 筆。

---

### 42.3 新增文件時的 KB 維護流程

```
觸發：發現資料夾新增了一份 SOP/WI/FR
      ↓
Step 1：確認文件編號與章節（放入哪一章）
      ↓
Step 2：讀取文件全文，拆解可獨立問答的 fact
      ↓
Step 3：建立對應的 clarify 條目（若該文件尚無）
      ↓
Step 4：建立各個 fact 條目，每個 fact 補 10 種問法
      ↓
Step 5：在 HANDOFF 更新「第X章：新增文件 [編號]，增加 N 筆 fact」
      ↓
Step 6：跑驗收腳本，確認新 fact 可正確命中
      ↓
Step 7：GitHub Pages 實機驗收通過後 commit + push
```

---

### 42.4 文件修訂時的 KB 維護流程

```
觸發：SOP/WI/FR 發行新版本（內容有變動）
      ↓
Step 1：比對新舊版本差異（只看有變動的段落）
      ↓
Step 2：找出對應的 KB 條目（透過 sop_ref 欄位追蹤）
      ↓
Step 3：更新有變動的 KB 條目內容（不影響無變動的條目）
      ↓
Step 4：同步前台顯示區塊（依第七章同步規則）
      ↓
Step 5：在 HANDOFF 記錄「[文件編號] 版本更新，KB 對應條目 [fact ID] 已更新」
      ↓
Step 6：驗收 + push
```

---

### 42.5 刪除文件時的 KB 維護流程

```
觸發：某份 SOP/WI/FR 被廢止或合併
      ↓
Step 1：找出所有 sop_ref 含該文件編號的 KB 條目
      ↓
Step 2：確認這些事實是否有其他文件支撐
      ├── 有其他文件支撐 → 更新 sop_ref，不刪除條目
      └── 無其他支撐 → 刪除該 KB 條目
      ↓
Step 3：刪除前先 git commit checkpoint（確保可回退）
      ↓
Step 4：在 HANDOFF 記錄「[文件編號] 廢止，相關 KB 條目 [fact ID] 已刪除/更新」
```

---

### 42.6 流程圖文件的 Phase 2 標記

**部分 SOP/WI 文件內含流程圖**，這些流程圖是 Phase 2 視覺互動的素材來源。

**標記規則：** 在對應的 KB clarify 條目加入標記（不影響搜尋邏輯）：

```javascript
docs.push({
  id: "clarify-receiving-shipping",
  sourceType: "clarify",
  hasFlowchart: true,           // ← 文件有流程圖，Phase 2 可做視覺化
  flowchartDesc: "進貨驗收流程圖（4步驟）",  // ← 描述流程圖內容
  // ... 其他欄位
});
```

**Phase 2 工作時，優先從 `hasFlowchart: true` 的條目製作互動流程圖元件。**

---

### 42.7 Phase 1 系統性重建計畫

> **原則：不是刪掉全部重建，而是「逐章審核+補齊」。**

**全八章 fact 已建立完成（2026-06-01 第三十三～三十四次全量逐章校對，2026-06-03 第六十四次複核）：**

> ⚠️ 原本此處標示「待補章節（四、六、七、八章）」已過時。第三十三～三十四次已逐份讀取 SOP 原文、全量校對 96 筆 fact（後續累積至 122 筆），四、六、七、八章均已建立 fact 並通過驗收。本段於 2026-06-03 第六十五次依實際 KB 內容更正。

| 章 | fact 數 | | 章 | fact 數 |
|----|--------|--|----|--------|
| 第一章 | 37 | | 第五章 | 22 |
| 第二章 | 27 | | 第六章 | 8 |
| 第三章 | 33 | | 第七章 | 4（委外 DP72-01 單一 SOP）|
| 第四章 | 10 | | 第八章 | 4（內部稽核 DP82-01 單一 SOP）|

- fact 總計 122 筆，全部具 `sop_ref`（0 筆缺漏）；驗收 `verify_facts_ghpages.mjs` 1296/1296 通過。
- **第七、八章各 4 筆**：對應原文各僅 1 份 SOP，4 筆為該 SOP 可拆解事實的自然上限；每筆 fact 帶 12～78 個問法變體，符合「每個問題 ≥10 種問法」。不得為湊滿「每章 10 個問題」而捏造原文沒有的 fact（牴觸 §11/§45 evidence-based）。

**每章重建包含：**
1. 逐份讀取文件原文
2. 拆解 fact，每個補 10 種問法
3. 安全審查（Layer 2 vs Layer 3 邊界）
4. 驗收腳本全章通過
5. GitHub Pages 實機驗收
6. HANDOFF 標記完成

---

## 四十三、開工與檢查自動修正規則

### 43.1 開工 / 專案繼續時的固定動作

當使用者說「開工」「專案繼續」「繼續專案」時，AI 必須先完成 AGENTS.md 指定的四份文件讀取，再回到本規範執行檢查。

固定讀取順序：

1. `HANDOFF.md`
2. `G:\我的雲端硬碟\我的第二大腦\踩坑紀錄.md`
3. `G:\我的雲端硬碟\我的第二大腦\專案\2026codex AI測試.md`
4. `GDP_智慧查詢規範.md`

若使用者本次已指定明確任務，可直接做該任務；若未指定任務，才詢問要從 HANDOFF 哪個待辦開始。

### 43.1.1 交接狀態判讀優先序（2026-06-03 新增）

**開工讀取的目的，是判斷最新有效狀態，不是把所有文件曾經出現過的待辦全部列出。**

每次開工、換對話、換電腦、或由不同 AI 接手時，必須依下列順序判讀：

| 優先序 | 來源 | 用法 |
|--------|------|------|
| 1 | `HANDOFF.md` 最上方最新輪次 | 判斷目前完成、未完成、阻塞與下次接續。 |
| 2 | 最新 `HTML視覺權限改善報告_2026-06-02.md` 補記 | 判斷 HTML 視覺、互動、文件保護、測驗平台與登入暫停狀態。 |
| 3 | 本規範 `GDP_智慧查詢規範.md` | 判斷什麼可以自動修、什麼必須先問使用者。 |
| 4 | 第二大腦專案 note 最新日期 | 補充決策歷程與使用者已確認事項。 |
| 5 | `AGENTS.md` | 作為開工入口與摘要，不作為覆蓋最新 HANDOFF 的依據。 |
| 6 | HANDOFF 舊輪次、第二大腦較早段落、歷史 commit 記錄 | 只能作背景，不得直接列為目前未完成。 |

**若文件互相矛盾，依下列方式決定最新狀態：**

1. 先看日期與輪次：最新輪次優先於舊輪次。
2. 再看驗收：有 `JS_PARSE_OK`、`verify_facts_ghpages.mjs`、實機桌機/手機、GitHub Pages 永久網址驗收者，優先於只有文字待辦者。
3. 再看 Git 狀態：`HEAD`、`origin/<branch>`、工作樹、未 push commit 必須實際查，不可推測。
4. 再看線上：只要涉及永久網址是否生效，必須查 GitHub Pages，不可只看本機檔。
5. 若仍無法判定，列為「需確認」，不可宣稱「真正未完成」或直接開工修改。

**禁止行為：**

- 不可把 `AGENTS.md` 舊待辦表直接整理成「真正未完成項目」。
- 不可把 HANDOFF 舊輪次中的「未 push」「未完成」「回退待重做」直接套用到最新狀態。
- 不可忽略最新報告中的「誤判撤銷」或「使用者裁示跳過」。
- 不可把「暫停需前置條件」寫成一般未完成，例如登入/正式權限、測驗平台題庫。
- 不可為了讓待辦清單看起來完整，把已完成的 Phase 1、mobileMenu、已驗收互動項目重新列為未完成。

**狀態分類標準：**

| 類別 | 定義 | 下一步 |
|------|------|--------|
| ✅ 已完成 | 最新輪次有完成與驗收紀錄，且 Git/Pages 未推翻。 | 不再列為未完成；只可列為背景。 |
| 🔄 進行中 | 已有部分完成，但可針對具體區塊再提升。 | 列具體區塊，不可籠統寫「全部未做」。 |
| ⚠️ 可再盤點 | 可能仍有改善空間，但未確認有違規。 | 先檢查再決定是否修改。 |
| 📝 設計完成待資料 | 已有設計，但缺使用者提供來源、題庫、角色或範圍。 | 先向使用者收資料，不可自行推測。 |
| ⏸️ 暫停 | 使用者或規範明定目前不能開工。 | 不列派工；等前置條件確認。 |
| ❌ 真正未完成 | 最新輪次確認尚未做，且不屬暫停或需前置。 | 可依使用者指定開始。 |

**本專案截至 2026-06-03 第六十六次的已確認狀態：**

- Phase 1 KB：✅ 已完成，122 筆 fact、全 8 章、0 缺 `sop_ref`、1296/1296。
- HTML 視覺/互動/文件保護：最新實機複檢未發現需立即自動修正的違規。
- mobileMenu：✅ 本機與 GitHub Pages 線上命中 0。
- 常見缺失逐筆頁碼：使用者已裁示維持現狀；無 OCR/頁碼對照時不得硬補。
- 測驗平台：📝 設計完成待使用者提供各部門受測 SOP 範圍與題庫來源。
- 登入/正式權限：⏸️ 暫停，待權限表確認。

### 43.2 檢查時不得只回報問題

後續檢查 HTML、KB、Firestore、權限或視覺互動時，只要發現不符合本規範且可以從既有來源判斷正確作法，必須直接修正，不等使用者一問一答指出。

可自動修正的類型：

- KB 三欄出現 AI 指令語、開發備忘、路徑、`更新者：system`。
- `xinshing` 欄開頭出現「新勝醫藥依規定」「新勝醫藥為」「依規定，新勝醫藥」等非稽查短答語氣。
- clarify keywords 保留 who / when / 期限 / 頻率等應屬於 fact 的專屬問法。
- 快速查詢按鈕與手動查詢結果不一致。
- 快速查詢第一名不是 fact，或命中 fact 但回答混入其他流程。
- HTML / KB / 搜尋 intent / 稽核提示殘留「冷藏」「冷鏈」「冷藏倉」「冷藏庫」「冷藏設備」。
- 同一 fact 在智慧查詢、部門頁、規範到現場、稽查重點、FAQ 出現不同內容。
- HTML 修改後未做 JavaScript 語法檢查。
- 搜尋評分邏輯與 `verify_facts_ghpages.mjs` 不一致。
- 未登入 / 一般員工畫面露出不該公開的 Layer 2 / Layer 3 公司內容。

### 43.2.1 自動修正範圍界線

檢查時應分成「可直接修正」與「必須先問使用者」兩類。

**可直接修正：**

| 類型 | 處理 |
|-----|------|
| 錯字、亂碼、重複文句 | 直接改成正確繁體中文 |
| AI 指令語、開發備忘、路徑資訊 | 從 learner-facing 內容移除 |
| `xinshing` 欄非稽查短答語氣 | 改成員工可直接回答的短句 |
| clarify keyword 搶 fact | 移除 clarify 越界 keyword，補 fact 自然問法 |
| 快速查詢與手動查詢不一致 | 同步按鈕 data-q、keywords、驗收腳本 |
| 快速查詢命中 clarify 或長段流程 | 建立/補強對應 fact，並將具體 keyword 從 clarify 移出 |
| 冷藏/冷鏈相關殘留 | 依公司單一常溫倉庫事實改寫或移除 |
| 同一 fact 前台多處不一致 | 依 SOP/WI/FR 來源同步全部區塊 |
| 權限標籤缺漏但可由既有部門資料判斷 | 補上 `departments` / `allowedRoles` 標籤 |
| HTML/JS 語法錯誤 | 直接修復並跑語法檢查 |

**必須先問使用者：**

| 類型 | 原因 |
|-----|------|
| SOP/WI/FR 未明文的公司制度 | 不可自行創造公司做法 |
| 新增跨部門權限 | 會影響員工能看到哪些資料 |
| 管理者、組長、特殊角色名單 | 需公司實際授權 |
| 常見缺失來源不明 | 不可用 AI 推測當作稽查缺失 |
| Firebase rules / Authentication 權限架構改動 | 會影響正式登入安全 |
| 是否把某資訊列為公司內部缺失 | 需有公司內部稽核或使用者確認 |

### 43.3 需要先問使用者的情形

遇到以下情形不可自行猜測：

- 新增或修改公司制度，但 SOP/WI/FR 沒有明文依據。
- 判斷某職稱是否能跨部門看資料，且本規範沒有列明。
- 管理者、組長、GDP 權責人、管理藥師的實際權限名單尚未確認。
- 常見缺失來源不是 PIC/S 或食藥署公開資料。
- 需要新增 Firebase collection / rules / Authentication 流程，且會影響正式登入權限。

### 43.4 每次檢查的固定清單

每次修改或驗收前，至少檢查：

```
□ 本次改動是否有 SOP/WI/FR 或官方來源依據
□ 同一 fact 是否已同步六個前台區塊
□ 智慧查詢第一名是否正確，是否排除無關結果
□ 快速查詢是否與手動輸入同題一致
□ 所有快速查詢是否第一名命中 fact，而非 clarify
□ clarify keywords 是否仍含「誰、多久、幾月、幾筆、期限、頻率、怎麼處理、如何評鑑」等應屬於 fact 的具體問法
□ HTML / KB / 搜尋 intent / 稽核提示是否仍殘留冷藏或冷鏈相關字樣
□ 一般員工是否只看到自己角色可見資料
□ 組長 / 管理者跨部門權限是否有明確規則支撐
□ 常見缺失是否來自官方或已記錄來源
□ 若外部來源有明文，是否已回公司 SOP/WI/FR 核對；若 SOP 未明文，是否已列差異清單而非自動補成公司制度
□ HTML / JS 語法檢查是否通過
□ 若有 HTML/CSS/JS 修改，是否 commit + push + GitHub Pages 驗收
```

---

## 四十四、登入後部門權限與智慧查詢範圍規則

### 44.1 權限設計原則

登入功能上線後，資料顯示與智慧查詢都必須依角色限制，不得只限制畫面導覽而讓查詢仍可問到全站資料。

權限控制需同時套用：

- 左側導覽與部門頁可見範圍
- 智慧查詢可搜尋的 facts / clarify / 常見缺失
- 快速查詢按鈕
- 稽查重點與常見缺失卡片
- 文件閱讀地圖與收件時程
- 統計面板的可見資料範圍

### 44.2 一般人員預設規則

一般人員只看自己部門相關資料。

例：倉管一般人員：

- 只能看到倉管相關教材、倉管頁、倉管稽查重點。
- 智慧查詢只接受倉管相關問題；非倉管範圍應顯示「此問題不在目前角色可查範圍」類型的中性提示，不顯示其他部門答案。
- 快速查詢只列倉庫常見缺失、倉管文件時間點、倉管稽查問答。
- 倉庫常見缺失需依歷年政府公開資料建立；同一缺失若有不同年度多筆資料，以最新年度為準。

### 44.2.1 查詢拒答規則

登入後若使用者查詢超出目前角色可見範圍，系統不得顯示答案摘要、不得顯示其他部門名稱、不得透露「某部門有這份資料」。

拒答文案應中性、簡短：

> 此問題不在目前角色可查範圍，請洽主管或管理者確認。

禁止拒答文案：

| 禁止寫法 | 原因 |
|---------|------|
| 「這是採購部資料，你不能看」 | 洩露資料歸屬 |
| 「此問題涉及文件 DPxx-xx」 | 洩露文件編號與架構 |
| 「你沒有權限查看倉庫退貨流程」 | 洩露其他角色可見主題 |
| 顯示模糊答案後再提示無權限 | 已經洩露內容 |

實作要求：

- 權限過濾需先於搜尋結果渲染；未授權資料不可進入結果清單。
- 快速查詢按鈕必須在角色層先過濾，不得顯示不可查的按鈕。
- 統計與熱門問題也要套用角色範圍，不得用排行榜透露其他部門常查內容。

### 44.3 組長跨部門權限規則

組長可見範圍不是自動全開，必須依實際業務交界逐項列明。

已確認方向：

| 角色情境 | 可見範圍 | 原因 |
|---------|---------|------|
| 倉庫組長 | 倉庫資料 | 本部門管理責任 |
| 倉庫組長涉及到貨驗收 | 採購每日到貨驗收紀錄表相關資料 + 倉庫收貨資料 | 採購每日早上需印製到貨驗收紀錄表，倉管組長可能需對接 |
| 倉庫組長涉及文件變更 | 文件變更流程相關資料 | 倉庫作業或表單變更時需理解文件變更流程 |

未列明的跨部門情境，不得自行放寬權限。需先問使用者確認後，再寫入本章或部門權限表。

### 44.4 管理者與特殊角色

管理者、GDP 權責人、管理藥師、品保主管等角色可能有跨部門或全站權限，但實作前必須先建立正式權限表。

正式權限表至少包含：

| 欄位 | 說明 |
|-----|------|
| roleId | 系統角色代碼 |
| 顯示名稱 | 員工看到的角色名稱 |
| 所屬部門 | 主要部門 |
| 可見部門 | 可看哪些部門資料 |
| 可查詢主題 | 智慧查詢允許哪些 topic / department tags |
| 快速查詢範圍 | 快速查詢按鈕來源 |
| 常見缺失範圍 | 可見的政府常見缺失 |
| 收件時程範圍 | 可見哪些行事曆項目 |
| 管理功能 | 是否可審核帳號、調整職稱、查看統計 |
| 需使用者確認 | 尚未確定的跨部門例外 |

### 44.5 資料模型要求

每筆 KB / 常見缺失 / 快速查詢 / 文件地圖項目，登入功能前需補齊權限標籤：

```javascript
{
  departments: ["倉管"],
  allowedRoles: ["warehouse_staff", "warehouse_lead", "admin"],
  crossDeptFor: ["warehouse_lead:採購到貨驗收", "warehouse_lead:文件變更"],
  visibilityLayer: "layer2",
  deficiencyYear: 2025,
  deficiencySource: "TFDA 公開資料"
}
```

規則：

- 沒有 `departments` 或 `allowedRoles` 的 learner-facing 資料，不得進入登入後正式查詢。
- 權限過濾必須在資料層與 UI 層同時做；不可只用 CSS 隱藏。
- Firestore rules 需配合權限模型，避免使用者直接讀取未授權 collection。

---

## 四十五、角色化常見缺失與快速查詢規則

### 45.1 常見缺失來源

角色化常見缺失只能來自：

- 食藥署公開 GDP 查核、輔導、常見缺失資料。
- PIC/S GDP 稽查指引或公開缺失分類。
- 已由使用者提供並確認可用的官方資料。

不得用 AI 推測「某部門可能常犯什麼錯」。

### 45.1.1 公司缺失與政府常見缺失分離

前台必須明確區分「政府公開常見缺失」與「公司內部缺失」。

| 類型 | 可否顯示 | 顯示名稱 | 來源要求 |
|-----|---------|---------|---------|
| 政府公開常見缺失 | ✅ 可顯示 | 政府公開常見缺失 | PIC/S 或食藥署公開資料 |
| 本部門應準備證據 | ✅ 可顯示 | 本部門應準備的證據 | SOP/WI/FR 或內部訓練規範 |
| 公司內部實際缺失 | ⚠️ 需確認 | 公司內部稽核發現 | 需使用者提供或確認內部稽核紀錄 |
| AI 推測缺失 | ❌ 禁止 | 不得顯示 | 無合法來源 |

禁止把政府公開常見缺失寫成「本公司常見缺失」。除非使用者提供公司內部稽核、CAPA、缺失紀錄並明確同意，否則前台一律使用「政府公開常見缺失」。

建議前台呈現格式：

```
政府公開常見缺失：訓練紀錄未完整保存
本部門應準備的證據：年度訓練計畫、簽到紀錄、測驗或合格紀錄
稽查員可能怎麼問：教育訓練怎麼做？紀錄保存在哪裡？
公司如何回答：依內部訓練制度執行，並保存訓練紀錄與合格結果。
```

### 45.2 同一缺失多年度資料

若同一缺失在不同年度有多筆資料：

1. 以最新年度作為前台主要顯示。
2. 舊年度可保留為趨勢或歷史紀錄，但不得蓋過最新年度。
3. 前台文字需標示年度或資料期間，避免讓使用者誤以為是公司內部實際缺失。

### 45.3 快速查詢按鈕生成規則

快速查詢必須依角色生成：

| 角色 | 快速查詢內容 |
|-----|-------------|
| 一般人員 | 自己部門最常見缺失、固定週期、稽查常問短答 |
| 組長 | 本部門 + 已核准跨部門情境 + 收件/稽核管理問題 |
| 管理者 | 全站缺失、跨部門流程、帳號/訓練/統計管理問題 |

每個快速查詢按鈕必須有對應的可命中 keyword；按鈕結果與手動輸入同一句必須一致。

---

## 四十六、網頁版視覺化互動規劃規則

### 46.1 互動必須由文字內容決定

視覺化不是裝飾。每個區域要先讀該區文字，判斷學習任務，再選互動型態。

| 文字內容類型 | 優先互動型態 |
|-------------|-------------|
| 流程步驟 | 拖拉排序、流程節點點選、左右滑動 stepper |
| 權責分工 | 部門泳道、角色切換、責任矩陣 |
| 時間點 / 頻率 | 時間軸、月份篩選、頻率分組、勾選清單 |
| 稽查問答 | 翻卡、情境題、問答配對、證據卡展開 |
| 常見缺失 | 缺失卡片、年度篩選、部門篩選、改善證據對照 |
| 文件申請 / 閱讀重點 | 純文字文件名 + 學習重點卡，不顯示 SOP 路徑 |
| 跨部門流程 | 拖拉式責任交接、泳道流程、交界情境卡 |

### 46.1.1 視覺化互動等級

互動需分級驗收，不得把裝飾動畫當成交付。

| 等級 | 名稱 | 定義 | 是否算主要互動 |
|-----|------|------|---------------|
| L0 | 裝飾效果 | hover、陰影、淡入、按鈕變色、scroll fade-in | ❌ 不算 |
| L1 | 基本展開 | tabs、accordion、點擊展開、modal 顯示說明 | ⚠️ 只算基本互動 |
| L2 | 操作篩選 | 部門/角色/月分/年度篩選、勾選核對、狀態切換、排序 | ✅ 算有效互動 |
| L3 | 訓練型互動 | 拖拉排序、情境題、問答配對、流程判斷、操作後給回饋 | ✅ 優先採用 |

驗收規則：

- 每個主要頁面至少要有 1 個 L2 以上互動。
- 「稽查重點」區優先使用 L3；若只做 L1，需有明確原因。
- L0 可作為輔助視覺，但不得在交付說明中宣稱為主要互動。

### 46.1.2 每種內容的首選互動路徑

| 內容 | 首選互動 | 不建議 |
|-----|---------|--------|
| 稽查問答 | 翻卡 + 情境題 + 答案回饋 | 純 Q&A 條列 |
| 流程 | stepper / 泳道流程 / 拖拉排序 | 靜態流程圖無操作 |
| 時程 | 月份時間軸 + 勾選核對 + 頻率篩選 | 大表格堆疊 |
| 權責 | 部門矩陣 / 角色切換 / RACI 類責任格 | 單純列人名或部門名 |
| 常見缺失 | 年度篩選 + 缺失原因 + 準備證據 | 寫成公司缺失清單 |
| 文件閱讀地圖 | 學習路徑 + 調閱提示 + 申請目的 | SOP 路徑或全文連結 |

### 46.1.3 文件閱讀地圖呈現方式

文件閱讀地圖的目的不是讓員工直接看 SOP 內容，而是讓員工知道「遇到什麼問題時，要向文管調閱哪一類資料」。

建議呈現為「學習路徑 / 調閱指引」：

| 顯示欄位 | 說明 |
|---------|------|
| 我想了解 | 員工的自然問題，例如「到貨驗收怎麼做？」 |
| 應調閱資料 | 文件名稱或文件編號 + 中文名稱，純文字顯示 |
| 調閱目的 | 了解流程、確認表單、準備稽查證據 |
| 適用角色 | 哪些部門或角色需要讀 |
| 申請對象 | 文管部 |
| 不顯示內容 | SOP 路徑、全文、表單欄位、完整流程細節 |

互動建議：

- 用情境卡讓員工先選「我遇到的問題」。
- 點選後顯示「建議向文管調閱：文件名稱 / 用途 / 為什麼需要」。
- 可加「已申請 / 已閱讀 / 需主管協助」勾選狀態，但不連到文件本身。
- 未登入或非該部門角色不可看到不屬於自己的文件調閱提示。

### 46.2 不合格互動

以下不算完成互動化：

- 只有 hover 陰影、淡入動畫、按鈕變色。
- 只有靜態卡片堆疊，沒有學習行為。
- 拖拉/點選後沒有改變資訊或沒有提供判斷回饋。
- 視覺元件與該區文字無關，只是套版。

### 46.3 每個互動元件的驗收標準

每個新互動元件完成前需確認：

```
□ 它服務哪一個學習任務（GDP 是什麼 / 常見缺失 / 稽查怎麼答 / 什麼時候做文件）
□ 使用者操作後會得到什麼新資訊或回饋
□ 文字不溢位、不重疊，手機與桌面皆可操作
□ 不顯示 SOP 路徑、內部文件正文或開發者備忘
□ 對應 fact 與前台內容一致
□ 若牽涉權限，未登入與一般員工不可看到不該看的資料
```

### 46.4 視覺驗收與瀏覽器檢查

每次新增或修改視覺互動元件後，必須實際用瀏覽器檢查，不可只看程式碼。

最低驗收：

```
□ 桌機視窗至少檢查一次
□ 手機寬度至少檢查一次
□ 每個新增互動元件至少操作一次
□ 文字沒有重疊、截斷、溢出按鈕或卡片
□ 點擊 / 拖拉 / 篩選 / 勾選後有正確回饋
□ 權限相關區塊在未登入與一般員工視角不露出不該看的資訊
□ 若是 HTML/CSS/JS 修改，需做 JavaScript 語法檢查
□ push 後需用 GitHub Pages 永久網址驗收
```

建議驗收方式：

- 使用瀏覽器或 Playwright 截圖保存桌機與手機畫面。
- 若使用本機預覽，仍需在 push 後用永久網址確認線上版本。
- 若視覺驗收發現文字重疊或互動不可操作，視為未完成，不得只記為待辦。

---

## 四十七、行事曆勾選的多人同步與「誰做的」歸屬（登入系統開工時必做）

> 背景：年度資料收件行事曆與關鍵設備行事曆為**全公司共用的一份核對表**（單一 Firestore 文件 + `merge:true`，多人寫同一份）。使用者要求：之後做權限時，「假設 031 把 1 月份資料標完成，要看得出這個月是誰做的」。

### 47.1 目前資料模型只記「做了沒」，不記「誰做的」

| 寫入位置 | 記錄內容 | 有沒有「誰」 |
|---------|---------|-----------|
| `gdpTrainingCalendarStatus/{sectionId}/items/{年度__itemId}` | `sectionId, itemId, statusYear, itemIndex, status, updatedAt` | ❌ 無人欄；多人共用同一份、`merge:true`，只留最後一次 `updatedAt` |
| `gdpUsageEvents`（analyticsStore 事件流） | `eventType, visitorId, displayName, targetLabel, status, createdAt` | △ 有 `visitorId`/`displayName`，但 `visitorId` 是**每台瀏覽器隨機 id**（localStorage `gdp-visitor-id`），`displayName` 預設「匿名使用者」，**無法對應真實員工工號 031** |

**結論：登入系統做起來之前，無法可靠查出「1 月是誰標的」。** 這是預期內，因為登入/權限為暫停項。

### 47.2 登入/權限開工時必做（先決策，不可 AI 自行實作）

1. **先拍板語意**：行事曆是「全公司共用一份核對表 + 標記誰做的」（依使用者意圖），不是每人各自一份。
2. **狀態本體加人欄**：`markedBy`（工號，如 031）、`markedByName`、`markedAt`，可查「目前這個狀態是誰設的」（只留最後一人）。
3. **加稽核軌跡（GDP 紀錄可追溯，更該有）**：每次點擊 append 一筆 `{工號, 姓名, sectionId, itemId, 月份, 狀態, 時間}`，保留**完整「誰在何時把哪個月標完成/取消」歷史**，不被覆蓋。現有 `gdpUsageEvents` 已 90% 是此形狀，登入後把匿名 `visitorId` 換成已驗證工號即可。
4. **Firestore rules 防冒名**：強制 `markedBy == request.auth.uid`，否則 047 可寫假裝是 031 的紀錄。
5. 上述屬 §43「帳號/權限設計需先與使用者確認 collection 與 rules」，與官方缺失 collection 同一類，不得在權限表（部門 × 角色 × 可見資料）確認前自行實作。

### 47.3 硬性規則：行事曆 set() 欄位異動必須同步改 Firestore rules（本案踩坑）

**根因案例（2026-06-09 加年度隔離時引入、2026-06-10 才發現）**：`calendarStatusStore.set()` 改寫入 `statusYear`、docId 改為 `${年度}__${itemId}` 後，**線上部署的** `gdpTrainingCalendarStatus` rules 仍是舊版（`hasOnly` 不含 `statusYear`、且 `data.itemId == 路徑itemId` 因 docId 加年度前綴而永遠不等），導致**所有行事曆勾選 Firestore 寫入 permission-denied**，程式 fallback 到 localStorage（只存自己這台瀏覽器）→ **多人之間完全不同步，各看各的勾選**。REST 實測：線上只有 5 筆舊格式 doc、最新停在 6/8、equipment-calendar 0 筆，6/9 起新格式一筆沒進雲端。

**更深一層**：`firestore.rules` **檔案其實早已改對**（auto-save 進 git），但**從沒被 deploy 到 Firebase**。「git 裡 rules 正確」≠「線上 rules 正確」。auto-save 只進版本控制，不部署。

**規則：**
- 任何改 `calendarStatusStore`（或其他 store）的**寫入欄位集合或 docId 命名**時，**必須同時更新 `firestore.rules` 的 `hasOnly`/`hasAll`/docId 綁定，並實際 `deploy`（不是只 commit），再用 REST 讀回確認新格式 doc 寫得進去**。否則寫入被靜默 catch、退回 localStorage，症狀是「線上看起來能勾、但別人看不到、重整後可能消失」，且可潛伏多日。
- 驗收同步功能不可只靠單機 localStorage：須在**正式網域**用兩個瀏覽器/裝置實測 A 勾選 B 看得到；或用 REST 讀 `gdpTrainingCalendarStatus/<section>/items` 確認出現含 `statusYear`、docId 為 `年度__itemId` 的新 doc。
- 本機 localhost 出現 permission-denied 屬非授權網域正常 fallback；但**正式 GH Pages 網域若也 permission-denied，就是 rules 與程式（或部署）不同步的 bug，不是「設計上的 fallback」**，不可當正常。
- 此 Google Drive 工作區連 Read 檔案內容都可能是漂移舊版；重大判斷（尤其 rules/部署）要用 `git show HEAD:<檔>`、`git hash-object` 對齊，不可只信單次 Read。

---

## 四十八、ERP 式角色權限矩陣（正航式・預設模板＋可微調）— Phase 3 設計

> 來源：使用者 2026-06-12 提供①現行組織職稱表 ②正航 ERP「用戶權限設置」畫面，裁示 GDP 平台權限要採「**群組(角色) × 功能 × 動作**」可勾選矩陣，由管理員維護，**預設模板＋可微調**（非全空白矩陣、非只「部門看部門」）。
> **本章為 Phase 3 設計（§37.1：可設計、不可現在寫 enforcement code）。** 本次只定設計＋補 §44.5 資料標籤，不接登入、不接過濾生效。

### 48.1 兩條使用者裁示的設計原則（高於一切細節）

1. **一人多職 → 多角色聯集**：單一帳號可指派多個 roleId，可見部門與權限取**聯集（取最寬）**。沿用既有測試專區 `composeTemplate` 兼任取 SOP 聯集的同套邏輯。
2. **職位常變 → 角色清單由管理員維護、不 hardcode**：roleId / 部門 / 職稱清單存 Firestore `gdpDeptConfig`（§38.8），程式只認 roleId，不寫死人名或職稱字串。「公司越大越會細分」→ 設計需容許日後新增分區組長等角色而不改 code。
3. **職務角色與平台管理權限必須分離（2026-06-17 使用者再次明定）**：品保、業務組長、GDP權責人、倉庫組長、採購、業助等是「職務/部門角色」，不等於永遠具有帳號審核或管理面板權限。現在的品保、業務組長、GDP權責人可因公司現況另授予 `gdp_admin`，但未來新任同職稱人員不得自動變成平台管理者。平台管理者是一個獨立權限，需由超管或既有管理者明確勾選/設定。
4. **部門員工樹是管理畫面的核心（2026-06-17 使用者再次明定）**：左側需固定按部門分組；例如 032 申請倉管，應出現在「倉管」下方；新採購到職，應出現在「採購」下方。此樹狀結構同時適用待審核帳號與在職員工權限管理，不可只在有 pending 時才顯示。

### 48.2 角色群組（依組織表，2026-06-12，須與 §44.4 正式權限表並用）

| roleId | 顯示名稱 | 系統部門 | 預設層級 | 現況備註 |
|--------|---------|---------|---------|---------|
| `role_rp` | GDP權責人（總經理） | rp | 職務角色 | 目前應另授予 `gdp_admin`；未來新任 GDP 權責人是否為平台管理者需個別設定 |
| `role_qa` | 品保 | qa | 職務角色 | 目前品保經理應另授予 `gdp_admin`；未來新進品保不一定是平台管理者 |
| `role_sales_lead` | 業務組長（經理） | sales | 職務角色 | 目前兼採購，可同時授予 `role_purchase`；目前應另授予 `gdp_admin` |
| `role_purchase` | 採購 | purchase | 職務角色 | 目前由業務組長兼任；未來可能新增獨立採購，或採購兼業助 |
| `role_sales` | 業務 | sales | 職務角色 | 一般業務，不預設平台管理者 |
| `role_sales_assistant` | 業助 | sales / purchase | 職務角色 | 未來可能由新採購兼任業助，也可能採購與業助分成兩人 |
| `role_warehouse_lead` | 倉庫組長 | warehouse | 職務角色 | 是否為平台管理者依實際指定，不由職稱自動判定 |
| `role_warehouse_staff` | 倉管 | warehouse | 職務角色 | 一般倉管，不預設平台管理者 |
| `role_accounting_hr` | 會計／人事 | personnel | 職務角色 | 目前由會計擔任人事 |
| `pharmacist` | 管理藥師 | pharmacist | 職務角色 | 基本不在公司、掛名；預設僅本部門教材，無管理功能 |
| `role_doc` | 文管 | doc | 職務角色 | 文管職務角色，和開發者/超管身份分開 |
| `gdp_admin` | 平台管理者 | cross | 管理權限 | 可審核帳號、進入管理面板、調整員工角色；需明確勾選或由超管 email 名單設定 |
| `doc_superadmin` | 文管（開發者） | doc | **超級管理員** | 031；hardcode email，負責新增第一個管理員、系統設定（§38.1） |

**層級沿用 §38.1 三層＋超管**：一般員工 / 組長·主管 / 管理員 / 超級管理員。
**本公司目前現況（2026-06-17 使用者重述）**：GDP權責人＝總經理、業務組長＝經理、採購目前由業務組長兼任、品保＝經理、文管＝031（開發者）、另有倉庫組長、倉管、會計/人事。現況中品保、業務組長、GDP權責人應為網站平台管理者；031 為文管與開發者超管。這是「目前人員指派」，不是永久職稱規則。

**過渡相容規則**：舊版 Firestore profile 可能仍含 `rp_admin/qa_admin/sales_lead/warehouse_lead`。前台需把這些 legacy role 正規化顯示為「對應職務 + `gdp_admin`」，避免管理者看見舊模型；rules 可暫時保留 legacy 放行，直到所有既有帳號重存成新 roles 後再移除。新核准/新調整不得再產生 legacy role。

### 48.3 模組 × 功能 × 動作（取代正航的會計動作欄）

**動作欄（GDP 平台版）：** 檢視 ｜ 勾選/標記 ｜ 列印/匯出 ｜ 審核 ｜ 跨部門全查 ｜ 帳號管理

| 模組 | 功能項 | 適用動作 |
|------|-------|---------|
| 智慧查詢 | 查詢本部門範圍 / 查詢全部門 | 檢視・跨部門全查 |
| 部門教材頁 | 檢視部門頁 / 列印讀書單 | 檢視・列印・跨部門全查 |
| 常見缺失 | 檢視政府公開缺失卡 | 檢視・跨部門全查 |
| 文件閱讀地圖 | 檢視本部門文件地圖 | 檢視・跨部門全查 |
| 收件時程行事曆 | 檢視 / 勾選完成 / 覆核他人勾選 / 看全部門 | 檢視・勾選・審核・跨部門全查（§38.7：一般員工不顯示，組長以上才顯示）|
| 測驗 | 作答 / 看自己成績 / 審核他人成績 / 看全部門成績 | 檢視・勾選・審核・跨部門全查 |
| 帳號管理 | 審核申請 / 停用 / 改角色 / 產啟用碼 / 維護部門職稱清單 | 帳號管理（僅管理員以上）|
| 統計面板 | 看本部門統計 / 看全部門統計 | 檢視・跨部門全查 |

### 48.4 預設權限模板（每角色一套，管理員可逐格微調）

以「一般員工 / 組長(管理員) / 超管」三套基準模板開局，套到各 roleId 後管理員再微調：

| 模組·功能 | 一般員工 | 組長/管理員 | 超級管理員 |
|---|---|---|---|
| 智慧查詢 | 本部門檢視 | 本部門＋§44.3 跨部門 | 全部門 |
| 部門教材＋列印讀書單 | 本部門檢視＋列印 | 本部門＋跨部門 | 全部門 |
| 常見缺失 / 文件地圖 | 本部門檢視 | 本部門＋跨部門 | 全部門 |
| 收件時程行事曆 | **不顯示**（§38.7） | 檢視＋勾選＋覆核（本部門範圍）| 全部門 |
| 測驗 | 作答＋看自己 | ＋審核本部門成績 | 全部門 |
| 帳號管理 | — | 審核／停用／改角色／產啟用碼 | ＋設超管／系統設定 |
| 統計面板 | — | 本部門 | 全部門 |

**§44.3 跨部門須逐項列明、不自動全開**：業務組長兼採購＝業務＋採購；倉庫組長＝倉管＋採購到貨驗收＋文件變更（§44.3 已確認）。未列明的跨部門例外，必須先問使用者再寫入 §44.4 權限表。

### 48.5 Firestore 資料模型（Phase 3 實作前須 §43 確認 collection 與 rules）

| collection | docId | 主要欄位 |
|---|---|---|
| `gdpRolePermissions` | roleId | `{ level, departments:[], crossDept:[], perms:{ "模組.功能": {view,edit,print,review,viewAll,admin} } }` |
| `gdpUsers` | uid | `{ empId, name, roles:[roleId], status:"active|disabled" }`（roles 為陣列＝一人多職）|
| `gdpUserApplications` | autoId | §38.6（待審核申請）|
| `gdpActivationCodes` | autoId | §38.3（一次性、48h、綁定申請）|
| `gdpPromotionRequests` | autoId | §38.4（組長代申請晉升）|
| `gdpDeptConfig` | `config` | `{ departments:[], titles:[] }`（管理員維護，員工申請表即時讀取）|

### 48.6 單一強制點（chokepoint）架構

- `resolveVisibility(user)` →（讀 user.roles → 各 roleId 的 gdpRolePermissions）→ `ctx = { effectiveDepts(聯集), effectivePerms(各模板逐格取最寬), isAdmin, isSuperAdmin }`。
- **所有 learner-facing 渲染（導覽、智慧查詢結果、快速查詢按鈕、常見缺失卡、文件地圖、行事曆、統計）一律經過單一 `canSee(item, ctx)` / `canDo(action, module, ctx)`**，不得各自寫過濾。
- **未登入 / 無 role → 預設「全顯示」**（＝目前行為，本次補標籤後行為完全不變）。Phase 3 接上登入時，只在這一處把預設改為依 ctx 過濾即生效。
- **§44 雙層過濾**：權限過濾須在**資料層（結果不進清單）＋ UI 層**同時做，**不可只用 CSS 隱藏**；快速查詢按鈕在角色層先過濾、不顯示不可查的按鈕；統計/熱門問題也套角色範圍。
- **§44.2.1 拒答中性**：超出可見範圍時，不顯示答案摘要、不顯示其他部門名稱、不透露「某部門有此資料」；文案「此問題不在目前角色可查範圍，請洽主管或管理者確認」。

### 48.7 §44.5 資料標籤對應（本次補齊的資料層基礎）

| 資料 | 標籤方式 | 狀態 |
|---|---|---|
| KB fact（clarify-* / fact-*）| 個別 `departments` + `allowedRoles` | ✅ 170/170 已標 |
| section（含 dept 頁／核心／行事曆）| 本體新增 `departments` + `allowedRoles`（驅動導覽與 dept 頁可見性）| 本次補 |
| focus（常見缺失）| **繼承所屬 section.departments**（不逐項標，由 chokepoint 取 section 脈絡）| 設計沿用 |
| 快速查詢（12 題）| 平行 `quickQuestionDepts` 對應表；**data-q 字串與 .kb-quick class 不動**（守 §26.3.1 固定驗收題）| 本次補 |
| searchOnly 索引（`section-*`/`docmap-*`/`sop-*`/`file-*`）| 由來源衍生 `departments`（section→section.departments、docmap→[deptId]、其餘標 internal）| 本次補 |

### 48.8 Phase 規則與實作前置

- 本章為 **Phase 3 設計**：**可設計、不可現在寫 enforcement / 登入 / 矩陣 UI 的 code**（§37.1）。
- 實作前必備（§38.9）：管理員 email 清單、超管 email（hardcode）、初始部門/職稱清單、email 通知服務、`gdpRolePermissions`/`gdpUsers` 等 collection 的 Firestore rules 草擬與**部署**（§47.3 硬規：改 store 寫入欄位/docId 必須同步改 rules 並實際 deploy，非只 commit）。
- 本次動工邊界：①本章設計定版 ②補 §44.5 資料標籤（全程不改現有顯示行為、`verify_facts` 維持 1296/1296）。**不接登入、不接過濾、不建矩陣 UI。**

---

## 四十九、AI 管理助理維護模式（平台內自然語言維護入口）

> 來源：使用者 2026-06-15 確認，因公司其他人不會使用 agent、GitHub、Firebase 或程式更新流程，後續平台需要像政府 AI 智慧機器人或 GDP 智慧查詢一樣，讓管理者用自然語言提出維護需求；但 AI 不得直接修改正式網站或繞過審核。

### 49.1 核心定位

AI 管理助理是**管理者的維護入口**，不是自動上線機器人。

正式原則：

1. 管理者以自然語言提出需求。
2. AI 先判斷需求類型與風險。
3. AI 產生草稿、變更摘要、來源依據與影響範圍。
4. 有權限的人按「確認」後，才可寫入 Firestore 日常資料。
5. 涉及 HTML / JavaScript / Firebase rules / GitHub Pages 的程式變更，AI 只能建立更新單或 Pull Request，不得直接改正式站。

### 49.2 四種維護類型

| 類型 | 範例 | AI 可做 | 必須審核 |
|------|------|---------|----------|
| 帳號與職務 | 新員工申請、031 離職、倉管晉升組長、業務組長代理採購 | 帶管理者填表、產生變更摘要、寫入待確認資料 | 管理者確認後才生效 |
| 題庫與測驗 | 題目答案疑似錯誤、某職務要增加測驗範圍 | 建草稿、列來源文件、標示影響題目 | 文管/講師/管理者確認 |
| 教材內容修正 | 某段稽查回答需調整、某 SOP 摘要需更新 | 建議修文、列原文依據、建立待審核版本 | 來源核對後才上架 |
| 網站功能需求 | 新增按鈕、調整權限邏輯、修改登入流程 | 建立功能需求單、產生 PR 草案或交給 Codex/GitHub 流程 | PR、測試、預覽、管理者核准 |

### 49.3 帳號與職務可走平台內確認

下列屬日常管理資料，可由 AI 管理助理引導管理者操作，但需按確認後才寫入：

- 新帳號申請審核。
- 部門、職稱、角色調整。
- 晉升或降職。
- 兼任與代理職務，含生效日與結束日。
- 離職停用。
- 重新啟用。

所有異動必須寫入 `gdpAuditLogs` 或 `gdpRoleChangeLogs`，至少包含：

| 欄位 | 說明 |
|------|------|
| actorUid / actorEmpId | 誰做的 |
| targetUid / targetEmpId | 改誰 |
| before | 異動前角色、部門、狀態 |
| after | 異動後角色、部門、狀態 |
| reason | 管理者輸入或 AI 協助整理的原因 |
| approvedAt | 核准時間 |
| source | `aiAdminAssistant` / `manualAdminPanel` |

### 49.4 教材、題庫與 KB 不得直接覆蓋正式資料

AI 管理助理若收到「這題錯了」「這段說法要改」「新增 SOP 摘要」等需求，必須先建立待審核草稿：

```text
原內容：
建議修改：
修改原因：
來源文件：
影響區塊：
是否牽涉 SOP/WI/FR 原文：
是否牽涉政府公開常見缺失：
```

禁止：

- 未核對來源就直接改 `xinshing`、題目答案或稽查回答。
- 把 AI 推論當公司制度。
- 直接把草稿設為 active。
- 直接把政府常見缺失寫成公司已發生缺失。

### 49.5 程式功能變更必須走 PR / 預覽 / 測試

凡屬以下範圍，AI 管理助理不得直接在正式網站套用：

- HTML / CSS / JavaScript 修改。
- Firebase rules 修改。
- Firestore collection 結構異動。
- GitHub Pages 發佈設定。
- 登入、權限、資料過濾、測驗計分邏輯。
- 任何會影響 learner-facing 顯示或資料安全的功能。

標準流程：

1. AI 建立功能需求單。
2. AI 或 Codex 產生分支 / Pull Request。
3. 自動跑 `JS_PARSE_OK`、`verify_facts_ghpages.mjs`、權限/合規掃描。
4. 產生預覽或測試結果。
5. 管理者看摘要與預覽後核准。
6. 合併後才部署到 GitHub Pages / Firebase。

### 49.6 管理者不需懂 agent，但需看到可判斷資訊

AI 管理助理回覆管理者時，不應要求管理者理解 Git、agent、Firebase CLI 或程式碼。每次高風險變更至少顯示：

- 要改什麼。
- 為什麼改。
- 影響哪些頁面、題庫、角色或資料。
- 需要誰核准。
- 可否回復。
- 驗收結果是否通過。

### 49.7 安全邊界

- AI 管理助理只對已登入且具 `gdp_admin`、`doc_superadmin` 或指定管理權限者開放。
- 一般員工不得使用 AI 管理助理修改資料。
- AI 不得取得或顯示 API key、Firebase private key、GitHub token。
- AI 不得顯示他人完整個資、完整測驗答案庫或未授權部門資料。
- 所有 AI 協助變更都必須留下紀錄，可供日後追溯。

### 49.8 第一版建議實作順序

1. 先做 AI 管理助理的「需求分類與草稿」介面，不接程式自動改版。
2. 帳號與職務異動先接平台內確認流程。
3. 教材與題庫修正先接待審核草稿，不直接上架。
4. 程式功能需求先只建立 GitHub Issue / PR 草案。
5. 等登入與權限穩定後，再評估是否讓 AI 觸發更完整的 Codex/GitHub Action 流程。

---

## 五十、進站前平台使用同意書（consent gate）

> 來源：使用者 2026-06-16 確認。進站前須先閱讀條列式使用聲明，勾選同意或不同意，並記錄員工編號與結果供後續稽核。

### 50.1 定位與流程

- 同意書是登入 gate 的**一個關卡 state**，不是獨立頁面：插在 `gateState()` 的 `picker→profile→pending` 之後、`ready` 之前，命名 `consent`。
- 觸發條件：帳號已 `active`，但 `profile.consentVersion !== CONSENT_VERSION`。
- **超級管理員（文管/開發者）也必須同意**，不得豁免（稽核精神）。
- 同意 → 寫稽核 log ＋ 在 `gdpUsers` 標記 `consentVersion`/`consentedAt` → 放行進站。
- 不同意 → 寫稽核 log（decision=disagree）→ 登出，不得進站。

### 50.2 條文內容原則

- 以**條列式**呈現，至少涵蓋：① 為公司內部 GDP 教育訓練平台（含國際法規、食藥署常見缺失、稽核知識）② 勿用身分證等私人身分資訊當密碼 ③ 不得提供/轉發/展示給公司以外人員 ④ 內容可能與其他公司不同，一切以國際法規、食藥署及本公司正式文件（SOP/WI/FR）為準，有疑問反映管理者 ⑤ 不取代 SOP 全文 ⑥ 同意即記錄員工編號與結果供查核。
- 條文為**程式內受信任常數 `CONSENT_TERMS`**（可含 `<strong>`），渲染時不可 `esc`；但帶入的使用者值（姓名、員工編號）仍須 `esc`。

### 50.3 版本與重新同意

- `CONSENT_VERSION="YYYY-MM-DD"` 寫死於 client；條文有實質變更時 bump 版本，所有人下次進站自動重新閱讀並重新同意，無需清資料。

### 50.4 稽核資料與 rules

- **新集合 `gdpConsentLogs`（append-only）**：欄位 `uid`/`empId`/`name`/`email`/`decision`('agree'|'disagree')/`version`/`createdAt`。rules：`read: if isAdmin()`；`create: if signedIn() && data.uid==auth.uid && decision in ['agree','disagree']`；`update,delete: if false`。**不可改寫 `gdpAuditLogs`**（其 create 限 isAdmin，一般員工寫不進去）。
- 同意回寫 `gdpUsers` 自己的 `consentVersion`/`consentedAt` → `gdpUsers` self-update 白名單須含這兩欄並**重新部署 rules**，否則同意被靜默 deny、員工卡死進不去（§47.3 同型坑）。

---

## 附錄 C：文件結構說明

| 路徑 | 用途 |
|-----|------|
| `HTML資料庫/新勝GDP資料庫.html` | 主要工作檔 |
| `Firebase設定/firestore.rules` | Firestore 規則 |
| `Firebase設定/sync-gdp-knowledge.ps1` | Firestore 同步腳本 |
| `HANDOFF.md` | 開工接續紀錄 |
| `AGENTS.md` | 跨 AI agent 共用規則 |
| `CLAUDE.md` | Claude Code 開工指示 |
| `GDP_智慧查詢規範.md` | 本文件 |

---

*本文件 v3.3 新增 §49「AI 管理助理維護模式（平台內自然語言維護入口）」：依使用者 2026-06-15 確認，未來管理者可在平台內用自然語言提出帳號、職務、題庫、教材或功能維護需求；AI 只能產生草稿、變更摘要、需求單或 PR 流程，不得直接修改正式網站；日常帳號/職務資料須管理者確認後寫入 Firestore 並留異動紀錄，教材與題庫須先建待審核草稿，程式功能變更必須走 PR / 預覽 / 測試 / 核准。v3.2 新增 §48「ERP 式角色權限矩陣（正航式・預設模板＋可微調）— Phase 3 設計」：依使用者 2026-06-12 提供之組織職稱表與正航 ERP 用戶權限設置畫面，定版「群組(角色) × 功能 × 動作」可勾選矩陣設計（一人多職取聯集、角色清單管理員維護不 hardcode、預設模板＋可微調、單一 chokepoint 雙層過濾、Firestore collection 與資料標籤對應）；明定本章為 Phase 3 設計，可設計不可現在寫 enforcement/登入/矩陣 UI code，本次只定設計＋補 §44.5 資料標籤。v3.1 新增「外部明文與公司 SOP 的自動核對規則」：PIC/S 或食藥署有明文時，agent 在建立常見缺失、稽查回答、題庫或 fact 時必須同步回公司 SOP/WI/FR 核對；公司文件未明文者只能列差異清單交使用者判斷，不得自動補成公司制度或正式題庫。v3.0 新增 §47 行事曆勾選的多人同步與「誰做的」歸屬設計，並明定「改 store 寫入欄位/docId 必須同步改 Firestore rules 並重新部署」硬性規則（2026-06-10 發現行事曆 statusYear/docId 改版後 rules 未同步、寫入全 permission-denied、多人各看各的 localStorage 之踩坑）。v2.9 補強 §43.1.1 交接狀態判讀優先序。v2.8 曾補強 §26 快速查詢全量驗收規則、§27 冷藏/冷鏈相關字樣硬性掃描、§43 自動修正固定清單。後續如有新踩坑點，應同步更新此規範，並更新版本號。*

*規範更新責任：每次收工若發現新踩坑，先寫入踩坑紀錄.md，再決定是否需補入本規範；若屬新規則（非已有章節覆蓋）則必須補入。*
