# 測試專區製作方法：文管範本

更新：2026-06-05

## 目的

本文件記錄 `HTML資料庫/新勝GDP資料庫.html` 測試專區的製作方式，供後續 AI agent 依同一方法建立其他部門測驗。

目前已建置文管、人事、採購三個草稿題庫範本。本文件的用途是讓其他 AI 擴充新職責時照同一模板做，不重新設計測試平台、不回到多選 checkbox、不自行推測 SOP 範圍。

## 硬性規則

1. 測試專區是獨立導覽分組，不混入新人部門或年度行事曆，也不放進 GDP 知識庫同一分組。
2. 導覽排序需在 `GDP 知識庫` 下方成立獨立 `測試專區`。
3. 導覽只保留單一入口 `測試專區`；第一頁只放測驗規則與開始按鈕，不放部門/職責選擇。
4. 測驗畫面採三階段：
   - 開始測試
   - 測驗人員資料
   - 考試畫面
5. 第二頁測驗人員資料欄位依序為：姓名、性別、職責（主要職稱）、報到日期、學歷、兼任/代理職務。
6. `職責（主要職稱）` 必須是下拉選單，只列已建題庫的職責；不可讓考生自由輸入或用 checkbox 多選。
7. `兼任 / 代理職務` 必須是單一下拉選單，預設 `無兼任 / 代理`；選定主要職責後，下拉只列其他已建題庫職責。
8. 不使用 checkbox。原因：漏勾會造成受測 SOP 範圍不足，考完後才發現會導致重測。
9. 目前低選擇量設計只支援「一個主要職責 + 最多一個兼任/代理」。若日後有多重兼任，應由登入/人事資料預先帶入，不要回到多 checkbox。
10. 進入考試畫面後，右上角顯示倒數時間。
11. 考試進行中需限制切換至部門文件閱讀地圖與 GDP 智慧查詢。
12. 題數只能選 `10`、`20`、`25`、`50` 題，因為必須讓 100 分可整除：
   - 10 題：每題 10 分
   - 20 題：每題 5 分
   - 25 題：每題 4 分
   - 50 題：每題 2 分
13. 每份測驗最多 50 題。
14. 題數需依「來源文件多寡、內容風險、是否管理職、是否年度整合」決定；文件少不得硬塞題目。
15. 每題需有來源文件、來源段落、答案、解析、`reviewedBy`、`reviewDate`，否則只能停在草稿，不得上架正式測驗。
16. 題目不得由 AI 依常識自行補制度、補答案或補表單欄位。
17. 政府公開常見缺失可納入年度測驗，但需取自 TFDA/PIC/S 或使用者確認來源，不得寫成本公司缺失。
18. 倒數時間歸零時需自動交卷，未作答題以未作答計，不可只停錶。
19. 合格或未合格都要顯示本次作答結果；合格後提供人事可列印資料包，未合格則提示重新測驗。
20. 前台考生畫面不得顯示題庫維護欄位，例如 `reviewedBy`、`reviewDate` 或內部審查狀態。

## 最新 UI 模板（其他 AI 必須照做）

### 第一頁：開始測試

第一頁只做規則說明，目的是讓考生知道測驗規則，不在這裡選職責或測驗別。

必要內容：

- 測驗依 FR24-03 與 WI10-01 職務訓練要求設計。
- 70 分合格，未合格需重測。
- 新人三個月測驗 10 題；年度教育訓練測驗 20 題。
- 題目與選項每次隨機。
- 倒數歸零自動交卷。
- 考試期間限制切換文件與智慧查詢。
- 只留一個 `開始測試` 按鈕。

### 第二頁：測驗人員資料

第二頁才決定抽題範圍，欄位固定如下：

| 欄位 | 控制型態 | 規則 |
|------|----------|------|
| 測驗別 | radio | 新人三個月 / 年度教育訓練 |
| 姓名 | text | 必填 |
| 性別 | select | FR24-03 欄位 |
| 職責（主要職稱） | select | 只列已建題庫職責 |
| 報到日期 | text | 民國格式，例如 `114/06/04` |
| 學歷 | text | 可留空 |
| 兼任 / 代理職務 | select | 預設 `無兼任 / 代理`，只列主要職責以外的已建題庫職責 |
| 年度教育訓練日期 | text | 民國格式，必填 |
| 訓練項目 | text / hidden-sync | 由職責與兼任自動帶出 |
| 授課講師 | text | 必填 |
| 評核方式 | select | 線上測驗 / 紙本測驗 / 口頭評核 |

下方顯示 `受測範圍（依職責與兼任自動帶出）` 來源卡，只顯示章節與文件名稱，不顯示 SOP 路徑或全文。

### 第三頁：考試畫面

第三頁只顯示：

- 測驗人員基本資訊。
- 測驗別、題數、時間、70 分合格。
- 倒數時間。
- 題目、選項、上一題/下一題/提交測驗。
- 不顯示 `reviewedBy`、`reviewDate`、內部題庫狀態。

## 其他 AI 擴充新職責的固定步驟

新增任何職責時，依序完成以下步驟，不得跳步：

1. 讀 `WI10-01 SMF 廠商基本資料`「職務訓練要求」，確認該職責必測 SOP。
2. 讀對應 SOP/WI/FR 原文，逐題建立來源段落；不可只看檔名。
3. 建立 `examTemplates.<key>`：
   - `role`
   - `roleSource`
   - `commonDocs`
   - `roleDocs`
   - `printItems`
   - `sourceBlocks`
   - `blueprints`
   - `draftQuestions`
4. 在 `examDeptNames` 加入新 key 與顯示名稱。下拉選單會自動帶入。
5. 不新增新的 checkbox UI；使用既有 `roleSelect` 與 `actingSelect`。
6. 確認 `composeTemplate(keys)` 可合併新職責：
   - `roleDocs` 依文件代碼去重。
   - `printItems` 依文字去重。
   - `draftQuestions` 依題目文字去重。
   - DM10-01 / WI10-01 共同基礎只計一次。
7. 新題庫維持 `options[0]` 為正解，因 `buildQuestions()` 會先標記第 0 個為正解再洗牌。
8. 驗證新人 10 題、年度 20 題都能抽足；若題庫不足，不可硬上線，需回 SOP 補題或降低題數。
9. 跑語法、禁用字、題庫結構與 `verify_facts_ghpages.mjs`。
10. 更新 `HANDOFF.md`、第二大腦、commit、push。

### 標準程式資料形狀

```js
examTemplates.<key> = {
  role: "職責顯示名稱",
  roleSource: "WI10-01 對應職務訓練要求摘要",
  commonDocs: [
    { code: "DM10-01", name: "品質手冊", role: "共同基礎", why: "..." },
    { code: "WI10-01", name: "SMF 廠商基本資料", role: "共同基礎", why: "..." }
  ],
  roleDocs: [
    { code: "DPxx-xx", name: "SOP 中文名", role: "職責專業", why: "..." }
  ],
  printItems: [
    "DM10-01 GDP 共同基礎訓練",
    "WI10-01 GDP 共同基礎訓練",
    "DPxx-xx SOP 中文名訓練"
  ],
  sourceBlocks: [
    { doc: "DPxx-xx", section: "來源段落", summary: "可追溯摘要" }
  ],
  blueprints: [
    { topic: "題目主題", sourceDoc: "DPxx-xx", sourceSection: "來源段落", difficulty: "basic|applied" }
  ],
  draftQuestions: [
    {
      text: "題目文字",
      options: ["正解", "錯誤選項1", "錯誤選項2", "錯誤選項3"],
      sourceDoc: "DPxx-xx",
      sourceSection: "來源段落",
      explanation: "答案解析",
      status: "draft"
    }
  ]
};
```

## 文管範本設計

### 共同基礎

`DM10-01` 與 `WI10-01` 視為同一組共同基礎，不拆成兩張獨立考科：

| 文件 | 定位 |
|------|------|
| DM10-01 品質手冊 | GDP 原則：應該怎麼做 |
| WI10-01 SMF 廠商基本資料 | 公司實際做法：公司如何做 GDP |

製題時，同一題可同時考「GDP 原則」與「公司實際做法」。例如題庫群名稱可寫 `DM10-01 + WI10-01 共同基礎`。

### 文管專業範圍

| 文件 | 製題方向 |
|------|----------|
| DP42-01 文件管制作業程序書 | 文件制修訂、審查核准、登錄發行、訓練後生效、作廢與定期審查 |
| DP42-02 品質紀錄作業程序書 | 品質紀錄鑑別、儲存、完整性、檢索、保存期限與處置 |

### 題數配置

| 測驗入口 | 題數 | 分數 | 建議分布 |
|----------|------|------|----------|
| 新人三個月測驗 | 10 題 | 每題 10 分 | 共同基礎 3 題、DP42-01 4 題、DP42-02 3 題 |
| 年度教育訓練測驗 | 20 題 | 每題 5 分 | 共同基礎 5 題、DP42-01 7 題、DP42-02 5 題、官方公開缺失 3 題 |

文管目前只有 `DP42-01`、`DP42-02` 兩份專業文件，加上 `DM10-01/WI10-01` 共同基礎，25 題偏多；因此本範本採新人 10 題、年度 20 題。分布可依講師確認後微調，但題數仍只能是 10、20、25、50。

## FR24-03 與列印資料包

文管範本需對應 `FR24-03 員工教育訓練記錄表 文管` 的欄位：

- 測驗人員資料：姓名、性別、報到日期、學歷、單位、職稱。
- 教育訓練紀錄欄位：訓練項目、訓練單位、訓練者、訓練日期、人事簽核。
- HTML 第一版先產生「文管列印資料包」，供列印或人工保存；正式 Firebase 保存仍待登入/權限設計完成。
- 列印資料包可顯示訓練項目文件編號，因為這是 FR24-03 保存用資料；一般學習頁仍不得顯示 SOP 路徑或全文。
- 逾時自動交卷時，資料包需標示逾時交卷。
- 無法自行列印的人員（例如外部業務或不在公司內部印表機環境者），目前先提供「複製送交文管資料」與「下載送交文管資料」作為暫行送交方式，讓人員用公司既有管道交給文管。
- 不得產生公開分享連結，因為資料包含姓名、報到日期、分數與答題紀錄；登入與正式權限完成前，不可把這些資料放到公開網址或無權限保護的雲端連結。
- 待登入與權限功能完成後，需將暫行送交方式改為登入後的內部送交流程：由受測者提交給文管角色，文管可依權限檢視、列印 FR24-03 與考卷式答題紀錄，並保存送交/接收狀態。

## 其他部門仿照流程

1. 先固定放入共同基礎：`DM10-01 + WI10-01`。
2. 依 `WI10-01` 職務訓練要求找出該部門必測 SOP。
3. 若員工兼任或代理職務，測驗範圍取主要職務與兼任職務 SOP 聯集。
4. 依資料量選題數：
   - 來源少、1 到 2 份專業 SOP：10 題
   - 一般部門、3 到 5 份專業 SOP：20 題
   - 管理職、高風險或年度整合測驗：25 題
   - 多職務聯集、管理職且文件量大：50 題
5. 每個 SOP 原則上至少 1 題；高風險 SOP 可加權，但不得為湊題數拆出來源不明的題。
6. 題庫先列 `topic`、`sourceDoc`、`sourceSection`、`questionType`、`roles`、`difficulty`、`status:draft`。
7. 講師確認後才補 `reviewedBy`、`reviewDate`，並改為 `active`。

## HTML 實作位置

本輪在 `HTML資料庫/新勝GDP資料庫.html` 加入：

- section：
  - `exam`
- 導覽分組：
  - `測試專區`
- 共用資料：
  - `examTemplates.doc`
  - `examTemplates.hr`
  - `examTemplates.purchase`
  - 後續新職責新增 `examTemplates.<key>`
- 職責顯示：
  - `examDeptNames`
- 共用渲染：
  - `examTemplateSection(s)`
- 本機檢核互動：
  - `bindExamTemplate()`
- 抽題範圍合併：
  - `composeTemplate(keys)`
  - `selectedKeys()`
  - `applyRole()`

## 已建置部門（依本方法擴充）

2026-06-05 依本方法新增人事、採購兩個範本（`examTemplates.hr`、`examTemplates.purchase`），與文管共用同一渲染與作答機制。

最新版 UI 不使用「受測部門複選」。第二頁改為：

- `職責（主要職稱）`：單一下拉，預設文管。
- `兼任 / 代理職務`：單一下拉，預設無兼任；只列主要職責以外的已建題庫職責。
- 切換時來源卡、hidden `department/title/acting`、訓練項目欄與列印資料包 `printItems` 一併更新。

| 部門 | 共同基礎 | 專業 SOP（依 WI10-01 職務訓練要求） | 新人/年度題數 | 題庫題數 |
|------|----------|--------------------------------------|----------------|----------|
| 文管 | DM10-01 + WI10-01 | DP42-01、DP42-02 | 10／20 | 51 |
| 人事 | DM10-01 + WI10-01 | DP22-01 組織與職掌、DP24-01 員工教育訓練 | 10／20 | 26 |
| 採購 | DM10-01 + WI10-01 | DP52-01 供應商評鑑、DP54-01 進出貨管理、DP72-01 委外作業 | 10／20 | 34 |

- WI10-01 職務訓練要求逐字確認：人事＝DP22-01＋DP24-01；採購＝DP52-01＋DP54-01＋DP72-01。不可照部門名稱推測。
- 每題 `options[0]` 為正解（撰寫/校對慣例）；`buildQuestions()` 作答時洗牌選項並重算 `correctIndex`，考生畫面與列印考卷的 A/B/C/D 位置每次皆隨機。
- 題目全部回對應 SOP 原文（unzip docx 取 word/document.xml 逐段比對）；目前仍為**草稿題庫**，正式上架前需講師確認並補 `reviewedBy`、`reviewDate`。

### 兼任/代理職務：低選擇量下拉 + 受測範圍取聯集

- `職責（主要職稱）` 選文管 → 只考文管範圍。
- `兼任 / 代理職務` 預設 `無兼任 / 代理` → 不額外加入題庫。
- 若兼任選人事 → 題庫為文管 + 人事 SOP 聯集 + 共同基礎。
- 若兼任選採購 → 題庫為文管 + 採購 SOP 聯集 + 共同基礎。
- `composeTemplate(keys)` 合併所選職責的 `roleDocs`、`printItems`、`draftQuestions`，DM10-01／WI10-01 共同基礎只計一次。
- 此版最多一個兼任/代理；不要為了支援多兼任改回 checkbox。若未來要多兼任，改由登入/人事資料自動帶入。

## 禁止重犯

- 不可一次重寫完整測驗平台造成全站失效。
- 不可直接建立未審核正式題庫。
- 不可把 DM10-01 與 WI10-01 拆成互不相關的兩科。
- 不可使用 15 題、18 題等無法讓 100 分整除的題數。
- 不可不看文件量就把所有部門都設成 25 題或 50 題。
- 不可只按部門名稱推測 SOP 範圍，必須回 WI10-01 與來源 SOP。
- 不可把兼任/代理職務做成 checkbox，避免漏勾、多勾與重測風險。
- 不可把尚未建立題庫的職責放進下拉選單。
