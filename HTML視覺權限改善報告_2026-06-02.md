# HTML 視覺、合規與權限改善報告（2026-06-03 實機複檢版）

> 用途：供下一位 AI agent 接續處理 `HTML資料庫/新勝GDP資料庫.html`。
> 本輪檢查方式：**實際以瀏覽器開啟頁面**（桌機 1280px + 手機 375px，模擬主管與員工操作），非只看程式碼或後台。
> 依據：`AGENTS.md`、`GDP_智慧查詢規範.md v2.8`、`踩坑紀錄.md`、`html-training-deck`、`html-evidence-training-builder`、`evidence-based-content`。
> 檢查者：Claude（2026-06-03）

---

## 本輪重點

1. 上一版（2026-06-02）以「靜態掃描」得出「無」。本輪改用**實機瀏覽器逐頁操作**，發現問題 → **報告不刪除**。
2. 共檢出 3 項候選問題；逐一查證後：**2 項屬實並已修正、1 項為誤判已撤銷**。
3. 另完成「年度教育訓練測試平台」設計與前置清單（任務 3）。

> ⚠️ 教訓：**自動驗收全綠 ≠ 沒問題**，但**「DOM 裡看到某元素」也 ≠ 它就是使用者看到的元素**（見撤銷項）。複檢務必開瀏覽器、並確認元素的實際 `display`/作用中狀態。

---

## 自動驗收狀態（修正後，通過）

- `JS_PARSE_OK 1`
- `node verify_facts_ghpages.mjs`：`1296/1296` 通過，0 失敗。
- 合規掃描：AI 指令語、個資電話、冷鏈/冷藏設備、溫度測繪法規、常見缺失題 — 全數通過。
- 靜態掃描：`openDoc`、`.doc-modal`、`data-doc=`、`完整 SOP`、`熱門文件` = 0。

### 2026-06-03 Codex 再複檢補記

- 使用 Chrome 實際渲染本機 HTML，桌機 `1280×900` 與手機 `375×812` 各檢查一次。
- 桌機：左側導覽顯示；手機：`#mobilePicker` 自製選單顯示，原生 `#mobileMenu` 仍為隱藏狀態。
- 桌機與手機皆無水平溢位、無 console error。
- 精準操作 `#kbForm/#kbInput` 查詢「倉庫溫度幾度」：查詢後 `#kbMessages` 不再殘留「尚未查詢」，並正確顯示「倉庫溫度範圍」結果。
- 人事頁前台操作：可進入頁面、翻卡可點擊、桌機 checkbox 可勾選；可見文字未出現 SOP 路徑、禁用字或 learner-facing 文件代碼。
- 再跑 `JS_PARSE_OK 1` 與 `node verify_facts_ghpages.mjs`：`1296/1296` 通過。

---

## 一、已修正項目（本輪 commit）

### ✅ 修正 1【§5.3 / §31.2，踩坑回歸】智慧查詢「尚未查詢」殘留

- **問題**：在「GDP 智慧查詢」輸入「倉庫溫度幾度」按查詢後，`#kbMessages` 仍保留初始 `<p>尚未查詢…</p>` 且可見，與查詢氣泡並存。此為踩坑紀錄（第十二次）已記載問題之回歸。
- **修法**（`bindKbForm > doQuery` 開頭）：
  ```js
  const placeholder=box.querySelector("p");
  if(placeholder) placeholder.remove();
  ```
  首次查詢移除初始提示，保留對話 append 歷史；之後查詢無 `<p>` 即 no-op。
- **驗收**：`JS_PARSE_OK 1`、`1296/1296`，實機確認查詢後不再殘留。

### ✅ 修正 2【§39.5 / §28，原始碼層暴露】移除公開站 SOP 完整路徑清單

- **問題**：公開站 client JS 內嵌**完整文件路徑（含章節資料夾結構）**，「檢視原始碼」即可重建文件架構，牴觸 §39.5（禁展示完整文件架構）與 §28（防競業複製）。實際暴露面比初判更廣，分三處：
  1. `projectSourceFiles` 陣列（含 `簡報資料/...pptx`）。
  2. `sopLibrary` **81 個條目的 `file:` 欄**（`第X章資料夾/檔名.docx`）。
  3. clarify-* 條目的 `file:` 欄。
  （這些 `sop-*`/`file-*` 條目皆 `searchOnly:true` 不渲染於學員答案，但路徑仍存在於原始碼。）
- **修法**：
  - `projectSourceFiles` 改「代碼+中文名」，刪除 pptx。
  - 全檔所有 `file:"第X章.../..."` **去資料夾前綴**，只留檔名（保留搜尋與 `isUnsafeSourceDoc` 防護所需）。
- **結果**：projectSourceFiles 52→51；另去除 81 個 `file:` 資料夾路徑；**全檔資料夾路徑殘留 = 0**；`content` SOP 摘要與搜尋保留。
- **驗收**：`JS_PARSE_OK 1`、`1296/1296`（搜尋未受影響）。檔名+代碼屬 §39.4 允許範圍；§39.5 的「資料夾架構」風險已消除。
- **誠實補記**：首次只修 `projectSourceFiles`、報「已修」，經 GitHub Pages 線上比對才發現 81 個 `sopLibrary` 路徑未處理（修正不完整）→ 已補修完成。教訓：§39.5 路徑掃描須掃全檔所有 `file:`，不可只看單一陣列。

---

## 二、撤銷項目（誠實更正）

### ❌（撤銷）原列「§19.1 手機主導覽用原生 select」— 經複查不成立

- **初判**：在 DOM 看到 `<select id="mobileMenu">` 涵蓋全部單元，誤判為手機主導覽違規。
- **複查事實**：
  - CSS 第 357 行 `.mobile-menu{display:none}` — 該 `<select>` **永遠隱藏**，從未被覆寫顯示，僅作隱藏狀態鏡像（`showSection` 設其 `.value`）。
  - 第 358/371 行 `.mobile-picker{display:none}` → `@media(max-width:960px){.mobile-picker{display:block}}`，且第 370 行 `nav{display:none}`。**手機實際主導覽是自製按鈕面板 `#mobilePicker`**。
  - 面板由 `renderNav()`（第 4733–4767 行）建構：分組標題 `.mobile-picker-group` + 可點 `<button class="mobile-picker-option">` + 開合 toggle（aria-expanded）+ 點外關閉；`showSection` 同步 option active 狀態並更新 toggle 文字為目前單元（第 4792/4795 行）。
- **結論**：完全符合 §19.1（自製大按鈕面板、可點 button、同步目前狀態）。**無違規，不修改導覽程式。**
- **備註**：隱藏的 `<select>` 為無害鏡像，可保留；若要清爽可日後移除，非必要。

---

## 三、視覺互動建議（模擬使用者實機操作；非違規，供 Phase 2 排序）

### A. 30–50 歲主管視角
- 桌機：左側 280px sticky 分組側欄正常；四大學習目標卡（GDP是什麼／常見缺失／稽查如何回答／什麼時候做文件）利於掃讀。
- 建議：四大目標卡可加「點擊跳該區」錨點，主管一鍵直達。

### B. 18–30 歲一般員工視角
- 手機：版面手機優先、乾淨；翻卡（L3，flipped/flip-checked 切換正常）、stepper（23）、證據核對台（7 項）、右下浮動 `☰` 跳轉選單（時程/流程/稽查/文件）皆可操作；**無水平溢位、無 console error**。
- 結論：核心互動已達 §46 標準，體驗良好。本輪僅查詢殘留需修（已修）。

---

## 四、年度教育訓練「測試平台」建議（任務 3）

> 依使用者提供之 **FR24-03 員工教育訓練紀錄表（文管）** 與 GDP 內訓需求。
> 嚴守 §11 / §39 / §45 與 `evidence-based-content`：**題目與標準答案必須可追溯至 SOP 原文（Layer 2），AI 不得自行出題或杜撰答案。**

### 4.0 來源位置確認（2026-06-03 Codex 再複檢）

- `第二章人事/FR24-03 員工教育訓練記錄表 文管.docx` 已存在。
- `第四章文件管理/DP42-01文件管制作業程序書.docx` 已存在。
- `第四章文件管理/DP42-02品質紀錄作業程序書.docx` 已存在。
- 因此「文管測驗範圍 DP42-01 / DP42-02」已有來源檔可追溯；但題目與標準答案仍需依 SOP 原文逐題建立並由講師/使用者確認，不可只依 HTML 既有摘要自動產題。

### 4.1 FR24-03 表單結構（實際讀取）
- 表頭：姓名／性別／報到日期／學歷／**單位**／**職稱**
- 表格欄位：**訓練項目｜訓練單位｜訓練者｜訓練日期｜人事簽核**
- 文管列：`DP42-01 文件管制作業程序訓練`、`DP42-02 品質記錄管理程序訓練`
- 備註：本表由人事單位保存；訓練後經訓練講師評鑑受訓成效，由人事簽核。

→ 洞見：FR24-03 的「訓練項目」= **各職位依 SMF 職務說明所需受訓的 SOP 清單**。測試平台核心 = 把「部門/職位 → 受測 SOP 範圍」對應起來。

### 4.2 架構建議：**獨立測驗區塊**（非埋在各部門內）

理由：
1. **多重身分（最關鍵）**：使用者明說有「業務兼採購」「文管兼倉管」。獨立區塊可讓員工**複選一個或多個職位 → 受測 SOP 範圍取聯集 → 一次 15 分鐘測驗 → 一張對應 FR24-03 的可列印紀錄**。若埋在部門內，多重身分者須分次各考、難合成單張紀錄。
2. **共用基礎建設**：計時器、亂數抽題、評分（≥70 分合格）、列印樣板集中維護，優於 8 個部門頁重複。
3. **與 FR24-03 一對一**：列印結果 = FR24-03 版式。
4. **知識安全（§39）**：題目只測「概念與稽查應答」（Layer 2），**不得重現 SOP 完整流程/表單欄位設計**（Layer 3）。

> 各部門頁可放「前往測驗」入口導向獨立區塊，但題庫與作答邏輯只在獨立區塊維護。

### 4.3 功能需求對應
| 需求 | 實作 |
|------|------|
| 依部門 & 職位測試 | 「職位 → 受測 SOP」對應表（文管→DP42-01/02；其他部門依各自 FR24-03/SMF 職務說明） |
| 多重身分 | 複選職位，受測範圍取**聯集**，合併單一測驗與單張紀錄 |
| 每次題目不同 | 每 SOP 建題庫（建議題數 ≥ 抽題數 3–5 倍），每次亂數抽題 + 選項順序打亂 |
| 15 分鐘 | 前端倒數計時器，到時自動交卷 |
| 可列印 | `window.print()` + `@media print` 樣板，輸出 FR24-03 版式（含簽核欄） |
| 合格判定 | ≥70 分合格（沿用既有事實），畫面與列印標示合格/不合格 |

### 4.4 測驗結果列印版式（對應 FR24-03，員工資料由作答者手填或預留）
```
員工教育訓練測驗紀錄
姓名：____  單位：____  職稱：____  測驗日期：____
─────────────────────────────────────────────
訓練項目（受測SOP）        得分    合格   人事簽核
DP42-01 文件管制作業程序   __/__   □       ______
DP42-02 品質記錄管理程序   __/__   □       ______
─────────────────────────────────────────────
總分：__  結果：□合格(≥70) □不合格    講師評鑑：______
備註：本表由人事單位保存。
```

### 4.5 **建置前必須由使用者提供 / 拍板**（依 §43.3、§45、evidence-based-content）
1. **各部門受測 SOP 範圍**：目前僅文管（DP42-01/02）。其餘部門需提供各自 FR24-03 或 SMF 職務說明所列受訓 SOP（不可 AI 推測）。
2. **題庫來源與審核**：每題與標準答案須出自 SOP 原文並經使用者/講師確認；AI 不得自行出題（§11、§45）。
3. **Phase 順序**：§37.1 規定 Phase 1（KB）完成前不動 Phase 2/3。測驗平台屬新功能；若要先做，需使用者明確同意調整優先序。
4. **身分與成績儲存**：登入（Phase 3）暫停。無登入時採「自選職位 → 作答 → 列印 → 人事簽核」匿名流程；數位化保存成績（綁定身分）待登入上線再做。
5. **架構確認**：本報告建議「獨立區塊」（見 4.2），請使用者確認後再進入設計/實作。

### 4.6 資料蒐集模板（請使用者依此提供，AI 才能建題庫）
```
部門：____  職位：____
受測 SOP：[編號 中文名]、[編號 中文名]…
每個 SOP 由講師提供：
  - 重點概念條列（Layer 2，可作為稽查應答，不含完整流程細節）
  - 題目（單選/是非）＋正確答案＋對應 SOP 段落（供追溯）
```

---

## 五、暫停項目：登入與正式權限

登入端目前無法開工。不列派工：註冊/登入、帳號審核、角色權限 UI、Firestore rules 正式權限改寫、完整權限表。
恢復前需先由使用者確認「部門 × 角色 × 可見資料 × 查詢範圍」權限表；正式過濾須同時落在資料層與 Firestore rules，不可只靠前端隱藏。

---

## 六、處理順序與狀態

| # | 項目 | 狀態 |
|---|------|------|
| 1 | 修正 1：清除「尚未查詢」殘留 | ✅ 已修，2026-06-03 Codex 再複檢通過 |
| 2 | 修正 2：移除原始碼 SOP 路徑清單 | ✅ 已修，2026-06-03 Codex 再複檢未見前台路徑外露 |
| 3 | （原違規1 手機導覽）| ❌ 誤判，撤銷，不需處理 |
| 4 | 測試平台 | 📝 設計完成；待使用者補各部門受測範圍 + 題庫來源 + 確認優先序後實作 |

---

## 七、後續驗收要求（任何 HTML/CSS/JS 改動皆適用）

```text
□ JS_PARSE_OK（語法檢查）
□ node verify_facts_ghpages.mjs（1296/1296）
□ 桌面版「實機瀏覽器」檢查（非只截圖）
□ 手機 375px「實機瀏覽器」檢查 + 操作每個新增/變更互動元件
□ learner-facing 文件代碼 / 路徑命中數 = 0
□ 公司無冷藏倉字樣掃描
□ commit + push + GitHub Pages 永久網址驗收
□ 更新 HANDOFF.md 與第二大腦（踩坑紀錄 + 專案 note）
```
