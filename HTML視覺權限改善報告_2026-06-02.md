# HTML 視覺、合規與權限改善報告（2026-06-02 最新版）

> 用途：供下一位 AI agent 接續檢查 `HTML資料庫/新勝GDP資料庫.html`。本報告只保留目前仍需處理的項目；已完成或已由後續 commit 修正的舊項目以「✅ 已完成」標註並保留簡述，避免重複處理。
> 依據：`AGENTS.md`、`GDP_智慧查詢規範.md v2.8`、`html-training-deck`、`html-evidence-training-builder`。
> 更新依據：2026-06-02 第五十九次重新掃描目前 HTML 與既有驗收腳本。

---

## 目前驗收狀態（2026-06-02 第六十次）

- HTML 內嵌 JavaScript 語法檢查：`JS_PARSE_OK 1`。
- `node verify_facts_ghpages.mjs`：`1296/1296` 通過，0 失敗。
- 第六十次完成項 3、5、6、7 本機預檢；官方來源採食藥署 113/111 年度 GDP 業者說明會常見缺失講義、食藥署 GDP 檢查申請頁與 PIC/S PI 044-1。
- 前台 learner-facing 文件代碼掃描：以瀏覽器逐頁掃描 29 個區段內文，`[A-Z]{2}\d{2}-\d{2}` 命中數 = **0**（ALL_CLEAN）。
- 智慧查詢抽測（溫度超標／品質手冊／內部稽核）：fact/clarify 正常命中，無代碼、無檔案路徑外露。
- 未掃到 `openDoc`、`data-doc`、`SOP 按鈕`、`熱門文件`、`完整 SOP`。
- 未掃到「冷藏 / 冷鏈 / 冷藏倉 / 冷藏庫 / 冷藏設備」。
- 未掃到「不可寫成」「目前資料索引」「更新者：system」等典型 AI 指令語。

---

## 仍需修正項目

### ✅ 1. 自動索引型 KB 條目（已完成 第五十九次）

位置：`localDocs()` 內 4 類自動索引條目
- `sop-*`  SOP 摘要（`HTML資料庫/新勝GDP資料庫.html:1466`）
- `section-*` 頁面資料
- `docmap-*` 部門文件地圖
- `file-*` 資料夾檔案（`HTML資料庫/新勝GDP資料庫.html:1531`）

已做修正：
- 4 類條目全部加上 `searchOnly:true` + `visibilityLayer:"internal-index"`（僅供搜尋輔助，`keywords`/`sop_ref` 不動，搜尋命中不受影響）。
- `sop-*` 三欄移除「文件編號：${code}」。
- `file-*` 的 `xinshing` 移除「檔案路徑：${path}」，改為中性教材語句；路徑只保留在 `file` 與 `keywords` 供搜尋。
- `kbResultsHtml()`（`:4052`）新增 `results.filter(r=>!r.searchOnly)`：索引條目不再被選為 learner-facing 三欄答案；若僅剩索引條目則顯示中性導向訊息（建議換說法或向文管調閱）。

驗收：1296/1296、JS_PARSE_OK 1；智慧查詢抽測無路徑/代碼外露。

### ✅ 2. 第一層 learner-facing 文件代碼外露（本輪大幅清除 第五十九次）

已做修正：
- 新增 `stripInlineCode()`（`:3553` 附近）：移除句中與括號內（含「代碼、代碼」串）的 `DM/DP/WI/FR` 代碼，並清理頓號/空白殘留。資料層代碼保留，僅在 render 層移除。
- 套用 render 點：`標準對照－執行方式`(company 分頁卡)、`資料收件核對台`(data/note)、`部門資料交付時程`(data/note)、`稽查重點翻卡`(問句)、`稽查證據鏈`(audit 清單)、`視覺導讀 lead`、`教育訓練表`(record 欄)。
- 移除「資料收件核對台」meta 內「依據：(原始代碼)」一行（章節標籤已在標題呈現）。
- 直接改寫少數 strip 後語意斷裂的資料字串：偏差/設備/訓練/門禁等 `company` 句、2 句 audit 問句（緊急聯絡與品質手冊／SMF 一致、客戶清冊完成合格客戶清單與認可審查）、設備行事曆 `lead`、`companyProfile` 標題（移除「（WI10-01 §壹）」）。
- 逐頁瀏覽器掃描 29 區段，learner-facing 代碼命中數 = 0。

注意（刻意保留，勿誤刪）：
- `standards.docs`、`collectionCalendar.form/source`、各 fact/clarify `keywords`、`sop_ref` 仍保留代碼——這些是資料層／搜尋／驗收用途，且不在前台 learner-facing 正文渲染（`docs` 已不在 swipe 顯示）。

### ✅ 3. 常見缺失補官方來源（已完成 第六十次）

位置：
- 各 section `focus` 陣列（如 `:648` 起）
- 常見缺失渲染 `paneBody focus`（`standardsSwipe` 內）

已查到官方來源，並於常見缺失展開區新增「官方依據」與來源連結：
- `year:"2024"` / 實地查核統計：食藥署「113年度GDP業者說明會-GDP申請資料準備與常見缺失」PDF。
- `year:"2022"`：食藥署「111年度藥品優良運銷規範(GDP)業者說明會－實地查核流程與常見缺失」PDF。
- `type:"行政指導"`：食藥署藥品 GDP 專區「申請 GDP 檢查」注意事項頁。
- 早期 `研究年報` 類：食藥署藥品 GDP 相關活動/訓練講義頁「GDP法規解析與常見缺失說明」。
- `PIC/S` 預留來源：PIC/S `PI 044-1 Aide-Memoire on GDP Inspections` 官方 PDF。

仍保留原規則：
- 前台只顯示 `TFDA` / `PIC/S`；7 筆 `EU` 不顯示（`source:"EU"` 共 7 筆仍在資料層）。
- 本次未杜撰逐筆公告文號；前台顯示的是已查得的官方講義/公告頁來源。

### ✅ 4. 舊文件彈窗 CSS 殘留（已完成 第六十次）

位置：`HTML資料庫/新勝GDP資料庫.html:265`（`.doc-modal` / `.doc-dialog`）

已移除 `.doc-modal`、`.doc-dialog`、`.doc-head`、`.doc-body`、`.doc-close` CSS。精準掃描 `openDoc` 與 `data-doc=` 均為 0。

---

## 視覺化互動建議

### ✅ 1. 常見缺失：由 L2 勾選升級為「缺失 → 證據準備 → 稽查問法 → 回答方向」（已完成 第五十九次）
- 59 筆顯示中（TFDA/PIC·S）缺失，每筆補 `evidenceToPrepare`、`auditQuestion`、`answerDirection`（依文件順序逐筆撰寫之訓練導引；不含公司杜撰事實、不含文件代碼）。
- `paneBody` focus 展開內容改為四段式：來源 + 📋 本部門應準備的證據 + 🧑‍⚖️ 稽查員可能怎麼問 + 💬 公司回答方向；無新欄位時回退舊「建議」句。
- 新增 `.focus-sub` 系列 CSS（綠/金/藍三色塊）。checkbox + 進度條互動保留。
- 驗收：JS_PARSE_OK 1、1296/1296、桌機 + 手機(375px 無溢出)、注入 59/59 對齊。
- 注意：7 筆 EU 仍未補（前台不顯示）；新欄位內容刻意不放文件代碼，與項 2 一致。

### ✅ 2. 文件閱讀地圖：加入學習狀態（已完成 第六十次）
- 每張文件地圖卡加入「已申請／已閱讀／需協助」狀態，使用 localStorage 保存。
- 依卡片內容自動分類情境：到貨／訓練／稽核／異常／日常；同頁多情境時顯示情境篩選。
- 狀態以卡片上緣顏色回饋，不顯示 SOP 路徑、全文或表單欄位。

### ✅ 3. 證據鏈：升級為證據核對台（已完成 第六十次）
- 保留現場作業／文件紀錄雙節點展開。
- 新增可勾選核對台：紀錄即時、欄位完整、簽核日期完整、批號可追溯。
- 完成度與 checkbox 狀態使用 localStorage 保存；手機寬度改為單欄。

---

## 暫停項目：登入與正式權限

使用者已確認：登入那邊目前無法開工。本報告不再把以下列為派工：註冊/登入流程、帳號審核、角色權限 UI、Firestore rules 正式權限改寫、完整權限表。

保留提醒：恢復登入時需先建「部門 × 角色 × 可見資料 × 查詢範圍」權限表；過濾須在資料層與 Firestore rules 同時完成，不可只靠前端隱藏。

---

## 建議派工順序（不含登入）

1. ✅ 清理 4 類自動索引 KB 條目（已完成 第五十九次）。
2. ✅ 逐區降低 learner-facing 第一層文件代碼外露（已完成 第五十九次，29 區段 0 命中）。
3. ✅ 常見缺失補具體 TFDA / PIC/S 來源；無來源者不顯示（已完成 第六十次）。
4. ✅ 常見缺失互動升級為「缺失 → 證據準備 → 稽查問法 → 回答方向」（已完成 第五十九次，59 筆）。
5. ✅ 文件閱讀地圖加入學習狀態保存（已完成 第六十次）。
6. ✅ 證據鏈升級為證據核對台（已完成 第六十次）。
7. ✅ 移除 `.doc-modal` / `.doc-dialog` 殘留 CSS（已完成 第六十次）。

---

## 後續驗收要求

若修改 HTML / CSS / JS，至少完成：

```text
□ HTML / JavaScript 語法檢查（JS_PARSE_OK）
□ node verify_facts_ghpages.mjs（1296/1296）
□ 桌面版視覺檢查
□ 手機寬度視覺檢查
□ 常見缺失來源欄位檢查
□ 逐頁掃描 learner-facing 文件代碼命中數（應為 0）
□ 若改 HTML/CSS/JS，需 commit + push + GitHub Pages 永久網址驗收
□ 更新 HANDOFF.md 與第二大腦專案 note
```
