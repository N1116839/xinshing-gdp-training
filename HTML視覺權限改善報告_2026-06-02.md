# HTML 視覺、合規與權限改善報告（2026-06-02 複檢版）

> 用途：供下一位 AI agent 接續檢查 `HTML資料庫/新勝GDP資料庫.html`。
> 原則：本報告只列「目前仍需處理」或「目前不能開工」的項目；已完成項目不再列入派工，避免重複處理。
> 依據：`AGENTS.md`、`GDP_智慧查詢規範.md v2.8`、`html-training-deck`、`html-evidence-training-builder`。

---

## 本次複檢結論

目前 HTML 未發現仍需自動修正的規範違反項目。

## 驗收狀態

- HTML 內嵌 JavaScript 語法檢查：`JS_PARSE_OK 1`。
- `node verify_facts_ghpages.mjs`：`1296/1296` 通過，0 失敗。
- 渲染層掃描 14 個 section：未發現 learner-facing 文件代碼、檔案路徑、舊文件彈窗、禁用冷鏈/冷藏字樣、AI 指令語。
- 靜態掃描：未掃到 `openDoc`、`data-doc=`、`SOP 按鈕`、`完整 SOP`、`熱門文件`、`.doc-modal`、`.doc-dialog`。
- 靜態掃描唯一命中 `檔案路徑：` 的位置為程式防護規則，用於阻擋索引文字進入學員答案，不是前台顯示內容。

## 仍需修正項目

無。

## 視覺化互動建議

無。

目前已具備的主要互動包含：
- 常見缺失：勾選進度與「缺失 → 證據準備 → 稽查問法 → 回答方向」展開。
- 文件閱讀地圖：情境篩選與已申請／已閱讀／需協助狀態保存。
- 證據鏈：可勾選證據核對台。
- 稽查重點：翻卡練習與進度回饋。
- 部門流程：stepper 節點展開。
- 年度行事曆與部門時程：頻率／部門／狀態篩選。
- GDP 智慧查詢：問題輸入與快速問題查詢。

## 暫停項目：登入與正式權限

登入那邊目前無法開工。

本報告不把以下列為派工：註冊/登入流程、帳號審核、角色權限 UI、Firestore rules 正式權限改寫、完整權限表。

恢復登入前仍需先由使用者確認「部門 × 角色 × 可見資料 × 查詢範圍」權限表；正式過濾須同時落在資料層與 Firestore rules，不可只靠前端隱藏。

## 後續驗收要求

若之後修改 HTML / CSS / JS，至少完成：

```text
□ HTML / JavaScript 語法檢查（JS_PARSE_OK）
□ node verify_facts_ghpages.mjs（1296/1296）
□ 桌面版視覺檢查
□ 手機寬度視覺檢查
□ 常見缺失來源欄位檢查
□ 逐頁掃描 learner-facing 文件代碼命中數（應為 0）
□ 若改 HTML/CSS/JS，需 commit + push + GitHub Pages 永久網址驗收
□ 更新 HANDOFF.md 與第二大腦專案 note
```
