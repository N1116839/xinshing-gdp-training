# Project Instructions
> 任何 AI agent 開始工作前必讀。此為四台電腦、四個 AI 共用的唯一開工入口。
> 最後更新：2026-06-03（第六十六次，交接狀態刷新）

---

## 專案現況

| 項目 | 值 |
|------|-----|
| 專案名稱 | 新勝醫藥 GDP 內訓 HTML 資料庫 |
| 本機路徑 | `G:\我的雲端硬碟\2026codex\AI測試` |
| 主要交付物 | `HTML資料庫/新勝GDP資料庫.html` |
| GitHub Repo | `https://github.com/N1116839/xinshing-gdp-training` |
| GitHub 分支 | `codex/gdp-html-training-pages` |
| 永久網址 | `https://n1116839.github.io/xinshing-gdp-training/` |
| Firebase 專案 | `xinshing-gdp-training-20260525` |
| Git 使用者 | N1116839 |

**✅ PPT 合規簡報已完成**：`新勝醫藥_GDP合規簡報_v11_final.pptx`（共 17 頁），不需要再修改。

**✅ 目前最新 Git / Pages 狀態（2026-06-03 第六十六次核對）**

| 項目 | 最新狀態 |
|------|----------|
| 目前 HEAD / origin | `f9aaf0b`，`HEAD` = `origin/codex/gdp-html-training-pages` |
| 工作樹 | 讀取時已刷新 Git 索引並確認乾淨 |
| GitHub Pages | 永久網址已實測，`mobileMenu` 線上命中 0 |
| Phase 1 KB | ✅ 完成：122 筆 fact、全 8 章、0 缺 `sop_ref`、1296/1296 |
| HTML 視覺/文件保護 | 最新實機複檢未發現需立即自動修正的違規 |
| 登入/正式權限 | ⏸️ 暫停，需先確認正式權限表 |
| 測驗平台 | 📝 設計完成，未實作，需使用者提供受測 SOP 範圍與題庫來源 |

---

## 開工必讀（每次開工、專案繼續、換電腦都必須讀）

**按順序讀完，才能動手做任何事：**

1. **`HANDOFF.md`**（本資料夾）
   → 目前 Git 狀態、Firestore 狀態、待辦事項優先順序與踩坑點

2. **`G:\我的雲端硬碟\我的第二大腦\踩坑紀錄.md`**
   → 全域長期規則、已知錯誤模式與下次避免方式

3. **`G:\我的雲端硬碟\我的第二大腦\專案\2026codex AI測試.md`**
   → 專案完整歷程、決策紀錄與收工補記

4. **`GDP_智慧查詢規範.md`**（本資料夾）
   → 平台設計規範全文（46章），包含：fact架構、三欄標準、知識安全邊界、常見缺失來源規則、三階段開發藍圖、帳號審核設計、資料庫維護機制、開工自動檢查、登入權限邊界、視覺化互動規則
   → **若不讀此文件，上述所有規則都不在記憶中，會重複踩坑**

5. **`HTML視覺權限改善報告_2026-06-02.md`**（本資料夾）
   → 目前 HTML 視覺、合規、文件保護與權限待修正清單；包含 2026-06-02 下午複檢最新版判斷
   → **若要處理 HTML 視覺、互動、文件代碼、SOP 文案、登入權限或 Firestore 顯示文字，必須先讀此報告**

6. **`GDP_測驗平台與考題規範.md`**（本資料夾）
   → 測試專區、職稱/兼任判斷、WI10-01 職務對應 SOP、Firebase 題庫、FR24-03 列印、70 分合格與補考規則
   → **若要處理新人三個月測驗、年度教育訓練測驗、題庫、作答紀錄、列印給文管或任何測驗功能，必須先讀此文件**

讀完上述文件，若使用者已指定本次任務，直接依任務執行；若使用者只說「開工 / 專案繼續」且未指定任務，再由使用者指定從哪一個待辦項目開始，不可自行決定。

### 最新狀態判讀規則（2026-06-03 新增，避免其他 AI 誤接）

**開工讀取不是把所有文件裡出現過的待辦全部列出。必須先淘汰舊狀態。**

權威優先序如下：

1. `HANDOFF.md` 最上方最新輪次。
2. `HTML視覺權限改善報告_2026-06-02.md` 的最新版補記。
3. `GDP_智慧查詢規範.md` 的狀態表與硬性規則。
4. 第二大腦專案 note 最新日期。
5. 本檔 `AGENTS.md` 摘要。

若上述文件互相矛盾：

- 以最新日期與最新輪次為主，不可引用舊輪次當現況。
- 必須用 `git update-index -q --refresh`、`git diff HEAD --stat`、`git log origin/<branch>..HEAD` 判斷本地/遠端差異。
- 涉及 GitHub Pages 是否生效時，必須直接查永久網址，不可只看本地檔或 `git status`。
- 涉及 UI 違規時，必須實機看桌機/手機顯示；DOM 找得到某元素不等於使用者看得到。
- `AGENTS.md` 舊待辦表只能當摘要，不得覆蓋 `HANDOFF.md` 最新輪次。

---

## 資料夾結構

| 資料夾 | 用途 | 備註 |
|--------|------|------|
| `HTML資料庫/` | 新勝 GDP 資料庫網頁 | 主要工作區 |
| `簡報資料/` | PPT 簡報與輸出 | 已完成，勿覆蓋 |
| `Firebase設定/` | firestore.rules、sync 腳本 | 部署前確認 |
| `官方常見缺失來源/` | TFDA/PIC/S 政府公開常見缺失來源管理 | 供測驗題庫與常見缺失追溯；不是公司 SOP 原文 |
| 第一章至第八章 | 原始 SOP / WI / FR 文件 | 唯一依據，勿修改 |

---

## Firebase 狀態（2026-05-26 最新）

- Firestore rules：**已部署**（gdpDocStats, gdpUsageEvents, gdpUserStats, gdpQuestionStats, gdpRegulationReview）
- Firestore KB（`gdpKnowledgeBase`）：目前 71 筆，來自 52 個來源檔（2026-05-26 最後同步，排除 HTML/README/HANDOFF 非訓練來源）
- 使用者已授權：指定「上傳 github」「更新第二大腦」「使用 Firebase/Firestore」時，可直接執行，不需再問一次

---

## GitHub 狀態（2026-06-03 最新）

- 最後已核對 commit：`f9aaf0b docs: Phase 1 標記完成 + §42.7 章節覆蓋更正（第65次）`
- Pages build：永久網址已實測，`mobileMenu` 線上命中 0
- 目前沒有本地領先 origin 的 commit，也沒有遠端領先本地的 commit
- 只改 HTML/CSS/JS 搜尋邏輯時，必須 commit + push 永久網址才會生效
- 只改 Firestore 資料時，永久網址即時反映，不需推 GitHub

---

## 核心規則（違反即為錯誤，需立刻修正）

1. **KB 欄位只放教材內容**
   - `international` 欄：只放 PIC/S GDP 規範說明
   - `taiwan` 欄：只放台灣 GDP（西藥優良運銷準則）規範要求
   - `xinshing` 欄：只放新勝醫藥正面事實性作業描述
   - **嚴禁**：「不可寫成…」「目前資料索引…」「此筆來自…」等 AI 指令語

2. **資料必須可追溯 SOP/WI/FR 原文**
   - 不可自行推論、補充文件上沒有的事項
   - 有新舊版本時，以資料夾內較新的文件為主
   - 若目錄與 SOP 有出入，列出差異讓使用者確認

3. **公司無冷藏倉**
   - 所有 KB 與 HTML 不可出現冷鏈、冷藏倉、冷藏庫相關描述

4. **非訓練來源不可進入 KB**
   - 同步腳本排除：HTML 本身、README、HANDOFF、AGENTS、index 與 Firebase 設定檔

5. **收工必做四件事**
   - 更新 `HANDOFF.md`（記錄本輪狀態與下輪待辦）
   - 更新第二大腦踩坑紀錄與專案 note
   - `git commit`
   - `git push`（永久網址才會生效）

6. **檢查時自動依規範修正**
   - 後續檢查 HTML、KB、Firestore、權限或互動效果時，若發現不符合 `GDP_智慧查詢規範.md` 且可依來源判斷正確作法，必須直接修正，不等使用者逐項指出。
   - 需要先問使用者的情形：SOP/WI/FR 無明文依據、跨部門權限未列明、管理者/組長名單未確認、常見缺失來源不明、Firebase 權限架構會影響正式登入。
   - 可自動修正：錯字亂碼、指令語、前台不一致、快速查詢不一致、clarify 搶 fact、權限標籤缺漏、HTML/JS 語法錯誤。

7. **登入權限先按部門資料邊界設計**
   - 一般人員只看自己部門資料；智慧查詢、快速查詢、常見缺失也只能回自己部門相關內容。
   - 組長跨部門權限必須依實際業務交界列明。例如倉庫組長可因到貨驗收對接看到採購相關到貨驗收紀錄表資料，也可因文件變更看到文件變更流程。
   - 未列明的跨部門情境不可自行猜測，需先問使用者確認後再加入規範。
   - 查詢超出角色範圍時，不能顯示答案摘要、不能透露其他部門或文件名稱，只能顯示中性拒答提示。

8. **視覺化互動依文字內容規劃**
   - 每個區域先讀文字，再決定互動型態；流程用拖拉/節點/stepper，權責用泳道或矩陣，時程用時間軸或勾選清單，稽查問答用翻卡/配對/情境題。
   - hover、淡入、陰影、按鈕變色不算完成互動化；互動必須讓員工得到新資訊、完成判斷或練習稽查回答。
   - 互動分級：L0 裝飾不算互動；L1 展開只算基本互動；L2 篩選/勾選算有效互動；L3 拖拉/情境題/問答配對為訓練型互動。每個主要頁面至少 1 個 L2 以上，稽查重點優先 L3。
   - 文件閱讀地圖要做成「向文管調閱什麼資料」的學習路徑，不顯示 SOP 路徑、全文或表單欄位。
   - 視覺修改後必須用桌機與手機寬度實際檢查，操作新增互動元件；HTML/CSS/JS 修改需推送後用 GitHub Pages 永久網址驗收。

---

## 待辦事項（2026-06-03 第六十六次刷新；詳見 HANDOFF.md）

| 優先 | 項目 | 狀態 |
|------|------|------|
| 最高 | KB-1：clarify-* 條目台灣/新勝欄含指令語 | ✅ 完成 |
| 高 | C：KB 待補 18 筆三階回覆條目（DM10-01 等） | ✅ 完成（F 全量校對已補齊） |
| 高 | F：KB 全量逐章逐題校對 | ✅ 完成：122 筆 fact、全 8 章、1296/1296；第 7/8 章為單一 SOP 自然上限，不得捏造 fact |
| 中 | 統計：問題排行榜/熱門文件/季度追蹤無有效資料 | ✅ 完成（2026-06-01 植入 TFDA 常見缺失種子資料 + 真實法規更新季度） |
| 中 | D：全站互動性升級 | 🔄 進行中且多項已完成：翻卡、stepper、文件閱讀狀態、證據核對台；後續只能針對具體區塊精修 |
| 中 | E：重複區塊整合 | ⚠️ 可再實機盤點，但不可再直接列為「完全未做」 |
| 中 | 測驗平台 | 📝 設計完成、未實作；需使用者提供各部門受測 SOP 範圍與題庫來源 |
| 中 | 登入/正式權限 | ⏸️ 暫停；需先確認「部門 × 角色 × 可見資料 × 查詢範圍」權限表 |
| 低 | eyebrow 字體 16px → 18px | 低優先視覺微調，不是目前阻塞 |

**重要提醒：待辦順序由使用者決定；但若使用者只問「目前還有哪些未完成」，必須先依最新 HANDOFF 淘汰舊待辦，不可把歷史待辦原樣列出。**

### 測驗平台硬性規則（2026-06-03 新增）

- 測驗功能開工前必讀 `GDP_測驗平台與考題規範.md`。
- 左側導覽需獨立設「測試專區」，至少區分「新人三個月測驗」與「年度教育訓練測驗」。
- 題目範圍必須依 `WI10-01`「參、人事」中各職務訓練評鑑要求決定；主要職務與兼任/代理職務取 SOP 聯集。
- 受測範圍可包含政府公開常見缺失，但必須取自 TFDA/PIC/S 或使用者確認的官方資料，並與公司 SOP 題庫分層管理。
- 題目、選項、答案、解析必須回第一章至第八章 SOP/WI/FR 原文或官方 GDP 來源，不得 AI 自行出題後直接上架。
- 政府公開常見缺失建議建立 `gdpGovDeficiencySources` 與 `gdpGovDeficiencyItems`；本機來源放 `官方常見缺失來源/`。
- FR24-03 列印版可顯示文件編號與訓練項目；一般學員頁仍不得顯示 SOP 路徑或全文。
- 測驗成績 >=70 分合格；低於 70 分需重測，不限次數；每次 attempt 都需保存。
- 題庫與測驗紀錄放 Firebase 前，需先確認 collection 與 rules；登入/正式權限未完成前，不得讓一般使用者讀取他人成績。

---

## 技能（Skills）同步

| 技能 | 用途 |
|------|------|
| `startup-sync` | 開工讀第二大腦與狀態 |
| `shutdown-sync` | 收工更新第二大腦、commit、push |
| `html-training-deck` | HTML 內訓教材製作規則 |
| `html-evidence-training-builder` | GDP KB 可追溯資料模型 |

- **Canonical 路徑**：`G:\我的雲端硬碟\我的第二大腦\Codex標準\skills`
- **換電腦安裝**：從 Canonical 路徑複製到 `%USERPROFILE%\.codex\skills`
- **GitHub 懶人包**：`https://github.com/N1116839/codex-skills-lazy-pack`

---

## 已知環境限制

- PowerShell 5.1 環境；`python` / `py` 不可用
- Bash tool 可用但路徑需帶完整絕對路徑
- 中文路徑用 UTF-8 儲存，避免亂碼
- Obsidian MCP 中文路徑有時亂碼，改用 Read tool 直接讀寫第二大腦

---

## 智慧查詢精準回答硬性規則（2026-05-28）

使用者確認：要達到「倉庫多久盤點一次」「溫度測繪夏季冬季月份」這種精準回答品質，不能靠一題一題碰運氣反覆修很多次。後續 GDP 智慧查詢必須用以下硬性流程處理。

1. **先拆 fact，再寫搜尋**
   - 每個 fact 只回答一件可獨立問題，例如盤點頻率、溫度測繪月份、外部教育訓練來源、訓練紀錄保存。

2. **每個 fact 固定三欄**
   - PIC/S GDP
   - 台灣 GDP
   - 新勝做法
   - 三欄都只能放正式教材內容；新勝做法要是員工可直接回答稽查員的短答。

3. **每個 fact 先設計問法**
   - 建立時先設計至少 10 種自然問法，不等使用者查不到才補 keyword。

4. **每章建立測試表**
   - 每章至少 10 個不同問題。
   - 每個問題至少 10 種問法。

5. **驗收標準**
   - 第一名是否正確。
   - 有沒有多餘不相關結果。
   - 新勝做法是否能直接作為稽查應答。

6. **修正必須批次歸因，不可亂加 keyword**
   - 泛命中：收窄 keyword、降低通用詞權重或加硬排除。
   - 查不到：補自然問法、缺字問法、同義詞與文件編號變體。
   - 回太長：再拆 fact，不把整段 SOP 塞進一筆。
   - 答案像 AI 備忘：改成正式三欄教材語氣，備忘寫進第二大腦，不進 KB。

7. **不宣稱一次到 100%**
   - 正確作法是先做一章完整樣板，通過後複製同一流程到其他章，降低反覆修正次數。

建議先從第二章人事 / 教育訓練做完整樣板：回 SOP/WI/FR 拆 fact、每個 fact 補 10 種問法、建立測試表、本機預檢、推送後用 GitHub Pages 永久網址驗收，通過後再照同一模板擴到其他章。
